// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_trafficcommand.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Identifier, Orientation3d, StatePoint, Timestamp, Vector3d } from "./osi_common";
import { InterfaceVersion } from "./osi_version";

/**
 * \brief The scenario engine provides control commands in the traffic command
 * message to traffic participant models. The control commands are based on
 * the scenario.
 *
 * \note This interface is currently just a placeholder and will be
 * changed in experimental ways to support basic scenario execution.
 * In the future this interface shall be aligned with the level of
 * atomic actions as described in OpenSCENARIO 1.0 or later.
 */
export interface TrafficCommand {
  /**
   * The interface version used by the sender (scenario engine).
   *
   * \rules
   * is_set
   * \endrules
   */
  version?:
    | InterfaceVersion
    | undefined;
  /**
   * The data timestamp of the simulation environment. Zero time is arbitrary
   * but must be identical for all messages. Zero time does not need to
   * coincide with the UNIX epoch. It is recommended to use zero timestamp as
   * the starting time point of the simulation.
   *
   * \note For traffic command data the timestamp coincides both with
   * the notional simulation time the data applies to and the time it was sent
   * There is no inherent latency for traffic command data, as opposed
   * to sensor data.
   *
   * \rules
   * is_set
   * \endrules
   */
  timestamp?:
    | Timestamp
    | undefined;
  /**
   * The ID of this traffic participant.
   *
   * \rules
   * is_set
   * \endrules
   */
  traffic_participant_id?:
    | Identifier
    | undefined;
  /**
   * Commanded traffic action(s) if any.
   *
   * \note If more than one action is supplied in one command message
   * all actions are executed in parallel.
   */
  action: TrafficAction[];
}

/**
 * \brief Atomic traffic actions to be performed.
 *
 * The transmitted commands are the atomic traffic actions described by
 * this message.
 *
 * \note This message is notionally a multiple choice selection, that is, only
 * certain combinations of atomic traffic actions shall be transmitted within
 * certain time intervals, for example, for plausibility reasons. The restrictions
 * regarding that are not part of this message, yet are seen as a task of the
 * scenario description, for example, OpenSCENARIO.
 *
 * \note All traffic actions are sent only once just before they are about
 * to start. This is also true, if their execution is expected to
 * take simulation time. To inform the traffic participant
 * model that certain actions must or shall be terminated, there are
 * explicit actions nested inside this message (AbortActionsAction,
 * EndActionsAction), which hold a reference to the respective actions.
 * Furthermore, there exists a \c TrafficCommandUpdate message for the
 * traffic participant to report back on potentially dismissed actions.
 */
export interface TrafficAction {
  /** A FollowTrajectoryAction. */
  follow_trajectory_action?:
    | TrafficAction_FollowTrajectoryAction
    | undefined;
  /** A FollowPathAction. */
  follow_path_action?:
    | TrafficAction_FollowPathAction
    | undefined;
  /** An AcquireGlobalPositionAction. */
  acquire_global_position_action?:
    | TrafficAction_AcquireGlobalPositionAction
    | undefined;
  /** A LaneChangeAction. */
  lane_change_action?:
    | TrafficAction_LaneChangeAction
    | undefined;
  /** A SpeedAction. */
  speed_action?:
    | TrafficAction_SpeedAction
    | undefined;
  /** An AbortActionsAction. */
  abort_actions_action?:
    | TrafficAction_AbortActionsAction
    | undefined;
  /** An EndActionsAction. */
  end_actions_action?:
    | TrafficAction_EndActionsAction
    | undefined;
  /** A CustomAction. */
  custom_action?:
    | TrafficAction_CustomAction
    | undefined;
  /** A LongitudinalDistanceAction. */
  longitudinal_distance_action?:
    | TrafficAction_LongitudinalDistanceAction
    | undefined;
  /** A LaneOffsetAction. */
  lane_offset_action?:
    | TrafficAction_LaneOffsetAction
    | undefined;
  /** A LateralDistanceAction. */
  lateral_distance_action?:
    | TrafficAction_LateralDistanceAction
    | undefined;
  /** A TeleportAction. */
  teleport_action?: TrafficAction_TeleportAction | undefined;
}

/** Definition of following mode. */
export enum TrafficAction_FollowingMode {
  /**
   * FOLLOWING_MODE_POSITION - Following mode position forces the traffic participant to
   * follow a path/trajectory explicitly, disregarding any internal
   * constraints, for example, steering dynamics.
   */
  FOLLOWING_MODE_POSITION = 0,
  /**
   * FOLLOWING_MODE_FOLLOW - Following mode follow allows the traffic participant to
   * treat a path/trajectory as a target, to be achieved as closely
   * as possible while retaining any internal constraints,
   * for example, steering dynamics.
   */
  FOLLOWING_MODE_FOLLOW = 1,
}

export function trafficAction_FollowingModeFromJSON(object: any): TrafficAction_FollowingMode {
  switch (object) {
    case 0:
    case "FOLLOWING_MODE_POSITION":
      return TrafficAction_FollowingMode.FOLLOWING_MODE_POSITION;
    case 1:
    case "FOLLOWING_MODE_FOLLOW":
      return TrafficAction_FollowingMode.FOLLOWING_MODE_FOLLOW;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum TrafficAction_FollowingMode");
  }
}

export function trafficAction_FollowingModeToJSON(object: TrafficAction_FollowingMode): string {
  switch (object) {
    case TrafficAction_FollowingMode.FOLLOWING_MODE_POSITION:
      return "FOLLOWING_MODE_POSITION";
    case TrafficAction_FollowingMode.FOLLOWING_MODE_FOLLOW:
      return "FOLLOWING_MODE_FOLLOW";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum TrafficAction_FollowingMode");
  }
}

/** Definition of dynamic shapes. */
export enum TrafficAction_DynamicsShape {
  /** DYNAMICS_SHAPE_UNSPECIFIED - Shape is unspecified. */
  DYNAMICS_SHAPE_UNSPECIFIED = 0,
  /** DYNAMICS_SHAPE_LINEAR - Shape is linear. */
  DYNAMICS_SHAPE_LINEAR = 1,
  /** DYNAMICS_SHAPE_CUBIC - Shape is cubic. */
  DYNAMICS_SHAPE_CUBIC = 2,
  /** DYNAMICS_SHAPE_SINUSOIDAL - Shape is sinusoidal. */
  DYNAMICS_SHAPE_SINUSOIDAL = 3,
  /** DYNAMICS_SHAPE_STEP - Shape is a step function. */
  DYNAMICS_SHAPE_STEP = 4,
}

export function trafficAction_DynamicsShapeFromJSON(object: any): TrafficAction_DynamicsShape {
  switch (object) {
    case 0:
    case "DYNAMICS_SHAPE_UNSPECIFIED":
      return TrafficAction_DynamicsShape.DYNAMICS_SHAPE_UNSPECIFIED;
    case 1:
    case "DYNAMICS_SHAPE_LINEAR":
      return TrafficAction_DynamicsShape.DYNAMICS_SHAPE_LINEAR;
    case 2:
    case "DYNAMICS_SHAPE_CUBIC":
      return TrafficAction_DynamicsShape.DYNAMICS_SHAPE_CUBIC;
    case 3:
    case "DYNAMICS_SHAPE_SINUSOIDAL":
      return TrafficAction_DynamicsShape.DYNAMICS_SHAPE_SINUSOIDAL;
    case 4:
    case "DYNAMICS_SHAPE_STEP":
      return TrafficAction_DynamicsShape.DYNAMICS_SHAPE_STEP;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum TrafficAction_DynamicsShape");
  }
}

export function trafficAction_DynamicsShapeToJSON(object: TrafficAction_DynamicsShape): string {
  switch (object) {
    case TrafficAction_DynamicsShape.DYNAMICS_SHAPE_UNSPECIFIED:
      return "DYNAMICS_SHAPE_UNSPECIFIED";
    case TrafficAction_DynamicsShape.DYNAMICS_SHAPE_LINEAR:
      return "DYNAMICS_SHAPE_LINEAR";
    case TrafficAction_DynamicsShape.DYNAMICS_SHAPE_CUBIC:
      return "DYNAMICS_SHAPE_CUBIC";
    case TrafficAction_DynamicsShape.DYNAMICS_SHAPE_SINUSOIDAL:
      return "DYNAMICS_SHAPE_SINUSOIDAL";
    case TrafficAction_DynamicsShape.DYNAMICS_SHAPE_STEP:
      return "DYNAMICS_SHAPE_STEP";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum TrafficAction_DynamicsShape");
  }
}

/** \brief The action header */
export interface TrafficAction_ActionHeader {
  /**
   * The unique id of the action.
   *
   * \note This field is mandatory.
   *
   * \note This id must be unique within all traffic command
   * messages exchanged with one traffic participant.
   *
   * \rules
   * is_set
   * \endrules
   */
  action_id?: Identifier | undefined;
}

/**
 * \brief DynamicConstraints
 *
 * This message is used in Longitudinal and Lateral Distance Actions as
 * constraints on the TrafficParticipant not allowing them to exceed/drop below
 * certain speeds/acceleration levels.
 */
export interface TrafficAction_DynamicConstraints {
  /**
   * Maximum acceleration the distance controller is allowed to use for keeping distance.
   *
   * Unit: m/s^2
   */
  max_acceleration?:
    | number
    | undefined;
  /**
   * Maximum deceleration the distance controller is allowed to use for keeping distance.
   *
   * Unit: m/s^2
   */
  max_deceleration?:
    | number
    | undefined;
  /**
   * Maximum speed the distance controller is allowed to use for keeping distance.
   *
   * Unit: m/s
   */
  max_speed?: number | undefined;
}

/**
 * \brief Follow trajectory action.
 *
 * Controls a traffic participant to follow a trajectory using vertices
 * with timings. It specifies the motion in space as a function of time.
 *
 * \note The StatePoint messages in trajectory_point requires a
 * specified timestamp.
 *
 * \note This action is aligned with the FollowTrajectoryAction of
 * OpenSCENARIO 1.0 using a 4/7D trajectory with shape Polyline.
 */
export interface TrafficAction_FollowTrajectoryAction {
  /** The action header. */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /**
   * A list of trajectory StatePoints.
   *
   * Set the timestamp fields and position fields for all
   * trajectory points. The orientation fields can be set
   * depending on the constrain_orientation field being true.
   *
   * \note OSI uses singular instead of plural for repeated field names.
   */
  trajectory_point: StatePoint[];
  /**
   * Constrain the orientation of the traffic participant as specified
   * by the trajectory points.
   *
   * This boolean flag defines whether orientation values supplied in
   * the trajectory points (if any) shall be used to constrain the
   * orientation of the traffic participant or not.
   */
  constrain_orientation?:
    | boolean
    | undefined;
  /**
   * Specify the following mode that should be employed in executing
   * the trajectory.
   */
  following_mode?: TrafficAction_FollowingMode | undefined;
}

/**
 * \brief Follow path action.
 *
 * Controls a traffic participant to follow a path using vertices. It
 * specifies the motion in space independent of time.
 *
 * \note The StatePoint messages in path_point only requires a specified
 * position field. The orientation can be set optionally. Any
 * timestamp StatePoint values are ignored.
 *
 * \note This action is aligned with the FollowTrajectoryAction of
 * OpenSCENARIO 1.0 using a 3/6D trajectory with shape Polyline.
 */
export interface TrafficAction_FollowPathAction {
  /** The action header */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /**
   * A list of path StatePoints
   *
   * Set the position fields for all path points.
   * The timestamp field are not required and ignored.
   * The orientation fields can be set depending on the constrain_orientation
   * field being true.
   *
   * \note OSI uses singular instead of plural for repeated field names.
   */
  path_point: StatePoint[];
  /**
   * Constrain the orientation of the traffic participant as specified
   * by the path points.
   *
   * This boolean flag defines whether orientation values supplied in
   * the path points shall be used to constrain the orientation
   * of the traffic participant or not.
   */
  constrain_orientation?:
    | boolean
    | undefined;
  /**
   * Specify the following mode that should be employed in executing
   * the path.
   */
  following_mode?: TrafficAction_FollowingMode | undefined;
}

/**
 * \brief Acquire global position action.
 *
 * This action assigns a route to a traffic participant. The route
 * assigned will be the shortest route (along roads or satisfying any
 * other constraints a traffic participant is operating under) between
 * the traffic participant's current position and the position specified.
 *
 * As with all routing actions, the exact way this route is achieved is
 * under the control of the traffic participant model.
 *
 * \note This action is aligned with the AcquirePositionAction of
 * OpenSCENARIO 1.0 using a WorldPosition position argument.
 */
export interface TrafficAction_AcquireGlobalPositionAction {
  /** The action header. */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /**
   * Position in the global coordinate system.
   *
   * The position refers to the center (x,y,z) of the bounding box.
   *
   * \note Remark: The definition of the reference point follows the
   * specification of the \c BaseMoving message.
   */
  position?:
    | Vector3d
    | undefined;
  /**
   * Orientation in the global coordinate system.
   *
   * This is optional. If no orientation is given, the end orientation
   * is under control of the traffic participant.
   */
  orientation?: Orientation3d | undefined;
}

/**
 * \brief Lane change action.
 *
 * This action assigns a lane change to a traffic participant. The fields
 * of this message enable different levels of constraint for the traffic
 * participant depending on the use case. In case the constraints are
 * supposed to be small, the dynamics shape or even the duration and
 * the distance for the lane change can be omitted.
 *
 * \note This action is aligned with LaneChangeAction of OpenSCENARIO 1.0
 * defining the targeted lane and optionally the parametrization of the
 * lane change.
 */
export interface TrafficAction_LaneChangeAction {
  /** The action header. */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /**
   * Targeted lane relative to the current lane.
   *
   * Convention: +1 means to the right, -1 means to the left.
   */
  relative_target_lane?:
    | number
    | undefined;
  /**
   * Specified shape of the lane change action.
   * If the shape is unspecified (enum value 0), the shape of the
   * lane change is open to the traffic participant model.
   */
  dynamics_shape?:
    | TrafficAction_DynamicsShape
    | undefined;
  /**
   * Duration of the lane change.
   *
   * A value of 0.0 (the default value) imposes no constraint
   * on the duration, unless the dynamics shape is a step function,
   * where an immediate step is effected.
   *
   * Unit: s
   */
  duration?:
    | number
    | undefined;
  /**
   * Distance of the lane change.
   *
   * A value of 0.0 (the default value) imposes no constraint
   * on the distance, unless the dynamics shape is a step function,
   * where an immediate step is effected.
   *
   * Unit: m
   */
  distance?: number | undefined;
}

/**
 * \brief Speed action.
 *
 * This action assigns a targeted speed to a traffic participant.
 * The action can be constrained by specifying the dynamics
 * shape or even the duration and the distance of the acceleration /
 * deceleration process.
 *
 * \note This action is aligned with SpeedAction of OpenSCENARIO 1.0
 * defining the targeted speed and optionally the parametrization of the
 * speed transition.
 */
export interface TrafficAction_SpeedAction {
  /** The action header */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /** Targeted absolute speed. */
  absolute_target_speed?:
    | number
    | undefined;
  /**
   * Specified transition shape of the speed change action.
   * If the shape is unspecified (enum value 0), the shape of the
   * speed change is open to the traffic participant model.
   */
  dynamics_shape?:
    | TrafficAction_DynamicsShape
    | undefined;
  /**
   * Duration of the speed change.
   *
   * A value of 0.0 (the default value) imposes no constraint
   * on the duration, unless the dynamics shape is a step function,
   * where an immediate step is effected.
   *
   * Unit: s
   */
  duration?:
    | number
    | undefined;
  /**
   * Distance of the speed change.
   *
   * A value of 0.0 (the default value) imposes no constraint
   * on the distance, unless the dynamics shape is a step function,
   * where an immediate step is effected.
   *
   * Unit: m
   */
  distance?: number | undefined;
}

/**
 * \brief AbortActionsAction.
 *
 * This action tells a traffic participant that it should immediately
 * abort the execution of other actions referenced within this action.
 * In contrast to the EndActionsAction this action forces a hard
 * termination of the referenced actions.
 */
export interface TrafficAction_AbortActionsAction {
  /** The action header */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /**
   * Actions which must be aborted immediately.
   *
   * These fields hold the action ids of the actions that must be
   * aborted immediately.
   */
  target_action_id: Identifier[];
}

/**
 * \brief End actions action.
 *
 * This action tells a traffic participant that the execution of the
 * referenced actions is regarded as successfully performed. The
 * termination of the referenced actions is allowed to be performed
 * gracefully.
 */
export interface TrafficAction_EndActionsAction {
  /** The action header */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /**
   * Actions which are regarded as successfully executed.
   *
   * These fields hold the action ids of the actions that are regarded
   * as successfully executed and shall be terminated gracefully.
   */
  target_action_id: Identifier[];
}

/**
 * \brief Custom Action.
 *
 * This action assigns a custom command to a traffic participant.
 * The syntax and semantics of this action is implementation defined.
 * It is entirely up to each individual traffic participant which
 * custom commands it supports, if any, and how they must be phrased.
 *
 * \note This action is aligned with CustomCommandAction of OpenSCENARIO 1.0
 * providing a customizable user defined action.
 */
export interface TrafficAction_CustomAction {
  /** The Action Header. */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /**
   * The custom command given to the traffic participant. Used to convey a specific instruction
   * (for example, "exit_highway"), or event (for example, "left_indicator_activated").
   *
   * \note This corresponds to the content of the OpenSCENARIO 1.0 CustomCommandAction field.
   */
  command?:
    | string
    | undefined;
  /**
   * The type of the custom command given to the traffic participant. Can be used to simplify
   * how commands are grouped. For example, the command_type could be "sensor_failure" and the
   * command value could be "front_right_camera".  This avoids long commands, like
   * "sensor_failure: front_right_camera".
   *
   * \note This corresponds to the "type" attribute of the OpenSCENARIO 1.0 CustomCommandAction.
   */
  command_type?: string | undefined;
}

/**
 * \brief Longitudinal Distance Action
 *
 * The action tells the (host) traffic participant to reach a certain longitudinal distance
 * relative to a target traffic participant. The longitudinal distance is defined as the distance
 * along the centerline of the lane, on which the (host) traffic participant is currently located.
 * The interpolation strategy between centerline points for calculating
 * that distance along the centerline is open to the traffic participant modeler.
 *
 * \note This action is aligned with LongitudinalDistanceAction of OpenSCENARIO 1.0
 * defining the reference traffic participant and the distance.
 *
 * \note Limitation: This concept currently only works for lanes with a centerline, i.e. for lanes
 * of TYPE_DRIVING, not for lanes of TYPE_NONDRIVING or TYPE_INTERSECTION.
 */
export interface TrafficAction_LongitudinalDistanceAction {
  /** Action Header of the message */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /** Unique Id of the reference traffic participant to which the distance is defined. */
  target_traffic_participant_id?:
    | Identifier
    | undefined;
  /**
   * The distance to reach along the lane
   *
   * Unit: m
   */
  distance?:
    | number
    | undefined;
  /**
   * Determine the measurement of longitudinal distance between the traffic
   * participant receiving this action and the reference traffic
   * participant.
   * True: Longitudinal distance is measured using the distance between closest bounding box points.
   * False: Longitudinal distance is measured using the distance between the center of each object's bounding box.
   */
  freespace?:
    | boolean
    | undefined;
  /**
   * Define whether the traffic participant should only reach the distance once
   * or if it should also keep the distance after having reached it.
   * True: the traffic participant shall approach the reference participant
   * and follow with the distance specified until the action is aborted (communicate with AbortActionsAction).
   * False: the action ends as soon as the traffic participant has reached
   * the specified distance (communicate with EndActionsAction).
   */
  follow?:
    | boolean
    | undefined;
  /**
   * Parameter that assigns either unlimited dynamics (if omitted)
   * or limited maxAcceleration/maxDeceleration/maxSpeed to the action.
   */
  dynamic_constraints?: TrafficAction_DynamicConstraints | undefined;
}

/**
 * \brief Lateral Distance Action
 *
 * The action tells the (host) traffic participant to reach a certain lateral distance relative to
 * a target traffic participant. The lateral distance is defined along an imaginative perpendicular line
 * with respect to the centerline of the current (host) traffic participant's lane.
 * The interpolation strategy between centerline points for calculating that distance along the imaginative
 * perpendicular line is open to the traffic participant modeler.
 *
 * \note This action is aligned with LateralDistanceAction of OpenSCENARIO 1.0
 * defining the reference traffic participant and the distance.
 *
 * \note Limitation: This concept currently only works for lanes with a centerline, i.e. for lanes
 * of TYPE_DRIVING, not for lanes of TYPE_NONDRIVING or TYPE_INTERSECTION.
 */
export interface TrafficAction_LateralDistanceAction {
  /** Action Header of the message */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /** Id of the reference entity the lateral distance shall be kept to. */
  target_traffic_participant_id?:
    | Identifier
    | undefined;
  /**
   * Lateral distance value.
   *
   * Unit: m
   */
  distance?:
    | number
    | undefined;
  /**
   * Determine the measurement of lateral distance between the traffic
   * participant receiving this action and the reference traffic
   * participant.
   * True: Lateral distance is measured using the distance between closest bounding box points.
   * False: Lateral distance is measured using the distance between the center of each object's bounding box.
   */
  freespace?:
    | boolean
    | undefined;
  /**
   * Define whether the traffic participant should only reach the distance once
   * or if it should also keep the distance after having reached it.
   * True: the traffic participant shall approach the reference participant
   * and follow with the distance specified until the action is aborted (communicate with AbortActionsAction).
   * False: the action ends as soon as the traffic participant has reached
   * the specified distance (communicate with EndActionsAction).
   */
  follow?:
    | boolean
    | undefined;
  /**
   * Parameter that assigns either unlimited dynamics (if omitted)
   * or limited maxAcceleration/maxDeceleration/maxSpeed to the action.
   */
  dynamic_constraints?: TrafficAction_DynamicConstraints | undefined;
}

/**
 * \brief Lane Offset Action
 *
 * This action assigns a targeted lane offset to a traffic participant.
 * The action can be constrained by specifying the dynamics shape of the
 * lateral transition profile.
 *
 * \note This action is aligned with LaneOffsetAction of OpenSCENARIO 1.0.
 */
export interface TrafficAction_LaneOffsetAction {
  /** The Action Header */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /**
   * Targeted lane offset in meters relative to the centerline of the
   * current traffic participant's lane. Positive values mean left of the centerline,
   * negative values mean right of the centerline.
   *
   * Unit: m
   */
  target_lane_offset?:
    | number
    | undefined;
  /**
   * Specified transition shape of reaching the lane offset.
   * If the shape is unspecified (enum value 0), the shape of the
   * transition is open to the traffic participant model.
   */
  dynamics_shape?: TrafficAction_DynamicsShape | undefined;
}

/**
 * \brief Teleport Action
 *
 * This Action allows to immediately place a traffic participant to
 * another position.
 *
 * \note This action is aligned with the TeleportAction of
 * OpenSCENARIO 1.0 using a WorldPosition position argument.
 */
export interface TrafficAction_TeleportAction {
  /** The Action Header */
  action_header?:
    | TrafficAction_ActionHeader
    | undefined;
  /**
   * Position in the global coordinate system.
   *
   * The position refers to the center (x,y,z) of the bounding box.
   *
   * \note Remark: The definition of the reference point follows the
   * specification of the \c BaseMoving message.
   */
  position?:
    | Vector3d
    | undefined;
  /**
   * Orientation in the global coordinate system.
   *
   * This is optional, if no orientation is given, the end orientation
   * is under control of the traffic participant.
   */
  orientation?: Orientation3d | undefined;
}

function createBaseTrafficCommand(): TrafficCommand {
  return { version: undefined, timestamp: undefined, traffic_participant_id: undefined, action: [] };
}

export const TrafficCommand: MessageFns<TrafficCommand> = {
  encode(message: TrafficCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== undefined) {
      InterfaceVersion.encode(message.version, writer.uint32(10).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(18).fork()).join();
    }
    if (message.traffic_participant_id !== undefined) {
      Identifier.encode(message.traffic_participant_id, writer.uint32(26).fork()).join();
    }
    for (const v of message.action) {
      TrafficAction.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = InterfaceVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.traffic_participant_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.action.push(TrafficAction.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficCommand {
    return {
      version: isSet(object.version) ? InterfaceVersion.fromJSON(object.version) : undefined,
      timestamp: isSet(object.timestamp) ? Timestamp.fromJSON(object.timestamp) : undefined,
      traffic_participant_id: isSet(object.traffic_participant_id)
        ? Identifier.fromJSON(object.traffic_participant_id)
        : undefined,
      action: globalThis.Array.isArray(object?.action) ? object.action.map((e: any) => TrafficAction.fromJSON(e)) : [],
    };
  },

  toJSON(message: TrafficCommand): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = InterfaceVersion.toJSON(message.version);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = Timestamp.toJSON(message.timestamp);
    }
    if (message.traffic_participant_id !== undefined) {
      obj.traffic_participant_id = Identifier.toJSON(message.traffic_participant_id);
    }
    if (message.action?.length) {
      obj.action = message.action.map((e) => TrafficAction.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficCommand>, I>>(base?: I): TrafficCommand {
    return TrafficCommand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficCommand>, I>>(object: I): TrafficCommand {
    const message = createBaseTrafficCommand();
    message.version = (object.version !== undefined && object.version !== null)
      ? InterfaceVersion.fromPartial(object.version)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.traffic_participant_id =
      (object.traffic_participant_id !== undefined && object.traffic_participant_id !== null)
        ? Identifier.fromPartial(object.traffic_participant_id)
        : undefined;
    message.action = object.action?.map((e) => TrafficAction.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTrafficAction(): TrafficAction {
  return {
    follow_trajectory_action: undefined,
    follow_path_action: undefined,
    acquire_global_position_action: undefined,
    lane_change_action: undefined,
    speed_action: undefined,
    abort_actions_action: undefined,
    end_actions_action: undefined,
    custom_action: undefined,
    longitudinal_distance_action: undefined,
    lane_offset_action: undefined,
    lateral_distance_action: undefined,
    teleport_action: undefined,
  };
}

export const TrafficAction: MessageFns<TrafficAction> = {
  encode(message: TrafficAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.follow_trajectory_action !== undefined) {
      TrafficAction_FollowTrajectoryAction.encode(message.follow_trajectory_action, writer.uint32(10).fork()).join();
    }
    if (message.follow_path_action !== undefined) {
      TrafficAction_FollowPathAction.encode(message.follow_path_action, writer.uint32(18).fork()).join();
    }
    if (message.acquire_global_position_action !== undefined) {
      TrafficAction_AcquireGlobalPositionAction.encode(message.acquire_global_position_action, writer.uint32(26).fork())
        .join();
    }
    if (message.lane_change_action !== undefined) {
      TrafficAction_LaneChangeAction.encode(message.lane_change_action, writer.uint32(34).fork()).join();
    }
    if (message.speed_action !== undefined) {
      TrafficAction_SpeedAction.encode(message.speed_action, writer.uint32(42).fork()).join();
    }
    if (message.abort_actions_action !== undefined) {
      TrafficAction_AbortActionsAction.encode(message.abort_actions_action, writer.uint32(50).fork()).join();
    }
    if (message.end_actions_action !== undefined) {
      TrafficAction_EndActionsAction.encode(message.end_actions_action, writer.uint32(58).fork()).join();
    }
    if (message.custom_action !== undefined) {
      TrafficAction_CustomAction.encode(message.custom_action, writer.uint32(66).fork()).join();
    }
    if (message.longitudinal_distance_action !== undefined) {
      TrafficAction_LongitudinalDistanceAction.encode(message.longitudinal_distance_action, writer.uint32(74).fork())
        .join();
    }
    if (message.lane_offset_action !== undefined) {
      TrafficAction_LaneOffsetAction.encode(message.lane_offset_action, writer.uint32(82).fork()).join();
    }
    if (message.lateral_distance_action !== undefined) {
      TrafficAction_LateralDistanceAction.encode(message.lateral_distance_action, writer.uint32(90).fork()).join();
    }
    if (message.teleport_action !== undefined) {
      TrafficAction_TeleportAction.encode(message.teleport_action, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.follow_trajectory_action = TrafficAction_FollowTrajectoryAction.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.follow_path_action = TrafficAction_FollowPathAction.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.acquire_global_position_action = TrafficAction_AcquireGlobalPositionAction.decode(
            reader,
            reader.uint32(),
          );
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.lane_change_action = TrafficAction_LaneChangeAction.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.speed_action = TrafficAction_SpeedAction.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.abort_actions_action = TrafficAction_AbortActionsAction.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.end_actions_action = TrafficAction_EndActionsAction.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.custom_action = TrafficAction_CustomAction.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.longitudinal_distance_action = TrafficAction_LongitudinalDistanceAction.decode(
            reader,
            reader.uint32(),
          );
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.lane_offset_action = TrafficAction_LaneOffsetAction.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.lateral_distance_action = TrafficAction_LateralDistanceAction.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.teleport_action = TrafficAction_TeleportAction.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction {
    return {
      follow_trajectory_action: isSet(object.follow_trajectory_action)
        ? TrafficAction_FollowTrajectoryAction.fromJSON(object.follow_trajectory_action)
        : undefined,
      follow_path_action: isSet(object.follow_path_action)
        ? TrafficAction_FollowPathAction.fromJSON(object.follow_path_action)
        : undefined,
      acquire_global_position_action: isSet(object.acquire_global_position_action)
        ? TrafficAction_AcquireGlobalPositionAction.fromJSON(object.acquire_global_position_action)
        : undefined,
      lane_change_action: isSet(object.lane_change_action)
        ? TrafficAction_LaneChangeAction.fromJSON(object.lane_change_action)
        : undefined,
      speed_action: isSet(object.speed_action) ? TrafficAction_SpeedAction.fromJSON(object.speed_action) : undefined,
      abort_actions_action: isSet(object.abort_actions_action)
        ? TrafficAction_AbortActionsAction.fromJSON(object.abort_actions_action)
        : undefined,
      end_actions_action: isSet(object.end_actions_action)
        ? TrafficAction_EndActionsAction.fromJSON(object.end_actions_action)
        : undefined,
      custom_action: isSet(object.custom_action)
        ? TrafficAction_CustomAction.fromJSON(object.custom_action)
        : undefined,
      longitudinal_distance_action: isSet(object.longitudinal_distance_action)
        ? TrafficAction_LongitudinalDistanceAction.fromJSON(object.longitudinal_distance_action)
        : undefined,
      lane_offset_action: isSet(object.lane_offset_action)
        ? TrafficAction_LaneOffsetAction.fromJSON(object.lane_offset_action)
        : undefined,
      lateral_distance_action: isSet(object.lateral_distance_action)
        ? TrafficAction_LateralDistanceAction.fromJSON(object.lateral_distance_action)
        : undefined,
      teleport_action: isSet(object.teleport_action)
        ? TrafficAction_TeleportAction.fromJSON(object.teleport_action)
        : undefined,
    };
  },

  toJSON(message: TrafficAction): unknown {
    const obj: any = {};
    if (message.follow_trajectory_action !== undefined) {
      obj.follow_trajectory_action = TrafficAction_FollowTrajectoryAction.toJSON(message.follow_trajectory_action);
    }
    if (message.follow_path_action !== undefined) {
      obj.follow_path_action = TrafficAction_FollowPathAction.toJSON(message.follow_path_action);
    }
    if (message.acquire_global_position_action !== undefined) {
      obj.acquire_global_position_action = TrafficAction_AcquireGlobalPositionAction.toJSON(
        message.acquire_global_position_action,
      );
    }
    if (message.lane_change_action !== undefined) {
      obj.lane_change_action = TrafficAction_LaneChangeAction.toJSON(message.lane_change_action);
    }
    if (message.speed_action !== undefined) {
      obj.speed_action = TrafficAction_SpeedAction.toJSON(message.speed_action);
    }
    if (message.abort_actions_action !== undefined) {
      obj.abort_actions_action = TrafficAction_AbortActionsAction.toJSON(message.abort_actions_action);
    }
    if (message.end_actions_action !== undefined) {
      obj.end_actions_action = TrafficAction_EndActionsAction.toJSON(message.end_actions_action);
    }
    if (message.custom_action !== undefined) {
      obj.custom_action = TrafficAction_CustomAction.toJSON(message.custom_action);
    }
    if (message.longitudinal_distance_action !== undefined) {
      obj.longitudinal_distance_action = TrafficAction_LongitudinalDistanceAction.toJSON(
        message.longitudinal_distance_action,
      );
    }
    if (message.lane_offset_action !== undefined) {
      obj.lane_offset_action = TrafficAction_LaneOffsetAction.toJSON(message.lane_offset_action);
    }
    if (message.lateral_distance_action !== undefined) {
      obj.lateral_distance_action = TrafficAction_LateralDistanceAction.toJSON(message.lateral_distance_action);
    }
    if (message.teleport_action !== undefined) {
      obj.teleport_action = TrafficAction_TeleportAction.toJSON(message.teleport_action);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction>, I>>(base?: I): TrafficAction {
    return TrafficAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction>, I>>(object: I): TrafficAction {
    const message = createBaseTrafficAction();
    message.follow_trajectory_action =
      (object.follow_trajectory_action !== undefined && object.follow_trajectory_action !== null)
        ? TrafficAction_FollowTrajectoryAction.fromPartial(object.follow_trajectory_action)
        : undefined;
    message.follow_path_action = (object.follow_path_action !== undefined && object.follow_path_action !== null)
      ? TrafficAction_FollowPathAction.fromPartial(object.follow_path_action)
      : undefined;
    message.acquire_global_position_action =
      (object.acquire_global_position_action !== undefined && object.acquire_global_position_action !== null)
        ? TrafficAction_AcquireGlobalPositionAction.fromPartial(object.acquire_global_position_action)
        : undefined;
    message.lane_change_action = (object.lane_change_action !== undefined && object.lane_change_action !== null)
      ? TrafficAction_LaneChangeAction.fromPartial(object.lane_change_action)
      : undefined;
    message.speed_action = (object.speed_action !== undefined && object.speed_action !== null)
      ? TrafficAction_SpeedAction.fromPartial(object.speed_action)
      : undefined;
    message.abort_actions_action = (object.abort_actions_action !== undefined && object.abort_actions_action !== null)
      ? TrafficAction_AbortActionsAction.fromPartial(object.abort_actions_action)
      : undefined;
    message.end_actions_action = (object.end_actions_action !== undefined && object.end_actions_action !== null)
      ? TrafficAction_EndActionsAction.fromPartial(object.end_actions_action)
      : undefined;
    message.custom_action = (object.custom_action !== undefined && object.custom_action !== null)
      ? TrafficAction_CustomAction.fromPartial(object.custom_action)
      : undefined;
    message.longitudinal_distance_action =
      (object.longitudinal_distance_action !== undefined && object.longitudinal_distance_action !== null)
        ? TrafficAction_LongitudinalDistanceAction.fromPartial(object.longitudinal_distance_action)
        : undefined;
    message.lane_offset_action = (object.lane_offset_action !== undefined && object.lane_offset_action !== null)
      ? TrafficAction_LaneOffsetAction.fromPartial(object.lane_offset_action)
      : undefined;
    message.lateral_distance_action =
      (object.lateral_distance_action !== undefined && object.lateral_distance_action !== null)
        ? TrafficAction_LateralDistanceAction.fromPartial(object.lateral_distance_action)
        : undefined;
    message.teleport_action = (object.teleport_action !== undefined && object.teleport_action !== null)
      ? TrafficAction_TeleportAction.fromPartial(object.teleport_action)
      : undefined;
    return message;
  },
};

function createBaseTrafficAction_ActionHeader(): TrafficAction_ActionHeader {
  return { action_id: undefined };
}

export const TrafficAction_ActionHeader: MessageFns<TrafficAction_ActionHeader> = {
  encode(message: TrafficAction_ActionHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_id !== undefined) {
      Identifier.encode(message.action_id, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_ActionHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_ActionHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_ActionHeader {
    return { action_id: isSet(object.action_id) ? Identifier.fromJSON(object.action_id) : undefined };
  },

  toJSON(message: TrafficAction_ActionHeader): unknown {
    const obj: any = {};
    if (message.action_id !== undefined) {
      obj.action_id = Identifier.toJSON(message.action_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_ActionHeader>, I>>(base?: I): TrafficAction_ActionHeader {
    return TrafficAction_ActionHeader.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_ActionHeader>, I>>(object: I): TrafficAction_ActionHeader {
    const message = createBaseTrafficAction_ActionHeader();
    message.action_id = (object.action_id !== undefined && object.action_id !== null)
      ? Identifier.fromPartial(object.action_id)
      : undefined;
    return message;
  },
};

function createBaseTrafficAction_DynamicConstraints(): TrafficAction_DynamicConstraints {
  return { max_acceleration: 0, max_deceleration: 0, max_speed: 0 };
}

export const TrafficAction_DynamicConstraints: MessageFns<TrafficAction_DynamicConstraints> = {
  encode(message: TrafficAction_DynamicConstraints, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.max_acceleration !== undefined && message.max_acceleration !== 0) {
      writer.uint32(9).double(message.max_acceleration);
    }
    if (message.max_deceleration !== undefined && message.max_deceleration !== 0) {
      writer.uint32(17).double(message.max_deceleration);
    }
    if (message.max_speed !== undefined && message.max_speed !== 0) {
      writer.uint32(25).double(message.max_speed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_DynamicConstraints {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_DynamicConstraints();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.max_acceleration = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.max_deceleration = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.max_speed = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_DynamicConstraints {
    return {
      max_acceleration: isSet(object.max_acceleration) ? globalThis.Number(object.max_acceleration) : 0,
      max_deceleration: isSet(object.max_deceleration) ? globalThis.Number(object.max_deceleration) : 0,
      max_speed: isSet(object.max_speed) ? globalThis.Number(object.max_speed) : 0,
    };
  },

  toJSON(message: TrafficAction_DynamicConstraints): unknown {
    const obj: any = {};
    if (message.max_acceleration !== undefined) {
      obj.max_acceleration = message.max_acceleration;
    }
    if (message.max_deceleration !== undefined) {
      obj.max_deceleration = message.max_deceleration;
    }
    if (message.max_speed !== undefined) {
      obj.max_speed = message.max_speed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_DynamicConstraints>, I>>(
    base?: I,
  ): TrafficAction_DynamicConstraints {
    return TrafficAction_DynamicConstraints.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_DynamicConstraints>, I>>(
    object: I,
  ): TrafficAction_DynamicConstraints {
    const message = createBaseTrafficAction_DynamicConstraints();
    message.max_acceleration = object.max_acceleration ?? 0;
    message.max_deceleration = object.max_deceleration ?? 0;
    message.max_speed = object.max_speed ?? 0;
    return message;
  },
};

function createBaseTrafficAction_FollowTrajectoryAction(): TrafficAction_FollowTrajectoryAction {
  return { action_header: undefined, trajectory_point: [], constrain_orientation: false, following_mode: 0 };
}

export const TrafficAction_FollowTrajectoryAction: MessageFns<TrafficAction_FollowTrajectoryAction> = {
  encode(message: TrafficAction_FollowTrajectoryAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    for (const v of message.trajectory_point) {
      StatePoint.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.constrain_orientation !== undefined && message.constrain_orientation !== false) {
      writer.uint32(24).bool(message.constrain_orientation);
    }
    if (message.following_mode !== undefined && message.following_mode !== 0) {
      writer.uint32(32).int32(message.following_mode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_FollowTrajectoryAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_FollowTrajectoryAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.trajectory_point.push(StatePoint.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.constrain_orientation = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.following_mode = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_FollowTrajectoryAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      trajectory_point: globalThis.Array.isArray(object?.trajectory_point)
        ? object.trajectory_point.map((e: any) => StatePoint.fromJSON(e))
        : [],
      constrain_orientation: isSet(object.constrain_orientation)
        ? globalThis.Boolean(object.constrain_orientation)
        : false,
      following_mode: isSet(object.following_mode) ? trafficAction_FollowingModeFromJSON(object.following_mode) : 0,
    };
  },

  toJSON(message: TrafficAction_FollowTrajectoryAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.trajectory_point?.length) {
      obj.trajectory_point = message.trajectory_point.map((e) => StatePoint.toJSON(e));
    }
    if (message.constrain_orientation !== undefined) {
      obj.constrain_orientation = message.constrain_orientation;
    }
    if (message.following_mode !== undefined) {
      obj.following_mode = trafficAction_FollowingModeToJSON(message.following_mode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_FollowTrajectoryAction>, I>>(
    base?: I,
  ): TrafficAction_FollowTrajectoryAction {
    return TrafficAction_FollowTrajectoryAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_FollowTrajectoryAction>, I>>(
    object: I,
  ): TrafficAction_FollowTrajectoryAction {
    const message = createBaseTrafficAction_FollowTrajectoryAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.trajectory_point = object.trajectory_point?.map((e) => StatePoint.fromPartial(e)) || [];
    message.constrain_orientation = object.constrain_orientation ?? false;
    message.following_mode = object.following_mode ?? 0;
    return message;
  },
};

function createBaseTrafficAction_FollowPathAction(): TrafficAction_FollowPathAction {
  return { action_header: undefined, path_point: [], constrain_orientation: false, following_mode: 0 };
}

export const TrafficAction_FollowPathAction: MessageFns<TrafficAction_FollowPathAction> = {
  encode(message: TrafficAction_FollowPathAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    for (const v of message.path_point) {
      StatePoint.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.constrain_orientation !== undefined && message.constrain_orientation !== false) {
      writer.uint32(24).bool(message.constrain_orientation);
    }
    if (message.following_mode !== undefined && message.following_mode !== 0) {
      writer.uint32(32).int32(message.following_mode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_FollowPathAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_FollowPathAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path_point.push(StatePoint.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.constrain_orientation = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.following_mode = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_FollowPathAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      path_point: globalThis.Array.isArray(object?.path_point)
        ? object.path_point.map((e: any) => StatePoint.fromJSON(e))
        : [],
      constrain_orientation: isSet(object.constrain_orientation)
        ? globalThis.Boolean(object.constrain_orientation)
        : false,
      following_mode: isSet(object.following_mode) ? trafficAction_FollowingModeFromJSON(object.following_mode) : 0,
    };
  },

  toJSON(message: TrafficAction_FollowPathAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.path_point?.length) {
      obj.path_point = message.path_point.map((e) => StatePoint.toJSON(e));
    }
    if (message.constrain_orientation !== undefined) {
      obj.constrain_orientation = message.constrain_orientation;
    }
    if (message.following_mode !== undefined) {
      obj.following_mode = trafficAction_FollowingModeToJSON(message.following_mode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_FollowPathAction>, I>>(base?: I): TrafficAction_FollowPathAction {
    return TrafficAction_FollowPathAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_FollowPathAction>, I>>(
    object: I,
  ): TrafficAction_FollowPathAction {
    const message = createBaseTrafficAction_FollowPathAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.path_point = object.path_point?.map((e) => StatePoint.fromPartial(e)) || [];
    message.constrain_orientation = object.constrain_orientation ?? false;
    message.following_mode = object.following_mode ?? 0;
    return message;
  },
};

function createBaseTrafficAction_AcquireGlobalPositionAction(): TrafficAction_AcquireGlobalPositionAction {
  return { action_header: undefined, position: undefined, orientation: undefined };
}

export const TrafficAction_AcquireGlobalPositionAction: MessageFns<TrafficAction_AcquireGlobalPositionAction> = {
  encode(message: TrafficAction_AcquireGlobalPositionAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    if (message.position !== undefined) {
      Vector3d.encode(message.position, writer.uint32(18).fork()).join();
    }
    if (message.orientation !== undefined) {
      Orientation3d.encode(message.orientation, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_AcquireGlobalPositionAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_AcquireGlobalPositionAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.position = Vector3d.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.orientation = Orientation3d.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_AcquireGlobalPositionAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      position: isSet(object.position) ? Vector3d.fromJSON(object.position) : undefined,
      orientation: isSet(object.orientation) ? Orientation3d.fromJSON(object.orientation) : undefined,
    };
  },

  toJSON(message: TrafficAction_AcquireGlobalPositionAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.position !== undefined) {
      obj.position = Vector3d.toJSON(message.position);
    }
    if (message.orientation !== undefined) {
      obj.orientation = Orientation3d.toJSON(message.orientation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_AcquireGlobalPositionAction>, I>>(
    base?: I,
  ): TrafficAction_AcquireGlobalPositionAction {
    return TrafficAction_AcquireGlobalPositionAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_AcquireGlobalPositionAction>, I>>(
    object: I,
  ): TrafficAction_AcquireGlobalPositionAction {
    const message = createBaseTrafficAction_AcquireGlobalPositionAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.position = (object.position !== undefined && object.position !== null)
      ? Vector3d.fromPartial(object.position)
      : undefined;
    message.orientation = (object.orientation !== undefined && object.orientation !== null)
      ? Orientation3d.fromPartial(object.orientation)
      : undefined;
    return message;
  },
};

function createBaseTrafficAction_LaneChangeAction(): TrafficAction_LaneChangeAction {
  return { action_header: undefined, relative_target_lane: 0, dynamics_shape: 0, duration: 0, distance: 0 };
}

export const TrafficAction_LaneChangeAction: MessageFns<TrafficAction_LaneChangeAction> = {
  encode(message: TrafficAction_LaneChangeAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    if (message.relative_target_lane !== undefined && message.relative_target_lane !== 0) {
      writer.uint32(16).int32(message.relative_target_lane);
    }
    if (message.dynamics_shape !== undefined && message.dynamics_shape !== 0) {
      writer.uint32(24).int32(message.dynamics_shape);
    }
    if (message.duration !== undefined && message.duration !== 0) {
      writer.uint32(33).double(message.duration);
    }
    if (message.distance !== undefined && message.distance !== 0) {
      writer.uint32(41).double(message.distance);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_LaneChangeAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_LaneChangeAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.relative_target_lane = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dynamics_shape = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.duration = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.distance = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_LaneChangeAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      relative_target_lane: isSet(object.relative_target_lane) ? globalThis.Number(object.relative_target_lane) : 0,
      dynamics_shape: isSet(object.dynamics_shape) ? trafficAction_DynamicsShapeFromJSON(object.dynamics_shape) : 0,
      duration: isSet(object.duration) ? globalThis.Number(object.duration) : 0,
      distance: isSet(object.distance) ? globalThis.Number(object.distance) : 0,
    };
  },

  toJSON(message: TrafficAction_LaneChangeAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.relative_target_lane !== undefined) {
      obj.relative_target_lane = Math.round(message.relative_target_lane);
    }
    if (message.dynamics_shape !== undefined) {
      obj.dynamics_shape = trafficAction_DynamicsShapeToJSON(message.dynamics_shape);
    }
    if (message.duration !== undefined) {
      obj.duration = message.duration;
    }
    if (message.distance !== undefined) {
      obj.distance = message.distance;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_LaneChangeAction>, I>>(base?: I): TrafficAction_LaneChangeAction {
    return TrafficAction_LaneChangeAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_LaneChangeAction>, I>>(
    object: I,
  ): TrafficAction_LaneChangeAction {
    const message = createBaseTrafficAction_LaneChangeAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.relative_target_lane = object.relative_target_lane ?? 0;
    message.dynamics_shape = object.dynamics_shape ?? 0;
    message.duration = object.duration ?? 0;
    message.distance = object.distance ?? 0;
    return message;
  },
};

function createBaseTrafficAction_SpeedAction(): TrafficAction_SpeedAction {
  return { action_header: undefined, absolute_target_speed: 0, dynamics_shape: 0, duration: 0, distance: 0 };
}

export const TrafficAction_SpeedAction: MessageFns<TrafficAction_SpeedAction> = {
  encode(message: TrafficAction_SpeedAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    if (message.absolute_target_speed !== undefined && message.absolute_target_speed !== 0) {
      writer.uint32(17).double(message.absolute_target_speed);
    }
    if (message.dynamics_shape !== undefined && message.dynamics_shape !== 0) {
      writer.uint32(24).int32(message.dynamics_shape);
    }
    if (message.duration !== undefined && message.duration !== 0) {
      writer.uint32(33).double(message.duration);
    }
    if (message.distance !== undefined && message.distance !== 0) {
      writer.uint32(41).double(message.distance);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_SpeedAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_SpeedAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.absolute_target_speed = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dynamics_shape = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.duration = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.distance = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_SpeedAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      absolute_target_speed: isSet(object.absolute_target_speed) ? globalThis.Number(object.absolute_target_speed) : 0,
      dynamics_shape: isSet(object.dynamics_shape) ? trafficAction_DynamicsShapeFromJSON(object.dynamics_shape) : 0,
      duration: isSet(object.duration) ? globalThis.Number(object.duration) : 0,
      distance: isSet(object.distance) ? globalThis.Number(object.distance) : 0,
    };
  },

  toJSON(message: TrafficAction_SpeedAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.absolute_target_speed !== undefined) {
      obj.absolute_target_speed = message.absolute_target_speed;
    }
    if (message.dynamics_shape !== undefined) {
      obj.dynamics_shape = trafficAction_DynamicsShapeToJSON(message.dynamics_shape);
    }
    if (message.duration !== undefined) {
      obj.duration = message.duration;
    }
    if (message.distance !== undefined) {
      obj.distance = message.distance;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_SpeedAction>, I>>(base?: I): TrafficAction_SpeedAction {
    return TrafficAction_SpeedAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_SpeedAction>, I>>(object: I): TrafficAction_SpeedAction {
    const message = createBaseTrafficAction_SpeedAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.absolute_target_speed = object.absolute_target_speed ?? 0;
    message.dynamics_shape = object.dynamics_shape ?? 0;
    message.duration = object.duration ?? 0;
    message.distance = object.distance ?? 0;
    return message;
  },
};

function createBaseTrafficAction_AbortActionsAction(): TrafficAction_AbortActionsAction {
  return { action_header: undefined, target_action_id: [] };
}

export const TrafficAction_AbortActionsAction: MessageFns<TrafficAction_AbortActionsAction> = {
  encode(message: TrafficAction_AbortActionsAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    for (const v of message.target_action_id) {
      Identifier.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_AbortActionsAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_AbortActionsAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target_action_id.push(Identifier.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_AbortActionsAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      target_action_id: globalThis.Array.isArray(object?.target_action_id)
        ? object.target_action_id.map((e: any) => Identifier.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TrafficAction_AbortActionsAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.target_action_id?.length) {
      obj.target_action_id = message.target_action_id.map((e) => Identifier.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_AbortActionsAction>, I>>(
    base?: I,
  ): TrafficAction_AbortActionsAction {
    return TrafficAction_AbortActionsAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_AbortActionsAction>, I>>(
    object: I,
  ): TrafficAction_AbortActionsAction {
    const message = createBaseTrafficAction_AbortActionsAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.target_action_id = object.target_action_id?.map((e) => Identifier.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTrafficAction_EndActionsAction(): TrafficAction_EndActionsAction {
  return { action_header: undefined, target_action_id: [] };
}

export const TrafficAction_EndActionsAction: MessageFns<TrafficAction_EndActionsAction> = {
  encode(message: TrafficAction_EndActionsAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    for (const v of message.target_action_id) {
      Identifier.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_EndActionsAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_EndActionsAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target_action_id.push(Identifier.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_EndActionsAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      target_action_id: globalThis.Array.isArray(object?.target_action_id)
        ? object.target_action_id.map((e: any) => Identifier.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TrafficAction_EndActionsAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.target_action_id?.length) {
      obj.target_action_id = message.target_action_id.map((e) => Identifier.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_EndActionsAction>, I>>(base?: I): TrafficAction_EndActionsAction {
    return TrafficAction_EndActionsAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_EndActionsAction>, I>>(
    object: I,
  ): TrafficAction_EndActionsAction {
    const message = createBaseTrafficAction_EndActionsAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.target_action_id = object.target_action_id?.map((e) => Identifier.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTrafficAction_CustomAction(): TrafficAction_CustomAction {
  return { action_header: undefined, command: "", command_type: "" };
}

export const TrafficAction_CustomAction: MessageFns<TrafficAction_CustomAction> = {
  encode(message: TrafficAction_CustomAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    if (message.command !== undefined && message.command !== "") {
      writer.uint32(18).string(message.command);
    }
    if (message.command_type !== undefined && message.command_type !== "") {
      writer.uint32(26).string(message.command_type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_CustomAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_CustomAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.command = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.command_type = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_CustomAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      command: isSet(object.command) ? globalThis.String(object.command) : "",
      command_type: isSet(object.command_type) ? globalThis.String(object.command_type) : "",
    };
  },

  toJSON(message: TrafficAction_CustomAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.command !== undefined) {
      obj.command = message.command;
    }
    if (message.command_type !== undefined) {
      obj.command_type = message.command_type;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_CustomAction>, I>>(base?: I): TrafficAction_CustomAction {
    return TrafficAction_CustomAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_CustomAction>, I>>(object: I): TrafficAction_CustomAction {
    const message = createBaseTrafficAction_CustomAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.command = object.command ?? "";
    message.command_type = object.command_type ?? "";
    return message;
  },
};

function createBaseTrafficAction_LongitudinalDistanceAction(): TrafficAction_LongitudinalDistanceAction {
  return {
    action_header: undefined,
    target_traffic_participant_id: undefined,
    distance: 0,
    freespace: false,
    follow: false,
    dynamic_constraints: undefined,
  };
}

export const TrafficAction_LongitudinalDistanceAction: MessageFns<TrafficAction_LongitudinalDistanceAction> = {
  encode(message: TrafficAction_LongitudinalDistanceAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    if (message.target_traffic_participant_id !== undefined) {
      Identifier.encode(message.target_traffic_participant_id, writer.uint32(18).fork()).join();
    }
    if (message.distance !== undefined && message.distance !== 0) {
      writer.uint32(25).double(message.distance);
    }
    if (message.freespace !== undefined && message.freespace !== false) {
      writer.uint32(32).bool(message.freespace);
    }
    if (message.follow !== undefined && message.follow !== false) {
      writer.uint32(40).bool(message.follow);
    }
    if (message.dynamic_constraints !== undefined) {
      TrafficAction_DynamicConstraints.encode(message.dynamic_constraints, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_LongitudinalDistanceAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_LongitudinalDistanceAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target_traffic_participant_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.distance = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.freespace = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.follow = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dynamic_constraints = TrafficAction_DynamicConstraints.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_LongitudinalDistanceAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      target_traffic_participant_id: isSet(object.target_traffic_participant_id)
        ? Identifier.fromJSON(object.target_traffic_participant_id)
        : undefined,
      distance: isSet(object.distance) ? globalThis.Number(object.distance) : 0,
      freespace: isSet(object.freespace) ? globalThis.Boolean(object.freespace) : false,
      follow: isSet(object.follow) ? globalThis.Boolean(object.follow) : false,
      dynamic_constraints: isSet(object.dynamic_constraints)
        ? TrafficAction_DynamicConstraints.fromJSON(object.dynamic_constraints)
        : undefined,
    };
  },

  toJSON(message: TrafficAction_LongitudinalDistanceAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.target_traffic_participant_id !== undefined) {
      obj.target_traffic_participant_id = Identifier.toJSON(message.target_traffic_participant_id);
    }
    if (message.distance !== undefined) {
      obj.distance = message.distance;
    }
    if (message.freespace !== undefined) {
      obj.freespace = message.freespace;
    }
    if (message.follow !== undefined) {
      obj.follow = message.follow;
    }
    if (message.dynamic_constraints !== undefined) {
      obj.dynamic_constraints = TrafficAction_DynamicConstraints.toJSON(message.dynamic_constraints);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_LongitudinalDistanceAction>, I>>(
    base?: I,
  ): TrafficAction_LongitudinalDistanceAction {
    return TrafficAction_LongitudinalDistanceAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_LongitudinalDistanceAction>, I>>(
    object: I,
  ): TrafficAction_LongitudinalDistanceAction {
    const message = createBaseTrafficAction_LongitudinalDistanceAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.target_traffic_participant_id =
      (object.target_traffic_participant_id !== undefined && object.target_traffic_participant_id !== null)
        ? Identifier.fromPartial(object.target_traffic_participant_id)
        : undefined;
    message.distance = object.distance ?? 0;
    message.freespace = object.freespace ?? false;
    message.follow = object.follow ?? false;
    message.dynamic_constraints = (object.dynamic_constraints !== undefined && object.dynamic_constraints !== null)
      ? TrafficAction_DynamicConstraints.fromPartial(object.dynamic_constraints)
      : undefined;
    return message;
  },
};

function createBaseTrafficAction_LateralDistanceAction(): TrafficAction_LateralDistanceAction {
  return {
    action_header: undefined,
    target_traffic_participant_id: undefined,
    distance: 0,
    freespace: false,
    follow: false,
    dynamic_constraints: undefined,
  };
}

export const TrafficAction_LateralDistanceAction: MessageFns<TrafficAction_LateralDistanceAction> = {
  encode(message: TrafficAction_LateralDistanceAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    if (message.target_traffic_participant_id !== undefined) {
      Identifier.encode(message.target_traffic_participant_id, writer.uint32(18).fork()).join();
    }
    if (message.distance !== undefined && message.distance !== 0) {
      writer.uint32(25).double(message.distance);
    }
    if (message.freespace !== undefined && message.freespace !== false) {
      writer.uint32(32).bool(message.freespace);
    }
    if (message.follow !== undefined && message.follow !== false) {
      writer.uint32(40).bool(message.follow);
    }
    if (message.dynamic_constraints !== undefined) {
      TrafficAction_DynamicConstraints.encode(message.dynamic_constraints, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_LateralDistanceAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_LateralDistanceAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target_traffic_participant_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.distance = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.freespace = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.follow = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dynamic_constraints = TrafficAction_DynamicConstraints.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_LateralDistanceAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      target_traffic_participant_id: isSet(object.target_traffic_participant_id)
        ? Identifier.fromJSON(object.target_traffic_participant_id)
        : undefined,
      distance: isSet(object.distance) ? globalThis.Number(object.distance) : 0,
      freespace: isSet(object.freespace) ? globalThis.Boolean(object.freespace) : false,
      follow: isSet(object.follow) ? globalThis.Boolean(object.follow) : false,
      dynamic_constraints: isSet(object.dynamic_constraints)
        ? TrafficAction_DynamicConstraints.fromJSON(object.dynamic_constraints)
        : undefined,
    };
  },

  toJSON(message: TrafficAction_LateralDistanceAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.target_traffic_participant_id !== undefined) {
      obj.target_traffic_participant_id = Identifier.toJSON(message.target_traffic_participant_id);
    }
    if (message.distance !== undefined) {
      obj.distance = message.distance;
    }
    if (message.freespace !== undefined) {
      obj.freespace = message.freespace;
    }
    if (message.follow !== undefined) {
      obj.follow = message.follow;
    }
    if (message.dynamic_constraints !== undefined) {
      obj.dynamic_constraints = TrafficAction_DynamicConstraints.toJSON(message.dynamic_constraints);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_LateralDistanceAction>, I>>(
    base?: I,
  ): TrafficAction_LateralDistanceAction {
    return TrafficAction_LateralDistanceAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_LateralDistanceAction>, I>>(
    object: I,
  ): TrafficAction_LateralDistanceAction {
    const message = createBaseTrafficAction_LateralDistanceAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.target_traffic_participant_id =
      (object.target_traffic_participant_id !== undefined && object.target_traffic_participant_id !== null)
        ? Identifier.fromPartial(object.target_traffic_participant_id)
        : undefined;
    message.distance = object.distance ?? 0;
    message.freespace = object.freespace ?? false;
    message.follow = object.follow ?? false;
    message.dynamic_constraints = (object.dynamic_constraints !== undefined && object.dynamic_constraints !== null)
      ? TrafficAction_DynamicConstraints.fromPartial(object.dynamic_constraints)
      : undefined;
    return message;
  },
};

function createBaseTrafficAction_LaneOffsetAction(): TrafficAction_LaneOffsetAction {
  return { action_header: undefined, target_lane_offset: 0, dynamics_shape: 0 };
}

export const TrafficAction_LaneOffsetAction: MessageFns<TrafficAction_LaneOffsetAction> = {
  encode(message: TrafficAction_LaneOffsetAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    if (message.target_lane_offset !== undefined && message.target_lane_offset !== 0) {
      writer.uint32(17).double(message.target_lane_offset);
    }
    if (message.dynamics_shape !== undefined && message.dynamics_shape !== 0) {
      writer.uint32(24).int32(message.dynamics_shape);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_LaneOffsetAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_LaneOffsetAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.target_lane_offset = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dynamics_shape = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_LaneOffsetAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      target_lane_offset: isSet(object.target_lane_offset) ? globalThis.Number(object.target_lane_offset) : 0,
      dynamics_shape: isSet(object.dynamics_shape) ? trafficAction_DynamicsShapeFromJSON(object.dynamics_shape) : 0,
    };
  },

  toJSON(message: TrafficAction_LaneOffsetAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.target_lane_offset !== undefined) {
      obj.target_lane_offset = message.target_lane_offset;
    }
    if (message.dynamics_shape !== undefined) {
      obj.dynamics_shape = trafficAction_DynamicsShapeToJSON(message.dynamics_shape);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_LaneOffsetAction>, I>>(base?: I): TrafficAction_LaneOffsetAction {
    return TrafficAction_LaneOffsetAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_LaneOffsetAction>, I>>(
    object: I,
  ): TrafficAction_LaneOffsetAction {
    const message = createBaseTrafficAction_LaneOffsetAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.target_lane_offset = object.target_lane_offset ?? 0;
    message.dynamics_shape = object.dynamics_shape ?? 0;
    return message;
  },
};

function createBaseTrafficAction_TeleportAction(): TrafficAction_TeleportAction {
  return { action_header: undefined, position: undefined, orientation: undefined };
}

export const TrafficAction_TeleportAction: MessageFns<TrafficAction_TeleportAction> = {
  encode(message: TrafficAction_TeleportAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_header !== undefined) {
      TrafficAction_ActionHeader.encode(message.action_header, writer.uint32(10).fork()).join();
    }
    if (message.position !== undefined) {
      Vector3d.encode(message.position, writer.uint32(18).fork()).join();
    }
    if (message.orientation !== undefined) {
      Orientation3d.encode(message.orientation, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficAction_TeleportAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficAction_TeleportAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action_header = TrafficAction_ActionHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.position = Vector3d.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.orientation = Orientation3d.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficAction_TeleportAction {
    return {
      action_header: isSet(object.action_header)
        ? TrafficAction_ActionHeader.fromJSON(object.action_header)
        : undefined,
      position: isSet(object.position) ? Vector3d.fromJSON(object.position) : undefined,
      orientation: isSet(object.orientation) ? Orientation3d.fromJSON(object.orientation) : undefined,
    };
  },

  toJSON(message: TrafficAction_TeleportAction): unknown {
    const obj: any = {};
    if (message.action_header !== undefined) {
      obj.action_header = TrafficAction_ActionHeader.toJSON(message.action_header);
    }
    if (message.position !== undefined) {
      obj.position = Vector3d.toJSON(message.position);
    }
    if (message.orientation !== undefined) {
      obj.orientation = Orientation3d.toJSON(message.orientation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficAction_TeleportAction>, I>>(base?: I): TrafficAction_TeleportAction {
    return TrafficAction_TeleportAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficAction_TeleportAction>, I>>(object: I): TrafficAction_TeleportAction {
    const message = createBaseTrafficAction_TeleportAction();
    message.action_header = (object.action_header !== undefined && object.action_header !== null)
      ? TrafficAction_ActionHeader.fromPartial(object.action_header)
      : undefined;
    message.position = (object.position !== undefined && object.position !== null)
      ? Vector3d.fromPartial(object.position)
      : undefined;
    message.orientation = (object.orientation !== undefined && object.orientation !== null)
      ? Orientation3d.fromPartial(object.orientation)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
