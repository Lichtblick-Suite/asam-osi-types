// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_trafficcommandupdate.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Identifier, Timestamp } from "./osi_common";
import { InterfaceVersion } from "./osi_version";

/**
 * \brief This message enables the traffic participant model to send updates
 * to the scenario engine about the execution of its received \c TrafficCommand input.
 * While traffic actions are usually executed successfully by the traffic participant,
 * there may be actions which the traffic participant is not able to execute
 * either for capability or situation-specific reasons.
 *
 * This message allows a traffic participant to send feedback if an action
 * cannot happen as requested by the \c TrafficCommand. Currently, it is out of
 * scope to standardize the exact reason for non-executability or failed execution
 * because the reason can have multiple explanations. The responsibility for deciding
 * about successful or unsuccessful scenario execution lies fully on the side of the
 * scenario engine.
 *
 * \note This interface is currently just a placeholder and could be
 * changed in experimental ways to support semantics of upcoming OpenSCENARIO
 * versions.
 */
export interface TrafficCommandUpdate {
  /**
   * The interface version used by the sender (traffic participant model).
   *
   * \rules
   * is_set
   * \endrules
   */
  version?:
    | InterfaceVersion
    | undefined;
  /**
   * The data timestamp of the simulation environment. Zero time is arbitrary
   * but must be identical for all messages. Zero time does not need to
   * coincide with the UNIX epoch. It is recommended to use zero timestamp as
   * the starting time point of the simulation.
   *
   * \rules
   * is_set
   * \endrules
   */
  timestamp?:
    | Timestamp
    | undefined;
  /**
   * The ID of this traffic participant which must coincide with a prior sent ID, cf.
   * \c TrafficCommand::traffic_participant_id.
   *
   * \rules
   * is_set
   * \endrules
   */
  traffic_participant_id?:
    | Identifier
    | undefined;
  /**
   * Actions which a traffic participant dismisses and which are not yet ended or
   * aborted by the scenario engine (via a \c TrafficCommand::action.end_actions_action or a
   * \c TrafficCommand::action.abort_actions_action).
   * Thus, the valid time interval for this action is after the \c TrafficCommand::timestamp
   * for the respective action, which tells a traffic participant to perform an action, has
   * been sent, but must not be after a \c TrafficCommand::timestamp of the respective End-Actions-Action
   * or Abort-Actions-Action, which tells a traffic participant to end or abort that action.
   *
   * Furthermore, a \c TrafficCommand::action.end_actions_action or a
   * \c TrafficCommand::action.abort_actions_action must not be sent after this action
   * (the \c TrafficCommandUpdate::dismissed_action) for a respective action has been sent.
   *
   * \note If more than one dismissed action is supplied it means that
   * multiple actions are regarded as dismissed.
   */
  dismissed_action: TrafficCommandUpdate_DismissedAction[];
}

/** \brief Action which a traffic participant dismisses. */
export interface TrafficCommandUpdate_DismissedAction {
  /**
   * Dismissed traffic action id from the perspective of this traffic participant, if any.
   *
   * \note A dismissed traffic action id identifies a \c TrafficCommand::action which cannot be executed or
   * completed by the traffic participant. This field must have the same value as the
   * \c ActionHeader::action_id of a prior sent \c TrafficCommand and must correspond
   * to the same traffic participant.
   */
  dismissed_action_id?:
    | Identifier
    | undefined;
  /**
   * Information about the reason of failure.
   *
   * \note This is a custom, informal string without a standardized meaning.
   */
  failure_reason?: string | undefined;
}

function createBaseTrafficCommandUpdate(): TrafficCommandUpdate {
  return { version: undefined, timestamp: undefined, traffic_participant_id: undefined, dismissed_action: [] };
}

export const TrafficCommandUpdate: MessageFns<TrafficCommandUpdate> = {
  encode(message: TrafficCommandUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== undefined) {
      InterfaceVersion.encode(message.version, writer.uint32(10).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(18).fork()).join();
    }
    if (message.traffic_participant_id !== undefined) {
      Identifier.encode(message.traffic_participant_id, writer.uint32(26).fork()).join();
    }
    for (const v of message.dismissed_action) {
      TrafficCommandUpdate_DismissedAction.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficCommandUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficCommandUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = InterfaceVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.traffic_participant_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.dismissed_action.push(TrafficCommandUpdate_DismissedAction.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficCommandUpdate {
    return {
      version: isSet(object.version) ? InterfaceVersion.fromJSON(object.version) : undefined,
      timestamp: isSet(object.timestamp) ? Timestamp.fromJSON(object.timestamp) : undefined,
      traffic_participant_id: isSet(object.traffic_participant_id)
        ? Identifier.fromJSON(object.traffic_participant_id)
        : undefined,
      dismissed_action: globalThis.Array.isArray(object?.dismissed_action)
        ? object.dismissed_action.map((e: any) => TrafficCommandUpdate_DismissedAction.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TrafficCommandUpdate): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = InterfaceVersion.toJSON(message.version);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = Timestamp.toJSON(message.timestamp);
    }
    if (message.traffic_participant_id !== undefined) {
      obj.traffic_participant_id = Identifier.toJSON(message.traffic_participant_id);
    }
    if (message.dismissed_action?.length) {
      obj.dismissed_action = message.dismissed_action.map((e) => TrafficCommandUpdate_DismissedAction.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficCommandUpdate>, I>>(base?: I): TrafficCommandUpdate {
    return TrafficCommandUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficCommandUpdate>, I>>(object: I): TrafficCommandUpdate {
    const message = createBaseTrafficCommandUpdate();
    message.version = (object.version !== undefined && object.version !== null)
      ? InterfaceVersion.fromPartial(object.version)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.traffic_participant_id =
      (object.traffic_participant_id !== undefined && object.traffic_participant_id !== null)
        ? Identifier.fromPartial(object.traffic_participant_id)
        : undefined;
    message.dismissed_action =
      object.dismissed_action?.map((e) => TrafficCommandUpdate_DismissedAction.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTrafficCommandUpdate_DismissedAction(): TrafficCommandUpdate_DismissedAction {
  return { dismissed_action_id: undefined, failure_reason: "" };
}

export const TrafficCommandUpdate_DismissedAction: MessageFns<TrafficCommandUpdate_DismissedAction> = {
  encode(message: TrafficCommandUpdate_DismissedAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dismissed_action_id !== undefined) {
      Identifier.encode(message.dismissed_action_id, writer.uint32(10).fork()).join();
    }
    if (message.failure_reason !== undefined && message.failure_reason !== "") {
      writer.uint32(18).string(message.failure_reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficCommandUpdate_DismissedAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficCommandUpdate_DismissedAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dismissed_action_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.failure_reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficCommandUpdate_DismissedAction {
    return {
      dismissed_action_id: isSet(object.dismissed_action_id)
        ? Identifier.fromJSON(object.dismissed_action_id)
        : undefined,
      failure_reason: isSet(object.failure_reason) ? globalThis.String(object.failure_reason) : "",
    };
  },

  toJSON(message: TrafficCommandUpdate_DismissedAction): unknown {
    const obj: any = {};
    if (message.dismissed_action_id !== undefined) {
      obj.dismissed_action_id = Identifier.toJSON(message.dismissed_action_id);
    }
    if (message.failure_reason !== undefined) {
      obj.failure_reason = message.failure_reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficCommandUpdate_DismissedAction>, I>>(
    base?: I,
  ): TrafficCommandUpdate_DismissedAction {
    return TrafficCommandUpdate_DismissedAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficCommandUpdate_DismissedAction>, I>>(
    object: I,
  ): TrafficCommandUpdate_DismissedAction {
    const message = createBaseTrafficCommandUpdate_DismissedAction();
    message.dismissed_action_id = (object.dismissed_action_id !== undefined && object.dismissed_action_id !== null)
      ? Identifier.fromPartial(object.dismissed_action_id)
      : undefined;
    message.failure_reason = object.failure_reason ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
