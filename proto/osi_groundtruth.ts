// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_groundtruth.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Identifier, Timestamp, Vector3d } from "./osi_common";
import { EnvironmentalConditions } from "./osi_environment";
import { Lane, LaneBoundary } from "./osi_lane";
import { LogicalLane, LogicalLaneBoundary } from "./osi_logicallane";
import { MovingObject, StationaryObject } from "./osi_object";
import { Occupant } from "./osi_occupant";
import { ReferenceLine } from "./osi_referenceline";
import { RoadMarking } from "./osi_roadmarking";
import { TrafficLight } from "./osi_trafficlight";
import { TrafficSign } from "./osi_trafficsign";
import { InterfaceVersion } from "./osi_version";

/**
 * \brief The ground truth information from the simulation environment.
 *
 * This ground truth information is supposed to describe the whole simulated
 * environment around any simulated vehicle. For each simulated host vehicle
 * (there may be one or multiple), define an area around the vehicle which
 * is greater than the combined field of views (FOV) of all obstructed sensors
 * in the vehicle. The ground truth data is supposed to describe the convex
 * hull of all such areas w.r.t. a global simulation coordinate system.
 *
 * The simulation coordinate system may change during the simulation if and
 * only if all coordinates w.r.t. this coordinate system are also changed.
 *
 * The data has to be sent at a rate defined by the receiving partner. When
 * sending, values with default values might be left default in order to improve
 * performance.
 *
 * To provide a complete interface, all fields of all contained messages must be
 * properly set unless specifically stated in the field's definition that the
 * field may remain unset.
 *
 * In enums (e.g. types) the unknown (first / default) value is not allowed to
 * be used in the ground truth interface.
 *
 * \image html OSI_GroundTruth.svg "Ground Truth"
 */
export interface GroundTruth {
  /**
   * The interface version used by the sender (i.e. the simulation
   * environment).
   *
   * \rules
   * is_set
   * \endrules
   */
  version?:
    | InterfaceVersion
    | undefined;
  /**
   * The data timestamp of the simulation environment. The zero time point is
   * arbitrary but must be identical for all messages.
   * Recommendation: Zero time point for start point of the simulation.
   *
   * \note Zero time point does not need to coincide with the UNIX epoch.
   *
   * \note For ground truth data this timestamp coincides both with the
   * notional simulation time the data applies to and the time it was sent
   * (there is no inherent latency for ground truth data, as opposed to
   * sensor data).
   *
   * \rules
   * is_set
   * \endrules
   */
  timestamp?:
    | Timestamp
    | undefined;
  /**
   * The ID of the host vehicle object referencing to \c MovingObject .
   *
   * \note This ID has to be filled and is not optional!
   *
   * \rules
   * refers_to: MovingObject
   * is_set
   * \endrules
   */
  host_vehicle_id?:
    | Identifier
    | undefined;
  /**
   * The list of stationary objects (excluding traffic signs and traffic
   * lights).
   */
  stationary_object: StationaryObject[];
  /** The list of all other moving objects including all (host) vehicles. */
  moving_object: MovingObject[];
  /** The list of traffic signs. */
  traffic_sign: TrafficSign[];
  /** The list of traffic lights. */
  traffic_light: TrafficLight[];
  /**
   * The list of road surface markings (lane markings are excluded and
   * defined as \c LaneBoundary).
   */
  road_marking: RoadMarking[];
  /** The list of lane boundaries. */
  lane_boundary: LaneBoundary[];
  /** The list of lanes forming a road network. */
  lane: Lane[];
  /** The list of passengers in the (host) vehicle(s). */
  occupant: Occupant[];
  /** Conditions of the environment. */
  environmental_conditions?:
    | EnvironmentalConditions
    | undefined;
  /**
   * The ISO country code in 3 digit numeric format according to:
   * ISO Code 3166/1 [1].
   * E.g. Germany = 276, USA = 840.
   *
   * \par Reference:
   * [1] ISO International Organization for Standardization. (2013). <em>ISO 3166-1 Codes for the representation of names of countries and their subdivisions - Part 1: Country codes</em>. (ISO 3166-1:2013). Geneva, Switzerland.
   *
   * \rules
   * is_iso_country_code
   * \endrules
   */
  country_code?:
    | number
    | undefined;
  /**
   * Projection string that allows to transform all coordinates in GroundTruth
   * into a different cartographic projection after the \c proj_frame_offset
   * has been applied.
   *
   * The string follows the PROJ rules for projections [1].
   *
   * \par Reference:
   * [1] PROJ contributors. (2019). <em>PROJ coordinate transformation software library</em>. Open Source Geospatial Foundation. Retrieved January 25, 2019, from https://proj.org/usage/projections.html
   */
  proj_string?:
    | string
    | undefined;
  /**
   * Opaque reference of a map.
   *
   * \note Origin and orientation of the map have to coincide with the
   * inertial coordinate frame of the ground truth.
   *
   * \note It is implementation-specific how map_reference is resolved.
   */
  map_reference?:
    | string
    | undefined;
  /**
   * Opaque reference of an associated 3D model.
   *
   * The model covers the static parts of the environment that are not
   * provided as individual models referenced from ground truth objects
   * like moving or stationary objects.
   *
   * \note Origin and orientation of the model have to coincide with the
   * inertial coordinate frame of the ground truth.
   *
   * \note It is implementation-specific how model_references are resolved to
   * 3d models. The parts the world model contains are also implementation-specific.
   * For example, the world model can either contain street geometries or
   * derives street geometries automatically from a map reference.
   */
  model_reference?:
    | string
    | undefined;
  /** Reference lines used by LogicalLane */
  reference_line: ReferenceLine[];
  /** Logical lane boundaries used by LogicalLane */
  logical_lane_boundary: LogicalLaneBoundary[];
  /** Logical lanes used e.g. by traffic agents */
  logical_lane: LogicalLane[];
  /** Coordinate frame offset to be used for PROJ transformations. */
  proj_frame_offset?: GroundTruth_ProjFrameOffset | undefined;
}

/**
 * \brief Coordinate frame offset to transform from OSI's global coordinate
 * system to a coordinate reference system to be used for given PROJ
 * transformations.
 *
 * If an offset is defined, always apply the \c proj_frame_offset on
 * global OSI coordinates before applying any transformations defined in
 * \c proj_string.
 *
 * To apply the offset, global coordinates are first translated by the given
 * positional offset (x,y,z). Then, the yaw angle is used to rotate around
 * the new origin.
 *
 * The offset is applied on global OSI coordinates using an affine
 * transformation with rotation around z-axis:
 *
 * xWorld = xOSI * cos(yaw) - yOSI * sin(yaw) + xOffset
 *
 * yWorld = xOSI * sin(yaw) + yOSI * cos(yaw) + yOffset
 *
 * zWorld = zOSI + zOffset
 *
 * If no yaw is provided (recommended), the formulas simplify to:
 *
 * xWorld = xOSI + xOffset
 *
 * yWorld = yOSI + yOffset
 *
 * zWorld = zOSI + zOffset
 */
export interface GroundTruth_ProjFrameOffset {
  /** Positional offset for relocation of the coordinate frame. */
  position?:
    | Vector3d
    | undefined;
  /**
   * Yaw/heading angle for re-orientation of the coordinate frame around
   * the z-axis.
   */
  yaw?: number | undefined;
}

function createBaseGroundTruth(): GroundTruth {
  return {
    version: undefined,
    timestamp: undefined,
    host_vehicle_id: undefined,
    stationary_object: [],
    moving_object: [],
    traffic_sign: [],
    traffic_light: [],
    road_marking: [],
    lane_boundary: [],
    lane: [],
    occupant: [],
    environmental_conditions: undefined,
    country_code: 0,
    proj_string: "",
    map_reference: "",
    model_reference: "",
    reference_line: [],
    logical_lane_boundary: [],
    logical_lane: [],
    proj_frame_offset: undefined,
  };
}

export const GroundTruth: MessageFns<GroundTruth> = {
  encode(message: GroundTruth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== undefined) {
      InterfaceVersion.encode(message.version, writer.uint32(10).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(18).fork()).join();
    }
    if (message.host_vehicle_id !== undefined) {
      Identifier.encode(message.host_vehicle_id, writer.uint32(26).fork()).join();
    }
    for (const v of message.stationary_object) {
      StationaryObject.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.moving_object) {
      MovingObject.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.traffic_sign) {
      TrafficSign.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.traffic_light) {
      TrafficLight.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.road_marking) {
      RoadMarking.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.lane_boundary) {
      LaneBoundary.encode(v!, writer.uint32(74).fork()).join();
    }
    for (const v of message.lane) {
      Lane.encode(v!, writer.uint32(82).fork()).join();
    }
    for (const v of message.occupant) {
      Occupant.encode(v!, writer.uint32(90).fork()).join();
    }
    if (message.environmental_conditions !== undefined) {
      EnvironmentalConditions.encode(message.environmental_conditions, writer.uint32(98).fork()).join();
    }
    if (message.country_code !== undefined && message.country_code !== 0) {
      writer.uint32(104).uint32(message.country_code);
    }
    if (message.proj_string !== undefined && message.proj_string !== "") {
      writer.uint32(114).string(message.proj_string);
    }
    if (message.map_reference !== undefined && message.map_reference !== "") {
      writer.uint32(122).string(message.map_reference);
    }
    if (message.model_reference !== undefined && message.model_reference !== "") {
      writer.uint32(130).string(message.model_reference);
    }
    for (const v of message.reference_line) {
      ReferenceLine.encode(v!, writer.uint32(138).fork()).join();
    }
    for (const v of message.logical_lane_boundary) {
      LogicalLaneBoundary.encode(v!, writer.uint32(146).fork()).join();
    }
    for (const v of message.logical_lane) {
      LogicalLane.encode(v!, writer.uint32(154).fork()).join();
    }
    if (message.proj_frame_offset !== undefined) {
      GroundTruth_ProjFrameOffset.encode(message.proj_frame_offset, writer.uint32(162).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroundTruth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGroundTruth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = InterfaceVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.host_vehicle_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.stationary_object.push(StationaryObject.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.moving_object.push(MovingObject.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.traffic_sign.push(TrafficSign.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.traffic_light.push(TrafficLight.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.road_marking.push(RoadMarking.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.lane_boundary.push(LaneBoundary.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.lane.push(Lane.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.occupant.push(Occupant.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.environmental_conditions = EnvironmentalConditions.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.country_code = reader.uint32();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.proj_string = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.map_reference = reader.string();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.model_reference = reader.string();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.reference_line.push(ReferenceLine.decode(reader, reader.uint32()));
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.logical_lane_boundary.push(LogicalLaneBoundary.decode(reader, reader.uint32()));
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.logical_lane.push(LogicalLane.decode(reader, reader.uint32()));
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.proj_frame_offset = GroundTruth_ProjFrameOffset.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GroundTruth {
    return {
      version: isSet(object.version) ? InterfaceVersion.fromJSON(object.version) : undefined,
      timestamp: isSet(object.timestamp) ? Timestamp.fromJSON(object.timestamp) : undefined,
      host_vehicle_id: isSet(object.host_vehicle_id) ? Identifier.fromJSON(object.host_vehicle_id) : undefined,
      stationary_object: globalThis.Array.isArray(object?.stationary_object)
        ? object.stationary_object.map((e: any) => StationaryObject.fromJSON(e))
        : [],
      moving_object: globalThis.Array.isArray(object?.moving_object)
        ? object.moving_object.map((e: any) => MovingObject.fromJSON(e))
        : [],
      traffic_sign: globalThis.Array.isArray(object?.traffic_sign)
        ? object.traffic_sign.map((e: any) => TrafficSign.fromJSON(e))
        : [],
      traffic_light: globalThis.Array.isArray(object?.traffic_light)
        ? object.traffic_light.map((e: any) => TrafficLight.fromJSON(e))
        : [],
      road_marking: globalThis.Array.isArray(object?.road_marking)
        ? object.road_marking.map((e: any) => RoadMarking.fromJSON(e))
        : [],
      lane_boundary: globalThis.Array.isArray(object?.lane_boundary)
        ? object.lane_boundary.map((e: any) => LaneBoundary.fromJSON(e))
        : [],
      lane: globalThis.Array.isArray(object?.lane)
        ? object.lane.map((e: any) => Lane.fromJSON(e))
        : [],
      occupant: globalThis.Array.isArray(object?.occupant) ? object.occupant.map((e: any) => Occupant.fromJSON(e)) : [],
      environmental_conditions: isSet(object.environmental_conditions)
        ? EnvironmentalConditions.fromJSON(object.environmental_conditions)
        : undefined,
      country_code: isSet(object.country_code) ? globalThis.Number(object.country_code) : 0,
      proj_string: isSet(object.proj_string) ? globalThis.String(object.proj_string) : "",
      map_reference: isSet(object.map_reference) ? globalThis.String(object.map_reference) : "",
      model_reference: isSet(object.model_reference) ? globalThis.String(object.model_reference) : "",
      reference_line: globalThis.Array.isArray(object?.reference_line)
        ? object.reference_line.map((e: any) => ReferenceLine.fromJSON(e))
        : [],
      logical_lane_boundary: globalThis.Array.isArray(object?.logical_lane_boundary)
        ? object.logical_lane_boundary.map((e: any) => LogicalLaneBoundary.fromJSON(e))
        : [],
      logical_lane: globalThis.Array.isArray(object?.logical_lane)
        ? object.logical_lane.map((e: any) => LogicalLane.fromJSON(e))
        : [],
      proj_frame_offset: isSet(object.proj_frame_offset)
        ? GroundTruth_ProjFrameOffset.fromJSON(object.proj_frame_offset)
        : undefined,
    };
  },

  toJSON(message: GroundTruth): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = InterfaceVersion.toJSON(message.version);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = Timestamp.toJSON(message.timestamp);
    }
    if (message.host_vehicle_id !== undefined) {
      obj.host_vehicle_id = Identifier.toJSON(message.host_vehicle_id);
    }
    if (message.stationary_object?.length) {
      obj.stationary_object = message.stationary_object.map((e) => StationaryObject.toJSON(e));
    }
    if (message.moving_object?.length) {
      obj.moving_object = message.moving_object.map((e) => MovingObject.toJSON(e));
    }
    if (message.traffic_sign?.length) {
      obj.traffic_sign = message.traffic_sign.map((e) => TrafficSign.toJSON(e));
    }
    if (message.traffic_light?.length) {
      obj.traffic_light = message.traffic_light.map((e) => TrafficLight.toJSON(e));
    }
    if (message.road_marking?.length) {
      obj.road_marking = message.road_marking.map((e) => RoadMarking.toJSON(e));
    }
    if (message.lane_boundary?.length) {
      obj.lane_boundary = message.lane_boundary.map((e) => LaneBoundary.toJSON(e));
    }
    if (message.lane?.length) {
      obj.lane = message.lane.map((e) => Lane.toJSON(e));
    }
    if (message.occupant?.length) {
      obj.occupant = message.occupant.map((e) => Occupant.toJSON(e));
    }
    if (message.environmental_conditions !== undefined) {
      obj.environmental_conditions = EnvironmentalConditions.toJSON(message.environmental_conditions);
    }
    if (message.country_code !== undefined) {
      obj.country_code = Math.round(message.country_code);
    }
    if (message.proj_string !== undefined) {
      obj.proj_string = message.proj_string;
    }
    if (message.map_reference !== undefined) {
      obj.map_reference = message.map_reference;
    }
    if (message.model_reference !== undefined) {
      obj.model_reference = message.model_reference;
    }
    if (message.reference_line?.length) {
      obj.reference_line = message.reference_line.map((e) => ReferenceLine.toJSON(e));
    }
    if (message.logical_lane_boundary?.length) {
      obj.logical_lane_boundary = message.logical_lane_boundary.map((e) => LogicalLaneBoundary.toJSON(e));
    }
    if (message.logical_lane?.length) {
      obj.logical_lane = message.logical_lane.map((e) => LogicalLane.toJSON(e));
    }
    if (message.proj_frame_offset !== undefined) {
      obj.proj_frame_offset = GroundTruth_ProjFrameOffset.toJSON(message.proj_frame_offset);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroundTruth>, I>>(base?: I): GroundTruth {
    return GroundTruth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroundTruth>, I>>(object: I): GroundTruth {
    const message = createBaseGroundTruth();
    message.version = (object.version !== undefined && object.version !== null)
      ? InterfaceVersion.fromPartial(object.version)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.host_vehicle_id = (object.host_vehicle_id !== undefined && object.host_vehicle_id !== null)
      ? Identifier.fromPartial(object.host_vehicle_id)
      : undefined;
    message.stationary_object = object.stationary_object?.map((e) => StationaryObject.fromPartial(e)) || [];
    message.moving_object = object.moving_object?.map((e) => MovingObject.fromPartial(e)) || [];
    message.traffic_sign = object.traffic_sign?.map((e) => TrafficSign.fromPartial(e)) || [];
    message.traffic_light = object.traffic_light?.map((e) => TrafficLight.fromPartial(e)) || [];
    message.road_marking = object.road_marking?.map((e) => RoadMarking.fromPartial(e)) || [];
    message.lane_boundary = object.lane_boundary?.map((e) => LaneBoundary.fromPartial(e)) || [];
    message.lane = object.lane?.map((e) => Lane.fromPartial(e)) || [];
    message.occupant = object.occupant?.map((e) => Occupant.fromPartial(e)) || [];
    message.environmental_conditions =
      (object.environmental_conditions !== undefined && object.environmental_conditions !== null)
        ? EnvironmentalConditions.fromPartial(object.environmental_conditions)
        : undefined;
    message.country_code = object.country_code ?? 0;
    message.proj_string = object.proj_string ?? "";
    message.map_reference = object.map_reference ?? "";
    message.model_reference = object.model_reference ?? "";
    message.reference_line = object.reference_line?.map((e) => ReferenceLine.fromPartial(e)) || [];
    message.logical_lane_boundary = object.logical_lane_boundary?.map((e) => LogicalLaneBoundary.fromPartial(e)) || [];
    message.logical_lane = object.logical_lane?.map((e) => LogicalLane.fromPartial(e)) || [];
    message.proj_frame_offset = (object.proj_frame_offset !== undefined && object.proj_frame_offset !== null)
      ? GroundTruth_ProjFrameOffset.fromPartial(object.proj_frame_offset)
      : undefined;
    return message;
  },
};

function createBaseGroundTruth_ProjFrameOffset(): GroundTruth_ProjFrameOffset {
  return { position: undefined, yaw: 0 };
}

export const GroundTruth_ProjFrameOffset: MessageFns<GroundTruth_ProjFrameOffset> = {
  encode(message: GroundTruth_ProjFrameOffset, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.position !== undefined) {
      Vector3d.encode(message.position, writer.uint32(10).fork()).join();
    }
    if (message.yaw !== undefined && message.yaw !== 0) {
      writer.uint32(17).double(message.yaw);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroundTruth_ProjFrameOffset {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGroundTruth_ProjFrameOffset();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.position = Vector3d.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.yaw = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GroundTruth_ProjFrameOffset {
    return {
      position: isSet(object.position) ? Vector3d.fromJSON(object.position) : undefined,
      yaw: isSet(object.yaw) ? globalThis.Number(object.yaw) : 0,
    };
  },

  toJSON(message: GroundTruth_ProjFrameOffset): unknown {
    const obj: any = {};
    if (message.position !== undefined) {
      obj.position = Vector3d.toJSON(message.position);
    }
    if (message.yaw !== undefined) {
      obj.yaw = message.yaw;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroundTruth_ProjFrameOffset>, I>>(base?: I): GroundTruth_ProjFrameOffset {
    return GroundTruth_ProjFrameOffset.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroundTruth_ProjFrameOffset>, I>>(object: I): GroundTruth_ProjFrameOffset {
    const message = createBaseGroundTruth_ProjFrameOffset();
    message.position = (object.position !== undefined && object.position !== null)
      ? Vector3d.fromPartial(object.position)
      : undefined;
    message.yaw = object.yaw ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
