// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_motionrequest.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Orientation3d, StatePoint, Timestamp, Vector3d } from "./osi_common";
import { InterfaceVersion } from "./osi_version";

/**
 * \brief This message is intended as an interface between a
 * motion-planning function and the actuator management.
 * The motion-planning function can thereby be a representation of a
 * highly-automated driving function, a human driving behavior model, etc.
 *
 * The motion-planning function can either send a desired future trajectory or a desired
 * future state. The message can be defined by an additional variable.
 *
 * \note The coordinate system is defined as right-handed.
 * All coordinates and orientations are relative to the global coordinate system.
 * The reference point of the vehicle is the middle of the rear axis.
 * Units are m for positions, m/s for velocities, and m/s^2 for accelerations.
 */
export interface MotionRequest {
  /**
   * The interface version used by the sender (simulation environment).
   *
   * \rules
   * is_set
   * \endrules
   */
  version?:
    | InterfaceVersion
    | undefined;
  /**
   * The data timestamp of the simulation environment.
   * A reference to \c Timestamp message.
   *
   * \rules
   * is_set
   * \endrules
   */
  timestamp?:
    | Timestamp
    | undefined;
  /**
   * Define the type that is used to specify the motion request.
   * This must be set. Additionally, the field corresponding to the specified
   * option must be set.
   */
  motion_request_type?:
    | MotionRequest_MotionRequestType
    | undefined;
  /**
   * Defines a desired state.
   * If the output option is set to DESIRED_STATE, this field must be set.
   */
  desired_state?:
    | MotionRequest_DesiredState
    | undefined;
  /**
   * Defines a desired trajectory.
   * If the output option is set to DESIRED_TRAJECTORY, this field must be set.
   */
  desired_trajectory?: MotionRequest_DesiredTrajectory | undefined;
}

/**
 * Define different options for function output.
 * Each option corresponds to a field in the message.
 */
export enum MotionRequest_MotionRequestType {
  /** MOTION_REQUEST_TYPE_DESIRED_STATE - Desired state calculated by the function. */
  MOTION_REQUEST_TYPE_DESIRED_STATE = 0,
  /** MOTION_REQUEST_TYPE_TRAJECTORY - Desired trajectory calculated by the function. */
  MOTION_REQUEST_TYPE_TRAJECTORY = 1,
}

export function motionRequest_MotionRequestTypeFromJSON(object: any): MotionRequest_MotionRequestType {
  switch (object) {
    case 0:
    case "MOTION_REQUEST_TYPE_DESIRED_STATE":
      return MotionRequest_MotionRequestType.MOTION_REQUEST_TYPE_DESIRED_STATE;
    case 1:
    case "MOTION_REQUEST_TYPE_TRAJECTORY":
      return MotionRequest_MotionRequestType.MOTION_REQUEST_TYPE_TRAJECTORY;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum MotionRequest_MotionRequestType");
  }
}

export function motionRequest_MotionRequestTypeToJSON(object: MotionRequest_MotionRequestType): string {
  switch (object) {
    case MotionRequest_MotionRequestType.MOTION_REQUEST_TYPE_DESIRED_STATE:
      return "MOTION_REQUEST_TYPE_DESIRED_STATE";
    case MotionRequest_MotionRequestType.MOTION_REQUEST_TYPE_TRAJECTORY:
      return "MOTION_REQUEST_TYPE_TRAJECTORY";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum MotionRequest_MotionRequestType");
  }
}

/**
 * \brief The desired state is calculated by the function as a result of
 * the motion planning stack.
 *
 * The actuator management is supposed to reach the desired state at the
 * specified time.
 */
export interface MotionRequest_DesiredState {
  /** A reference to \c Timestamp message. */
  timestamp?:
    | Timestamp
    | undefined;
  /** Intended position to be reached in in x-, y-, and z-direction. */
  position?:
    | Vector3d
    | undefined;
  /** Intended orientation to be reached containing yaw, pitch and roll angle. */
  orientation?:
    | Orientation3d
    | undefined;
  /**
   * Intended velocity to be reached in in x-, y-, and z-direction.
   *
   * Unit: m/s
   */
  velocity?:
    | Vector3d
    | undefined;
  /**
   * Intended acceleration to be reached in x-, y-, and z-direction.
   *
   * Unit: m/s^2
   */
  acceleration?: Vector3d | undefined;
}

/**
 * \brief Defined trajectory desired by the function.
 *
 * This trajectory is the result of the trajectory planning step in the function.
 * The task of the actuator management is to follow this trajectory as closely as possible.
 * The timestamps inside the trajectory must be defined in global simulation time.
 *
 * \note The trajectory is kept as a separate message for future extensions.
 */
export interface MotionRequest_DesiredTrajectory {
  /**
   * The trajectory consists of intended position (x, y, and z) and
   * orientation (yaw, pitch and roll) of intended state to be reached.
   * A reference to \c StatePoint message.
   *
   * \note The position within the trajectory point references to the
   * middle point of the rear axis.
   */
  trajectory_point: StatePoint[];
}

function createBaseMotionRequest(): MotionRequest {
  return {
    version: undefined,
    timestamp: undefined,
    motion_request_type: 0,
    desired_state: undefined,
    desired_trajectory: undefined,
  };
}

export const MotionRequest: MessageFns<MotionRequest> = {
  encode(message: MotionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== undefined) {
      InterfaceVersion.encode(message.version, writer.uint32(10).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(18).fork()).join();
    }
    if (message.motion_request_type !== undefined && message.motion_request_type !== 0) {
      writer.uint32(24).int32(message.motion_request_type);
    }
    if (message.desired_state !== undefined) {
      MotionRequest_DesiredState.encode(message.desired_state, writer.uint32(34).fork()).join();
    }
    if (message.desired_trajectory !== undefined) {
      MotionRequest_DesiredTrajectory.encode(message.desired_trajectory, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MotionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMotionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = InterfaceVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.motion_request_type = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.desired_state = MotionRequest_DesiredState.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.desired_trajectory = MotionRequest_DesiredTrajectory.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MotionRequest {
    return {
      version: isSet(object.version) ? InterfaceVersion.fromJSON(object.version) : undefined,
      timestamp: isSet(object.timestamp) ? Timestamp.fromJSON(object.timestamp) : undefined,
      motion_request_type: isSet(object.motion_request_type)
        ? motionRequest_MotionRequestTypeFromJSON(object.motion_request_type)
        : 0,
      desired_state: isSet(object.desired_state)
        ? MotionRequest_DesiredState.fromJSON(object.desired_state)
        : undefined,
      desired_trajectory: isSet(object.desired_trajectory)
        ? MotionRequest_DesiredTrajectory.fromJSON(object.desired_trajectory)
        : undefined,
    };
  },

  toJSON(message: MotionRequest): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = InterfaceVersion.toJSON(message.version);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = Timestamp.toJSON(message.timestamp);
    }
    if (message.motion_request_type !== undefined) {
      obj.motion_request_type = motionRequest_MotionRequestTypeToJSON(message.motion_request_type);
    }
    if (message.desired_state !== undefined) {
      obj.desired_state = MotionRequest_DesiredState.toJSON(message.desired_state);
    }
    if (message.desired_trajectory !== undefined) {
      obj.desired_trajectory = MotionRequest_DesiredTrajectory.toJSON(message.desired_trajectory);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MotionRequest>, I>>(base?: I): MotionRequest {
    return MotionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MotionRequest>, I>>(object: I): MotionRequest {
    const message = createBaseMotionRequest();
    message.version = (object.version !== undefined && object.version !== null)
      ? InterfaceVersion.fromPartial(object.version)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.motion_request_type = object.motion_request_type ?? 0;
    message.desired_state = (object.desired_state !== undefined && object.desired_state !== null)
      ? MotionRequest_DesiredState.fromPartial(object.desired_state)
      : undefined;
    message.desired_trajectory = (object.desired_trajectory !== undefined && object.desired_trajectory !== null)
      ? MotionRequest_DesiredTrajectory.fromPartial(object.desired_trajectory)
      : undefined;
    return message;
  },
};

function createBaseMotionRequest_DesiredState(): MotionRequest_DesiredState {
  return {
    timestamp: undefined,
    position: undefined,
    orientation: undefined,
    velocity: undefined,
    acceleration: undefined,
  };
}

export const MotionRequest_DesiredState: MessageFns<MotionRequest_DesiredState> = {
  encode(message: MotionRequest_DesiredState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(10).fork()).join();
    }
    if (message.position !== undefined) {
      Vector3d.encode(message.position, writer.uint32(18).fork()).join();
    }
    if (message.orientation !== undefined) {
      Orientation3d.encode(message.orientation, writer.uint32(26).fork()).join();
    }
    if (message.velocity !== undefined) {
      Vector3d.encode(message.velocity, writer.uint32(34).fork()).join();
    }
    if (message.acceleration !== undefined) {
      Vector3d.encode(message.acceleration, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MotionRequest_DesiredState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMotionRequest_DesiredState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.timestamp = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.position = Vector3d.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.orientation = Orientation3d.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.velocity = Vector3d.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.acceleration = Vector3d.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MotionRequest_DesiredState {
    return {
      timestamp: isSet(object.timestamp) ? Timestamp.fromJSON(object.timestamp) : undefined,
      position: isSet(object.position) ? Vector3d.fromJSON(object.position) : undefined,
      orientation: isSet(object.orientation) ? Orientation3d.fromJSON(object.orientation) : undefined,
      velocity: isSet(object.velocity) ? Vector3d.fromJSON(object.velocity) : undefined,
      acceleration: isSet(object.acceleration) ? Vector3d.fromJSON(object.acceleration) : undefined,
    };
  },

  toJSON(message: MotionRequest_DesiredState): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = Timestamp.toJSON(message.timestamp);
    }
    if (message.position !== undefined) {
      obj.position = Vector3d.toJSON(message.position);
    }
    if (message.orientation !== undefined) {
      obj.orientation = Orientation3d.toJSON(message.orientation);
    }
    if (message.velocity !== undefined) {
      obj.velocity = Vector3d.toJSON(message.velocity);
    }
    if (message.acceleration !== undefined) {
      obj.acceleration = Vector3d.toJSON(message.acceleration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MotionRequest_DesiredState>, I>>(base?: I): MotionRequest_DesiredState {
    return MotionRequest_DesiredState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MotionRequest_DesiredState>, I>>(object: I): MotionRequest_DesiredState {
    const message = createBaseMotionRequest_DesiredState();
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.position = (object.position !== undefined && object.position !== null)
      ? Vector3d.fromPartial(object.position)
      : undefined;
    message.orientation = (object.orientation !== undefined && object.orientation !== null)
      ? Orientation3d.fromPartial(object.orientation)
      : undefined;
    message.velocity = (object.velocity !== undefined && object.velocity !== null)
      ? Vector3d.fromPartial(object.velocity)
      : undefined;
    message.acceleration = (object.acceleration !== undefined && object.acceleration !== null)
      ? Vector3d.fromPartial(object.acceleration)
      : undefined;
    return message;
  },
};

function createBaseMotionRequest_DesiredTrajectory(): MotionRequest_DesiredTrajectory {
  return { trajectory_point: [] };
}

export const MotionRequest_DesiredTrajectory: MessageFns<MotionRequest_DesiredTrajectory> = {
  encode(message: MotionRequest_DesiredTrajectory, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.trajectory_point) {
      StatePoint.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MotionRequest_DesiredTrajectory {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMotionRequest_DesiredTrajectory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.trajectory_point.push(StatePoint.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MotionRequest_DesiredTrajectory {
    return {
      trajectory_point: globalThis.Array.isArray(object?.trajectory_point)
        ? object.trajectory_point.map((e: any) => StatePoint.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MotionRequest_DesiredTrajectory): unknown {
    const obj: any = {};
    if (message.trajectory_point?.length) {
      obj.trajectory_point = message.trajectory_point.map((e) => StatePoint.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MotionRequest_DesiredTrajectory>, I>>(base?: I): MotionRequest_DesiredTrajectory {
    return MotionRequest_DesiredTrajectory.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MotionRequest_DesiredTrajectory>, I>>(
    object: I,
  ): MotionRequest_DesiredTrajectory {
    const message = createBaseMotionRequest_DesiredTrajectory();
    message.trajectory_point = object.trajectory_point?.map((e) => StatePoint.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
