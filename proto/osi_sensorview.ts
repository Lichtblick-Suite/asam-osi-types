// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_sensorview.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Identifier, MountingPosition, Timestamp, Vector3d } from "./osi_common";
import { GroundTruth } from "./osi_groundtruth";
import { HostVehicleData } from "./osi_hostvehicledata";
import {
  CameraSensorViewConfiguration,
  GenericSensorViewConfiguration,
  LidarSensorViewConfiguration,
  RadarSensorViewConfiguration,
  UltrasonicSensorViewConfiguration,
} from "./osi_sensorviewconfiguration";
import { InterfaceVersion } from "./osi_version";

/**
 * \brief The sensor view is derived from \c GroundTruth and used as
 * input to sensor models.
 *
 * The sensor view information is supposed to provide input to sensor
 * models for simulation of actual real sensors.
 * All information regarding the environment is given with respect to
 * the virtual sensor coordinate system specified in
 * \c SensorView::mounting_position, except for the individual physical
 * technology-specific data, which is given with respect to the physical
 * sensor coordinate system specified in the corresponding physical sensor's
 * \c #mounting_position, and the \c #global_ground_truth, which is given in
 * global coordinates.
 *
 * When simulating multiple distinct sensors, each sensor can consume an
 * individual copy of the \c SensorView interface. This allows an independent
 * treatment of the sensors.
 *
 * Alternatively combined sensor models can also consume one combined
 * \c SensorView, with either combined or separate \c SensorData outputs,
 * depending on model architecture.
 */
export interface SensorView {
  /**
   * The interface version used by the sender (simulation environment).
   *
   * \rules
   * is_set
   * \endrules
   */
  version?:
    | InterfaceVersion
    | undefined;
  /**
   * The data timestamp of the simulation environment. Zero time is arbitrary
   * but must be identical for all messages. Zero time does not need to
   * coincide with the UNIX epoch. Recommended is the starting time point of
   * the simulation.
   *
   * \note For sensor view data this timestamp coincides both with the
   * notional simulation time the data applies to and the time it was sent
   * (there is no inherent latency for sensor view data, as opposed to
   * sensor data).
   *
   * \rules
   * is_set
   * \endrules
   */
  timestamp?:
    | Timestamp
    | undefined;
  /**
   * The ID of the sensor at host vehicle's \c #mounting_position.
   *
   * This is the ID of the virtual sensor, to be used in its detected
   * object output; it is distinct from the IDs of its physical detectors,
   * which are used in the detected features.
   *
   * \rules
   * is_set
   * \endrules
   */
  sensor_id?:
    | Identifier
    | undefined;
  /**
   * The virtual mounting position of the sensor (origin and orientation of
   * the sensor frame). Both origin and orientation are given in and with
   * respect to the host vehicle coordinate system [1].
   *
   * The virtual position pertains to the sensor as a whole, regardless of the
   * actual position of individual physical detectors, and governs the
   * sensor-relative coordinates in detected objects of the sensor as a whole.
   * Individual features detected by individual physical detectors are
   * governed by the actual physical mounting positions of the detectors, as
   * indicated in the technology-specific sub-views and sub-view
   * configurations.
   *
   * \arg \b x-direction of sensor coordinate system: sensor viewing direction
   * \arg \b z-direction of sensor coordinate system: sensor (up)
   * \arg \b y-direction of sensor coordinate system: perpendicular to x and z
   * right hand system
   *
   * \par Reference:
   * [1] DIN Deutsches Institut fuer Normung e. V. (2013). <em>DIN ISO 8855 Strassenfahrzeuge - Fahrzeugdynamik und Fahrverhalten - Begriffe</em>. (DIN ISO 8855:2013-11). Berlin, Germany.
   *
   * \note This field is usually static during the simulation.
   * \note The origin of vehicle's coordinate system in world frame is
   * ( \c MovingObject::base . \c BaseMoving::position +
   * Inverse_Rotation_yaw_pitch_roll( \c MovingObject::base . \c
   * BaseMoving::orientation) * \c
   * MovingObject::VehicleAttributes::bbcenter_to_rear) . The orientation of
   * the vehicle's coordinate system is equal to the orientation of the
   * vehicle's bounding box \c MovingObject::base . \c
   * BaseMoving::orientation.
   *
   * \rules
   * is_set
   * \endrules
   */
  mounting_position?:
    | MountingPosition
    | undefined;
  /** The root mean squared error of the mounting position. */
  mounting_position_rmse?:
    | MountingPosition
    | undefined;
  /**
   * Host vehicle data.
   *
   * Host vehicle data is data that the host vehicle knows about itself,
   * e.g. from location sensors, internal sensors and ECU bus data, etc.,
   * that is made available to sensors as input.
   */
  host_vehicle_data?:
    | HostVehicleData
    | undefined;
  /**
   * Ground truth w.r.t. global coordinate system.
   *
   * This is the ground truth that is provided to the sensor model by the
   * simulation environment. It may be filtered as per the requirements of
   * the sensor model as expressed by the \c SensorViewConfiguration
   * message(s) that where exchanged during the simulation initialization
   * phase.
   *
   * \note The host vehicle is always contained in the ground truth provided,
   * regardless of any filtering. The ground truth MUST contain at least as
   * much of the ground truth data, as is requested by the sensor model, but
   * MAY always contain more data, since the filtering is intended only as
   * an optimization mechanism, not as a replacement of a proper sensor
   * field of view modeling.
   */
  global_ground_truth?:
    | GroundTruth
    | undefined;
  /**
   * The ID of the host vehicle in the \c #global_ground_truth data.
   *
   * \rules
   * refers_to: 'MovingObject'
   * is_set
   * \endrules
   */
  host_vehicle_id?:
    | Identifier
    | undefined;
  /** Generic SensorView(s). */
  generic_sensor_view: GenericSensorView[];
  /** Radar-specific SensorView(s). */
  radar_sensor_view: RadarSensorView[];
  /** Lidar-specific SensorView(s). */
  lidar_sensor_view: LidarSensorView[];
  /** Camera-specific SensorView(s). */
  camera_sensor_view: CameraSensorView[];
  /** Ultrasonic-specific SensorView(s). */
  ultrasonic_sensor_view: UltrasonicSensorView[];
}

/**
 * \brief Definition of the generic sensor view.
 *
 * Generic sensor view data.
 */
export interface GenericSensorView {
  /** Generic view configuration valid at the time the data was created. */
  view_configuration?: GenericSensorViewConfiguration | undefined;
}

/**
 * \brief Definition of the radar sensor view.
 *
 * Radar specific sensor view data.
 */
export interface RadarSensorView {
  /** Radar view configuration valid at the time the data was created. */
  view_configuration?:
    | RadarSensorViewConfiguration
    | undefined;
  /**
   * Ray tracing data.
   *
   * This field includes one entry for each ray, in left-to-right,
   * top-to-bottom order (think of scan lines in a TV).
   */
  reflection: RadarSensorView_Reflection[];
}

/** \brief Definition of the radar reflection. */
export interface RadarSensorView_Reflection {
  /**
   * Relative signal level of the reflection.
   *
   * This takes the combined antenna diagram (losses in TX and RX)
   * as well as the signal losses due to scattering and absorption
   * into account, and will, when multiplied by TX power yield the
   * actual RX power.
   *
   * Unit: dB
   */
  signal_strength?:
    | number
    | undefined;
  /**
   * Time of flight.
   *
   * This is the time of flight of the reflection, which is directly
   * proportional to the distance traveled.
   *
   * Unit: s
   */
  time_of_flight?:
    | number
    | undefined;
  /**
   * Doppler shift.
   *
   * Shift in frequency based on the specified TX frequency.
   *
   * Unit: Hz
   */
  doppler_shift?:
    | number
    | undefined;
  /**
   * TX horizontal angle (azimuth).
   *
   * Horizontal angle of incidence of the source of the reflection
   * at the TX antenna.
   *
   * Unit: rad
   */
  source_horizontal_angle?:
    | number
    | undefined;
  /**
   * TX vertical angle (elevation).
   *
   * Vertical angle of incidence of the source of the reflection
   * at the TX antenna.
   *
   * Unit: rad
   */
  source_vertical_angle?: number | undefined;
}

/**
 * \brief Definition of the lidar sensor view.
 *
 * Lidar specific sensor view data.
 */
export interface LidarSensorView {
  /** Lidar view configuration valid at the time the data was created. */
  view_configuration?:
    | LidarSensorViewConfiguration
    | undefined;
  /**
   * Ray tracing data.
   *
   * This field includes one entry for each ray, in left-to-right,
   * top-to-bottom order (think of scan lines in a TV).
   */
  reflection: LidarSensorView_Reflection[];
}

/** \brief Definition of the lidar reflection. */
export interface LidarSensorView_Reflection {
  /**
   * Relative signal level of the reflection.
   *
   * This takes the signal losses due to scattering and absorption
   * into account, and will, when multiplied by TX power yield the
   * potential RX power (disregarding any other RX/TX losses).
   *
   * Unit: dB
   */
  signal_strength?:
    | number
    | undefined;
  /**
   * Time of flight.
   *
   * This is the time of flight of the reflection, which is directly
   * proportional to the distance traveled.
   *
   * Unit: s
   */
  time_of_flight?:
    | number
    | undefined;
  /**
   * Doppler shift.
   *
   * Shift in frequency based on the specified TX frequency.
   *
   * Unit: Hz
   */
  doppler_shift?:
    | number
    | undefined;
  /**
   * normal to surface angle.
   *
   * The normal of the transmitted beam to the object, road marking, etc.
   * encounter. \note data is in Lidar coordinate system
   *
   * Unit: unit vector
   */
  normal_to_surface?:
    | Vector3d
    | undefined;
  /**
   * ID of the detected object this reflection is associated to.
   * can be used for ray tracing debug
   *
   * \note ID = MAX(uint64) indicates no reference to an object.
   */
  object_id?: Identifier | undefined;
}

/**
 * \brief Definition of the camera sensor view.
 *
 * Camera specific sensor view data.
 */
export interface CameraSensorView {
  /** Camera view configuration valid at the time the data was created. */
  view_configuration?:
    | CameraSensorViewConfiguration
    | undefined;
  /**
   * Raw image data.
   *
   * The raw image data in the memory layout specified by the camera
   * sensor input configuration. The pixel order is specified in
   * CameraSensorViewConfiguration.pixel_order with the
   * default value PIXEL_ORDER_DEFAULT (i.e. left-to-right, top-to-bottom).
   */
  image_data?: Uint8Array | undefined;
}

/**
 * \brief Definition of the ultrasonic sensor view.
 *
 * Ultrasonic specific sensor view data.
 */
export interface UltrasonicSensorView {
  /** Ultrasonic view configuration valid at the time the data was created. */
  view_configuration?: UltrasonicSensorViewConfiguration | undefined;
}

function createBaseSensorView(): SensorView {
  return {
    version: undefined,
    timestamp: undefined,
    sensor_id: undefined,
    mounting_position: undefined,
    mounting_position_rmse: undefined,
    host_vehicle_data: undefined,
    global_ground_truth: undefined,
    host_vehicle_id: undefined,
    generic_sensor_view: [],
    radar_sensor_view: [],
    lidar_sensor_view: [],
    camera_sensor_view: [],
    ultrasonic_sensor_view: [],
  };
}

export const SensorView: MessageFns<SensorView> = {
  encode(message: SensorView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== undefined) {
      InterfaceVersion.encode(message.version, writer.uint32(10).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(18).fork()).join();
    }
    if (message.sensor_id !== undefined) {
      Identifier.encode(message.sensor_id, writer.uint32(26).fork()).join();
    }
    if (message.mounting_position !== undefined) {
      MountingPosition.encode(message.mounting_position, writer.uint32(34).fork()).join();
    }
    if (message.mounting_position_rmse !== undefined) {
      MountingPosition.encode(message.mounting_position_rmse, writer.uint32(42).fork()).join();
    }
    if (message.host_vehicle_data !== undefined) {
      HostVehicleData.encode(message.host_vehicle_data, writer.uint32(50).fork()).join();
    }
    if (message.global_ground_truth !== undefined) {
      GroundTruth.encode(message.global_ground_truth, writer.uint32(58).fork()).join();
    }
    if (message.host_vehicle_id !== undefined) {
      Identifier.encode(message.host_vehicle_id, writer.uint32(66).fork()).join();
    }
    for (const v of message.generic_sensor_view) {
      GenericSensorView.encode(v!, writer.uint32(8002).fork()).join();
    }
    for (const v of message.radar_sensor_view) {
      RadarSensorView.encode(v!, writer.uint32(8010).fork()).join();
    }
    for (const v of message.lidar_sensor_view) {
      LidarSensorView.encode(v!, writer.uint32(8018).fork()).join();
    }
    for (const v of message.camera_sensor_view) {
      CameraSensorView.encode(v!, writer.uint32(8026).fork()).join();
    }
    for (const v of message.ultrasonic_sensor_view) {
      UltrasonicSensorView.encode(v!, writer.uint32(8034).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SensorView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSensorView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = InterfaceVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sensor_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mounting_position = MountingPosition.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mounting_position_rmse = MountingPosition.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.host_vehicle_data = HostVehicleData.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.global_ground_truth = GroundTruth.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.host_vehicle_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 1000: {
          if (tag !== 8002) {
            break;
          }

          message.generic_sensor_view.push(GenericSensorView.decode(reader, reader.uint32()));
          continue;
        }
        case 1001: {
          if (tag !== 8010) {
            break;
          }

          message.radar_sensor_view.push(RadarSensorView.decode(reader, reader.uint32()));
          continue;
        }
        case 1002: {
          if (tag !== 8018) {
            break;
          }

          message.lidar_sensor_view.push(LidarSensorView.decode(reader, reader.uint32()));
          continue;
        }
        case 1003: {
          if (tag !== 8026) {
            break;
          }

          message.camera_sensor_view.push(CameraSensorView.decode(reader, reader.uint32()));
          continue;
        }
        case 1004: {
          if (tag !== 8034) {
            break;
          }

          message.ultrasonic_sensor_view.push(UltrasonicSensorView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SensorView {
    return {
      version: isSet(object.version) ? InterfaceVersion.fromJSON(object.version) : undefined,
      timestamp: isSet(object.timestamp) ? Timestamp.fromJSON(object.timestamp) : undefined,
      sensor_id: isSet(object.sensor_id) ? Identifier.fromJSON(object.sensor_id) : undefined,
      mounting_position: isSet(object.mounting_position)
        ? MountingPosition.fromJSON(object.mounting_position)
        : undefined,
      mounting_position_rmse: isSet(object.mounting_position_rmse)
        ? MountingPosition.fromJSON(object.mounting_position_rmse)
        : undefined,
      host_vehicle_data: isSet(object.host_vehicle_data)
        ? HostVehicleData.fromJSON(object.host_vehicle_data)
        : undefined,
      global_ground_truth: isSet(object.global_ground_truth)
        ? GroundTruth.fromJSON(object.global_ground_truth)
        : undefined,
      host_vehicle_id: isSet(object.host_vehicle_id) ? Identifier.fromJSON(object.host_vehicle_id) : undefined,
      generic_sensor_view: globalThis.Array.isArray(object?.generic_sensor_view)
        ? object.generic_sensor_view.map((e: any) => GenericSensorView.fromJSON(e))
        : [],
      radar_sensor_view: globalThis.Array.isArray(object?.radar_sensor_view)
        ? object.radar_sensor_view.map((e: any) => RadarSensorView.fromJSON(e))
        : [],
      lidar_sensor_view: globalThis.Array.isArray(object?.lidar_sensor_view)
        ? object.lidar_sensor_view.map((e: any) => LidarSensorView.fromJSON(e))
        : [],
      camera_sensor_view: globalThis.Array.isArray(object?.camera_sensor_view)
        ? object.camera_sensor_view.map((e: any) => CameraSensorView.fromJSON(e))
        : [],
      ultrasonic_sensor_view: globalThis.Array.isArray(object?.ultrasonic_sensor_view)
        ? object.ultrasonic_sensor_view.map((e: any) => UltrasonicSensorView.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SensorView): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = InterfaceVersion.toJSON(message.version);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = Timestamp.toJSON(message.timestamp);
    }
    if (message.sensor_id !== undefined) {
      obj.sensor_id = Identifier.toJSON(message.sensor_id);
    }
    if (message.mounting_position !== undefined) {
      obj.mounting_position = MountingPosition.toJSON(message.mounting_position);
    }
    if (message.mounting_position_rmse !== undefined) {
      obj.mounting_position_rmse = MountingPosition.toJSON(message.mounting_position_rmse);
    }
    if (message.host_vehicle_data !== undefined) {
      obj.host_vehicle_data = HostVehicleData.toJSON(message.host_vehicle_data);
    }
    if (message.global_ground_truth !== undefined) {
      obj.global_ground_truth = GroundTruth.toJSON(message.global_ground_truth);
    }
    if (message.host_vehicle_id !== undefined) {
      obj.host_vehicle_id = Identifier.toJSON(message.host_vehicle_id);
    }
    if (message.generic_sensor_view?.length) {
      obj.generic_sensor_view = message.generic_sensor_view.map((e) => GenericSensorView.toJSON(e));
    }
    if (message.radar_sensor_view?.length) {
      obj.radar_sensor_view = message.radar_sensor_view.map((e) => RadarSensorView.toJSON(e));
    }
    if (message.lidar_sensor_view?.length) {
      obj.lidar_sensor_view = message.lidar_sensor_view.map((e) => LidarSensorView.toJSON(e));
    }
    if (message.camera_sensor_view?.length) {
      obj.camera_sensor_view = message.camera_sensor_view.map((e) => CameraSensorView.toJSON(e));
    }
    if (message.ultrasonic_sensor_view?.length) {
      obj.ultrasonic_sensor_view = message.ultrasonic_sensor_view.map((e) => UltrasonicSensorView.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SensorView>, I>>(base?: I): SensorView {
    return SensorView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SensorView>, I>>(object: I): SensorView {
    const message = createBaseSensorView();
    message.version = (object.version !== undefined && object.version !== null)
      ? InterfaceVersion.fromPartial(object.version)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.sensor_id = (object.sensor_id !== undefined && object.sensor_id !== null)
      ? Identifier.fromPartial(object.sensor_id)
      : undefined;
    message.mounting_position = (object.mounting_position !== undefined && object.mounting_position !== null)
      ? MountingPosition.fromPartial(object.mounting_position)
      : undefined;
    message.mounting_position_rmse =
      (object.mounting_position_rmse !== undefined && object.mounting_position_rmse !== null)
        ? MountingPosition.fromPartial(object.mounting_position_rmse)
        : undefined;
    message.host_vehicle_data = (object.host_vehicle_data !== undefined && object.host_vehicle_data !== null)
      ? HostVehicleData.fromPartial(object.host_vehicle_data)
      : undefined;
    message.global_ground_truth = (object.global_ground_truth !== undefined && object.global_ground_truth !== null)
      ? GroundTruth.fromPartial(object.global_ground_truth)
      : undefined;
    message.host_vehicle_id = (object.host_vehicle_id !== undefined && object.host_vehicle_id !== null)
      ? Identifier.fromPartial(object.host_vehicle_id)
      : undefined;
    message.generic_sensor_view = object.generic_sensor_view?.map((e) => GenericSensorView.fromPartial(e)) || [];
    message.radar_sensor_view = object.radar_sensor_view?.map((e) => RadarSensorView.fromPartial(e)) || [];
    message.lidar_sensor_view = object.lidar_sensor_view?.map((e) => LidarSensorView.fromPartial(e)) || [];
    message.camera_sensor_view = object.camera_sensor_view?.map((e) => CameraSensorView.fromPartial(e)) || [];
    message.ultrasonic_sensor_view = object.ultrasonic_sensor_view?.map((e) => UltrasonicSensorView.fromPartial(e)) ||
      [];
    return message;
  },
};

function createBaseGenericSensorView(): GenericSensorView {
  return { view_configuration: undefined };
}

export const GenericSensorView: MessageFns<GenericSensorView> = {
  encode(message: GenericSensorView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.view_configuration !== undefined) {
      GenericSensorViewConfiguration.encode(message.view_configuration, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenericSensorView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenericSensorView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.view_configuration = GenericSensorViewConfiguration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenericSensorView {
    return {
      view_configuration: isSet(object.view_configuration)
        ? GenericSensorViewConfiguration.fromJSON(object.view_configuration)
        : undefined,
    };
  },

  toJSON(message: GenericSensorView): unknown {
    const obj: any = {};
    if (message.view_configuration !== undefined) {
      obj.view_configuration = GenericSensorViewConfiguration.toJSON(message.view_configuration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenericSensorView>, I>>(base?: I): GenericSensorView {
    return GenericSensorView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenericSensorView>, I>>(object: I): GenericSensorView {
    const message = createBaseGenericSensorView();
    message.view_configuration = (object.view_configuration !== undefined && object.view_configuration !== null)
      ? GenericSensorViewConfiguration.fromPartial(object.view_configuration)
      : undefined;
    return message;
  },
};

function createBaseRadarSensorView(): RadarSensorView {
  return { view_configuration: undefined, reflection: [] };
}

export const RadarSensorView: MessageFns<RadarSensorView> = {
  encode(message: RadarSensorView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.view_configuration !== undefined) {
      RadarSensorViewConfiguration.encode(message.view_configuration, writer.uint32(10).fork()).join();
    }
    for (const v of message.reflection) {
      RadarSensorView_Reflection.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RadarSensorView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRadarSensorView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.view_configuration = RadarSensorViewConfiguration.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reflection.push(RadarSensorView_Reflection.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RadarSensorView {
    return {
      view_configuration: isSet(object.view_configuration)
        ? RadarSensorViewConfiguration.fromJSON(object.view_configuration)
        : undefined,
      reflection: globalThis.Array.isArray(object?.reflection)
        ? object.reflection.map((e: any) => RadarSensorView_Reflection.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RadarSensorView): unknown {
    const obj: any = {};
    if (message.view_configuration !== undefined) {
      obj.view_configuration = RadarSensorViewConfiguration.toJSON(message.view_configuration);
    }
    if (message.reflection?.length) {
      obj.reflection = message.reflection.map((e) => RadarSensorView_Reflection.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RadarSensorView>, I>>(base?: I): RadarSensorView {
    return RadarSensorView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RadarSensorView>, I>>(object: I): RadarSensorView {
    const message = createBaseRadarSensorView();
    message.view_configuration = (object.view_configuration !== undefined && object.view_configuration !== null)
      ? RadarSensorViewConfiguration.fromPartial(object.view_configuration)
      : undefined;
    message.reflection = object.reflection?.map((e) => RadarSensorView_Reflection.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRadarSensorView_Reflection(): RadarSensorView_Reflection {
  return {
    signal_strength: 0,
    time_of_flight: 0,
    doppler_shift: 0,
    source_horizontal_angle: 0,
    source_vertical_angle: 0,
  };
}

export const RadarSensorView_Reflection: MessageFns<RadarSensorView_Reflection> = {
  encode(message: RadarSensorView_Reflection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signal_strength !== undefined && message.signal_strength !== 0) {
      writer.uint32(9).double(message.signal_strength);
    }
    if (message.time_of_flight !== undefined && message.time_of_flight !== 0) {
      writer.uint32(17).double(message.time_of_flight);
    }
    if (message.doppler_shift !== undefined && message.doppler_shift !== 0) {
      writer.uint32(25).double(message.doppler_shift);
    }
    if (message.source_horizontal_angle !== undefined && message.source_horizontal_angle !== 0) {
      writer.uint32(33).double(message.source_horizontal_angle);
    }
    if (message.source_vertical_angle !== undefined && message.source_vertical_angle !== 0) {
      writer.uint32(41).double(message.source_vertical_angle);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RadarSensorView_Reflection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRadarSensorView_Reflection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.signal_strength = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.time_of_flight = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.doppler_shift = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.source_horizontal_angle = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.source_vertical_angle = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RadarSensorView_Reflection {
    return {
      signal_strength: isSet(object.signal_strength) ? globalThis.Number(object.signal_strength) : 0,
      time_of_flight: isSet(object.time_of_flight) ? globalThis.Number(object.time_of_flight) : 0,
      doppler_shift: isSet(object.doppler_shift) ? globalThis.Number(object.doppler_shift) : 0,
      source_horizontal_angle: isSet(object.source_horizontal_angle)
        ? globalThis.Number(object.source_horizontal_angle)
        : 0,
      source_vertical_angle: isSet(object.source_vertical_angle) ? globalThis.Number(object.source_vertical_angle) : 0,
    };
  },

  toJSON(message: RadarSensorView_Reflection): unknown {
    const obj: any = {};
    if (message.signal_strength !== undefined) {
      obj.signal_strength = message.signal_strength;
    }
    if (message.time_of_flight !== undefined) {
      obj.time_of_flight = message.time_of_flight;
    }
    if (message.doppler_shift !== undefined) {
      obj.doppler_shift = message.doppler_shift;
    }
    if (message.source_horizontal_angle !== undefined) {
      obj.source_horizontal_angle = message.source_horizontal_angle;
    }
    if (message.source_vertical_angle !== undefined) {
      obj.source_vertical_angle = message.source_vertical_angle;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RadarSensorView_Reflection>, I>>(base?: I): RadarSensorView_Reflection {
    return RadarSensorView_Reflection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RadarSensorView_Reflection>, I>>(object: I): RadarSensorView_Reflection {
    const message = createBaseRadarSensorView_Reflection();
    message.signal_strength = object.signal_strength ?? 0;
    message.time_of_flight = object.time_of_flight ?? 0;
    message.doppler_shift = object.doppler_shift ?? 0;
    message.source_horizontal_angle = object.source_horizontal_angle ?? 0;
    message.source_vertical_angle = object.source_vertical_angle ?? 0;
    return message;
  },
};

function createBaseLidarSensorView(): LidarSensorView {
  return { view_configuration: undefined, reflection: [] };
}

export const LidarSensorView: MessageFns<LidarSensorView> = {
  encode(message: LidarSensorView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.view_configuration !== undefined) {
      LidarSensorViewConfiguration.encode(message.view_configuration, writer.uint32(10).fork()).join();
    }
    for (const v of message.reflection) {
      LidarSensorView_Reflection.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LidarSensorView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLidarSensorView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.view_configuration = LidarSensorViewConfiguration.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reflection.push(LidarSensorView_Reflection.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LidarSensorView {
    return {
      view_configuration: isSet(object.view_configuration)
        ? LidarSensorViewConfiguration.fromJSON(object.view_configuration)
        : undefined,
      reflection: globalThis.Array.isArray(object?.reflection)
        ? object.reflection.map((e: any) => LidarSensorView_Reflection.fromJSON(e))
        : [],
    };
  },

  toJSON(message: LidarSensorView): unknown {
    const obj: any = {};
    if (message.view_configuration !== undefined) {
      obj.view_configuration = LidarSensorViewConfiguration.toJSON(message.view_configuration);
    }
    if (message.reflection?.length) {
      obj.reflection = message.reflection.map((e) => LidarSensorView_Reflection.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LidarSensorView>, I>>(base?: I): LidarSensorView {
    return LidarSensorView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LidarSensorView>, I>>(object: I): LidarSensorView {
    const message = createBaseLidarSensorView();
    message.view_configuration = (object.view_configuration !== undefined && object.view_configuration !== null)
      ? LidarSensorViewConfiguration.fromPartial(object.view_configuration)
      : undefined;
    message.reflection = object.reflection?.map((e) => LidarSensorView_Reflection.fromPartial(e)) || [];
    return message;
  },
};

function createBaseLidarSensorView_Reflection(): LidarSensorView_Reflection {
  return {
    signal_strength: 0,
    time_of_flight: 0,
    doppler_shift: 0,
    normal_to_surface: undefined,
    object_id: undefined,
  };
}

export const LidarSensorView_Reflection: MessageFns<LidarSensorView_Reflection> = {
  encode(message: LidarSensorView_Reflection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signal_strength !== undefined && message.signal_strength !== 0) {
      writer.uint32(9).double(message.signal_strength);
    }
    if (message.time_of_flight !== undefined && message.time_of_flight !== 0) {
      writer.uint32(17).double(message.time_of_flight);
    }
    if (message.doppler_shift !== undefined && message.doppler_shift !== 0) {
      writer.uint32(25).double(message.doppler_shift);
    }
    if (message.normal_to_surface !== undefined) {
      Vector3d.encode(message.normal_to_surface, writer.uint32(42).fork()).join();
    }
    if (message.object_id !== undefined) {
      Identifier.encode(message.object_id, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LidarSensorView_Reflection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLidarSensorView_Reflection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.signal_strength = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.time_of_flight = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.doppler_shift = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.normal_to_surface = Vector3d.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.object_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LidarSensorView_Reflection {
    return {
      signal_strength: isSet(object.signal_strength) ? globalThis.Number(object.signal_strength) : 0,
      time_of_flight: isSet(object.time_of_flight) ? globalThis.Number(object.time_of_flight) : 0,
      doppler_shift: isSet(object.doppler_shift) ? globalThis.Number(object.doppler_shift) : 0,
      normal_to_surface: isSet(object.normal_to_surface) ? Vector3d.fromJSON(object.normal_to_surface) : undefined,
      object_id: isSet(object.object_id) ? Identifier.fromJSON(object.object_id) : undefined,
    };
  },

  toJSON(message: LidarSensorView_Reflection): unknown {
    const obj: any = {};
    if (message.signal_strength !== undefined) {
      obj.signal_strength = message.signal_strength;
    }
    if (message.time_of_flight !== undefined) {
      obj.time_of_flight = message.time_of_flight;
    }
    if (message.doppler_shift !== undefined) {
      obj.doppler_shift = message.doppler_shift;
    }
    if (message.normal_to_surface !== undefined) {
      obj.normal_to_surface = Vector3d.toJSON(message.normal_to_surface);
    }
    if (message.object_id !== undefined) {
      obj.object_id = Identifier.toJSON(message.object_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LidarSensorView_Reflection>, I>>(base?: I): LidarSensorView_Reflection {
    return LidarSensorView_Reflection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LidarSensorView_Reflection>, I>>(object: I): LidarSensorView_Reflection {
    const message = createBaseLidarSensorView_Reflection();
    message.signal_strength = object.signal_strength ?? 0;
    message.time_of_flight = object.time_of_flight ?? 0;
    message.doppler_shift = object.doppler_shift ?? 0;
    message.normal_to_surface = (object.normal_to_surface !== undefined && object.normal_to_surface !== null)
      ? Vector3d.fromPartial(object.normal_to_surface)
      : undefined;
    message.object_id = (object.object_id !== undefined && object.object_id !== null)
      ? Identifier.fromPartial(object.object_id)
      : undefined;
    return message;
  },
};

function createBaseCameraSensorView(): CameraSensorView {
  return { view_configuration: undefined, image_data: new Uint8Array(0) };
}

export const CameraSensorView: MessageFns<CameraSensorView> = {
  encode(message: CameraSensorView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.view_configuration !== undefined) {
      CameraSensorViewConfiguration.encode(message.view_configuration, writer.uint32(10).fork()).join();
    }
    if (message.image_data !== undefined && message.image_data.length !== 0) {
      writer.uint32(18).bytes(message.image_data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CameraSensorView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCameraSensorView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.view_configuration = CameraSensorViewConfiguration.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.image_data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CameraSensorView {
    return {
      view_configuration: isSet(object.view_configuration)
        ? CameraSensorViewConfiguration.fromJSON(object.view_configuration)
        : undefined,
      image_data: isSet(object.image_data) ? bytesFromBase64(object.image_data) : new Uint8Array(0),
    };
  },

  toJSON(message: CameraSensorView): unknown {
    const obj: any = {};
    if (message.view_configuration !== undefined) {
      obj.view_configuration = CameraSensorViewConfiguration.toJSON(message.view_configuration);
    }
    if (message.image_data !== undefined) {
      obj.image_data = base64FromBytes(message.image_data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CameraSensorView>, I>>(base?: I): CameraSensorView {
    return CameraSensorView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CameraSensorView>, I>>(object: I): CameraSensorView {
    const message = createBaseCameraSensorView();
    message.view_configuration = (object.view_configuration !== undefined && object.view_configuration !== null)
      ? CameraSensorViewConfiguration.fromPartial(object.view_configuration)
      : undefined;
    message.image_data = object.image_data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseUltrasonicSensorView(): UltrasonicSensorView {
  return { view_configuration: undefined };
}

export const UltrasonicSensorView: MessageFns<UltrasonicSensorView> = {
  encode(message: UltrasonicSensorView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.view_configuration !== undefined) {
      UltrasonicSensorViewConfiguration.encode(message.view_configuration, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UltrasonicSensorView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUltrasonicSensorView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.view_configuration = UltrasonicSensorViewConfiguration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UltrasonicSensorView {
    return {
      view_configuration: isSet(object.view_configuration)
        ? UltrasonicSensorViewConfiguration.fromJSON(object.view_configuration)
        : undefined,
    };
  },

  toJSON(message: UltrasonicSensorView): unknown {
    const obj: any = {};
    if (message.view_configuration !== undefined) {
      obj.view_configuration = UltrasonicSensorViewConfiguration.toJSON(message.view_configuration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UltrasonicSensorView>, I>>(base?: I): UltrasonicSensorView {
    return UltrasonicSensorView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UltrasonicSensorView>, I>>(object: I): UltrasonicSensorView {
    const message = createBaseUltrasonicSensorView();
    message.view_configuration = (object.view_configuration !== undefined && object.view_configuration !== null)
      ? UltrasonicSensorViewConfiguration.fromPartial(object.view_configuration)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
