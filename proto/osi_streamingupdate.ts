// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_streamingupdate.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Identifier, Timestamp } from "./osi_common";
import { EnvironmentalConditions } from "./osi_environment";
import { HostVehicleData } from "./osi_hostvehicledata";
import { MovingObject, StationaryObject } from "./osi_object";
import { TrafficLight } from "./osi_trafficlight";
import { TrafficSign } from "./osi_trafficsign";
import { InterfaceVersion } from "./osi_version";

/**
 * \brief The streaming update interface enables simulation entities to send
 * partial updates to other modules that favor performance (especially latency)
 * over data completeness/consistency (e.g. visualization applications).
 *
 * Static and/or non-relevant objects can be omitted as required for the
 * specific use case. Adding an object's unique id to the repeated field \c
 * obsolete_id indicates that it will no longer be updated from then on.
 *
 * \note The receiver of partial streaming update messages can only rely on the
 * most up-to-date information at the corresponding timestamp. E.g. omitting
 * objects does not indicate static behavior but it may be sufficient for the
 * use case to update certain objects at a later point in time.
 */
export interface StreamingUpdate {
  /**
   * The interface version used by the sender.
   *
   * \rules
   * is_set
   * \endrules
   */
  version?:
    | InterfaceVersion
    | undefined;
  /**
   * The data timestamp where the information of contained objects is calculated.
   *
   * Zero time is arbitrary but must be identical for all messages.
   * Zero time does not need to coincide with the UNIX epoch.
   * Recommended is the starting time point of the simulation.
   *
   * \rules
   * is_set
   * \endrules
   */
  timestamp?:
    | Timestamp
    | undefined;
  /**
   * The list of stationary objects (excluding traffic signs and traffic
   * lights).
   */
  stationary_object_update: StationaryObject[];
  /** The list of moving objects. */
  moving_object_update: MovingObject[];
  /** The list of traffic signs. */
  traffic_sign_update: TrafficSign[];
  /** The list of traffic lights. */
  traffic_light_update: TrafficLight[];
  /** Conditions of the environment. */
  environmental_conditions_update?:
    | EnvironmentalConditions
    | undefined;
  /**
   * Host vehicle data.
   *
   * Host vehicle data is data that the host vehicle knows about itself,
   * e.g. from location sensors, internal sensors and ECU bus data, etc.,
   * that is made available to sensors as input.
   *
   * The ID inside this message allows an association to moving object data.
   */
  host_vehicle_data_update: HostVehicleData[];
  /**
   * Entities that will no longer be updated, because they are considered
   * obsolete by the sender.
   *
   * \note IDs are globally unique.
   */
  obsolete_id: Identifier[];
}

function createBaseStreamingUpdate(): StreamingUpdate {
  return {
    version: undefined,
    timestamp: undefined,
    stationary_object_update: [],
    moving_object_update: [],
    traffic_sign_update: [],
    traffic_light_update: [],
    environmental_conditions_update: undefined,
    host_vehicle_data_update: [],
    obsolete_id: [],
  };
}

export const StreamingUpdate: MessageFns<StreamingUpdate> = {
  encode(message: StreamingUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== undefined) {
      InterfaceVersion.encode(message.version, writer.uint32(10).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(18).fork()).join();
    }
    for (const v of message.stationary_object_update) {
      StationaryObject.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.moving_object_update) {
      MovingObject.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.traffic_sign_update) {
      TrafficSign.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.traffic_light_update) {
      TrafficLight.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.environmental_conditions_update !== undefined) {
      EnvironmentalConditions.encode(message.environmental_conditions_update, writer.uint32(58).fork()).join();
    }
    for (const v of message.host_vehicle_data_update) {
      HostVehicleData.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.obsolete_id) {
      Identifier.encode(v!, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamingUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamingUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = InterfaceVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stationary_object_update.push(StationaryObject.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.moving_object_update.push(MovingObject.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.traffic_sign_update.push(TrafficSign.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.traffic_light_update.push(TrafficLight.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.environmental_conditions_update = EnvironmentalConditions.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.host_vehicle_data_update.push(HostVehicleData.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.obsolete_id.push(Identifier.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamingUpdate {
    return {
      version: isSet(object.version) ? InterfaceVersion.fromJSON(object.version) : undefined,
      timestamp: isSet(object.timestamp) ? Timestamp.fromJSON(object.timestamp) : undefined,
      stationary_object_update: globalThis.Array.isArray(object?.stationary_object_update)
        ? object.stationary_object_update.map((e: any) => StationaryObject.fromJSON(e))
        : [],
      moving_object_update: globalThis.Array.isArray(object?.moving_object_update)
        ? object.moving_object_update.map((e: any) => MovingObject.fromJSON(e))
        : [],
      traffic_sign_update: globalThis.Array.isArray(object?.traffic_sign_update)
        ? object.traffic_sign_update.map((e: any) => TrafficSign.fromJSON(e))
        : [],
      traffic_light_update: globalThis.Array.isArray(object?.traffic_light_update)
        ? object.traffic_light_update.map((e: any) => TrafficLight.fromJSON(e))
        : [],
      environmental_conditions_update: isSet(object.environmental_conditions_update)
        ? EnvironmentalConditions.fromJSON(object.environmental_conditions_update)
        : undefined,
      host_vehicle_data_update: globalThis.Array.isArray(object?.host_vehicle_data_update)
        ? object.host_vehicle_data_update.map((e: any) => HostVehicleData.fromJSON(e))
        : [],
      obsolete_id: globalThis.Array.isArray(object?.obsolete_id)
        ? object.obsolete_id.map((e: any) => Identifier.fromJSON(e))
        : [],
    };
  },

  toJSON(message: StreamingUpdate): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = InterfaceVersion.toJSON(message.version);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = Timestamp.toJSON(message.timestamp);
    }
    if (message.stationary_object_update?.length) {
      obj.stationary_object_update = message.stationary_object_update.map((e) => StationaryObject.toJSON(e));
    }
    if (message.moving_object_update?.length) {
      obj.moving_object_update = message.moving_object_update.map((e) => MovingObject.toJSON(e));
    }
    if (message.traffic_sign_update?.length) {
      obj.traffic_sign_update = message.traffic_sign_update.map((e) => TrafficSign.toJSON(e));
    }
    if (message.traffic_light_update?.length) {
      obj.traffic_light_update = message.traffic_light_update.map((e) => TrafficLight.toJSON(e));
    }
    if (message.environmental_conditions_update !== undefined) {
      obj.environmental_conditions_update = EnvironmentalConditions.toJSON(message.environmental_conditions_update);
    }
    if (message.host_vehicle_data_update?.length) {
      obj.host_vehicle_data_update = message.host_vehicle_data_update.map((e) => HostVehicleData.toJSON(e));
    }
    if (message.obsolete_id?.length) {
      obj.obsolete_id = message.obsolete_id.map((e) => Identifier.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamingUpdate>, I>>(base?: I): StreamingUpdate {
    return StreamingUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamingUpdate>, I>>(object: I): StreamingUpdate {
    const message = createBaseStreamingUpdate();
    message.version = (object.version !== undefined && object.version !== null)
      ? InterfaceVersion.fromPartial(object.version)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.stationary_object_update = object.stationary_object_update?.map((e) => StationaryObject.fromPartial(e)) ||
      [];
    message.moving_object_update = object.moving_object_update?.map((e) => MovingObject.fromPartial(e)) || [];
    message.traffic_sign_update = object.traffic_sign_update?.map((e) => TrafficSign.fromPartial(e)) || [];
    message.traffic_light_update = object.traffic_light_update?.map((e) => TrafficLight.fromPartial(e)) || [];
    message.environmental_conditions_update =
      (object.environmental_conditions_update !== undefined && object.environmental_conditions_update !== null)
        ? EnvironmentalConditions.fromPartial(object.environmental_conditions_update)
        : undefined;
    message.host_vehicle_data_update = object.host_vehicle_data_update?.map((e) => HostVehicleData.fromPartial(e)) ||
      [];
    message.obsolete_id = object.obsolete_id?.map((e) => Identifier.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
