// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_occupant.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ExternalReference, Identifier } from "./osi_common";

/** \brief An occupant of a host vehicle, especially the driver of the vehicle. */
export interface Occupant {
  /**
   * The ID of the driver.
   *
   * \rules
   * is_globally_unique
   * \endrules
   */
  id?:
    | Identifier
    | undefined;
  /** Specific information about the classification of the occupant. */
  classification?:
    | Occupant_Classification
    | undefined;
  /**
   * External reference to the occupant source.
   *
   * \note For OpenDRIVE and OpenSCENARIO there is no direct counterpart.
   *
   * \note For non-ASAM Standards, it is implementation-specific how
   *       source_reference is resolved.
   *
   * \note The value has to be repeated because one object may be derived
   *       from more than one origin source, for example, from a scenario file
   *       and from sensors.
   */
  source_reference: ExternalReference[];
}

/** \brief Information regarding the classification of the occupant. */
export interface Occupant_Classification {
  /**
   * Flag determining whether the person is the driver of the vehicle or a
   * passenger.
   */
  is_driver?:
    | boolean
    | undefined;
  /** Seat position of the vehicle occupant. */
  seat?:
    | Occupant_Classification_Seat
    | undefined;
  /**
   * Describes the state of the passenger's hands related to the steering
   * wheel (mostly driver).
   */
  steering_control?: Occupant_Classification_SteeringControl | undefined;
}

/** Definition of seat positions. */
export enum Occupant_Classification_Seat {
  /** SEAT_UNKNOWN - Seat position is unknown (must not be used in ground truth). */
  SEAT_UNKNOWN = 0,
  /** SEAT_OTHER - Other (unspecified but known) seat. */
  SEAT_OTHER = 1,
  /**
   * SEAT_FRONT_LEFT - Seat position is in the front row, left seat.
   * This is usually the driver's seat in right-hand traffic.
   */
  SEAT_FRONT_LEFT = 2,
  /**
   * SEAT_FRONT_RIGHT - Seat position is in the front row, right seat.
   * This is usually the driver's seat in left-hand traffic.
   */
  SEAT_FRONT_RIGHT = 3,
  /** SEAT_FRONT_MIDDLE - Seat position is in the front row, middle seat. */
  SEAT_FRONT_MIDDLE = 4,
  /** SEAT_BACK_LEFT - Seat position is in the back row, left seat. */
  SEAT_BACK_LEFT = 5,
  /** SEAT_BACK_RIGHT - Seat position is in the back row, right seat. */
  SEAT_BACK_RIGHT = 6,
  /** SEAT_BACK_MIDDLE - Seat position is in the back row, middle seat. */
  SEAT_BACK_MIDDLE = 7,
  /** SEAT_THIRD_ROW_LEFT - Seat position is in the third row, left seat. */
  SEAT_THIRD_ROW_LEFT = 8,
  /** SEAT_THIRD_ROW_RIGHT - Seat position is in the third row, right seat. */
  SEAT_THIRD_ROW_RIGHT = 9,
  /** SEAT_THIRD_ROW_MIDDLE - Seat position is in the third row, middle seat. */
  SEAT_THIRD_ROW_MIDDLE = 10,
}

export function occupant_Classification_SeatFromJSON(object: any): Occupant_Classification_Seat {
  switch (object) {
    case 0:
    case "SEAT_UNKNOWN":
      return Occupant_Classification_Seat.SEAT_UNKNOWN;
    case 1:
    case "SEAT_OTHER":
      return Occupant_Classification_Seat.SEAT_OTHER;
    case 2:
    case "SEAT_FRONT_LEFT":
      return Occupant_Classification_Seat.SEAT_FRONT_LEFT;
    case 3:
    case "SEAT_FRONT_RIGHT":
      return Occupant_Classification_Seat.SEAT_FRONT_RIGHT;
    case 4:
    case "SEAT_FRONT_MIDDLE":
      return Occupant_Classification_Seat.SEAT_FRONT_MIDDLE;
    case 5:
    case "SEAT_BACK_LEFT":
      return Occupant_Classification_Seat.SEAT_BACK_LEFT;
    case 6:
    case "SEAT_BACK_RIGHT":
      return Occupant_Classification_Seat.SEAT_BACK_RIGHT;
    case 7:
    case "SEAT_BACK_MIDDLE":
      return Occupant_Classification_Seat.SEAT_BACK_MIDDLE;
    case 8:
    case "SEAT_THIRD_ROW_LEFT":
      return Occupant_Classification_Seat.SEAT_THIRD_ROW_LEFT;
    case 9:
    case "SEAT_THIRD_ROW_RIGHT":
      return Occupant_Classification_Seat.SEAT_THIRD_ROW_RIGHT;
    case 10:
    case "SEAT_THIRD_ROW_MIDDLE":
      return Occupant_Classification_Seat.SEAT_THIRD_ROW_MIDDLE;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Occupant_Classification_Seat");
  }
}

export function occupant_Classification_SeatToJSON(object: Occupant_Classification_Seat): string {
  switch (object) {
    case Occupant_Classification_Seat.SEAT_UNKNOWN:
      return "SEAT_UNKNOWN";
    case Occupant_Classification_Seat.SEAT_OTHER:
      return "SEAT_OTHER";
    case Occupant_Classification_Seat.SEAT_FRONT_LEFT:
      return "SEAT_FRONT_LEFT";
    case Occupant_Classification_Seat.SEAT_FRONT_RIGHT:
      return "SEAT_FRONT_RIGHT";
    case Occupant_Classification_Seat.SEAT_FRONT_MIDDLE:
      return "SEAT_FRONT_MIDDLE";
    case Occupant_Classification_Seat.SEAT_BACK_LEFT:
      return "SEAT_BACK_LEFT";
    case Occupant_Classification_Seat.SEAT_BACK_RIGHT:
      return "SEAT_BACK_RIGHT";
    case Occupant_Classification_Seat.SEAT_BACK_MIDDLE:
      return "SEAT_BACK_MIDDLE";
    case Occupant_Classification_Seat.SEAT_THIRD_ROW_LEFT:
      return "SEAT_THIRD_ROW_LEFT";
    case Occupant_Classification_Seat.SEAT_THIRD_ROW_RIGHT:
      return "SEAT_THIRD_ROW_RIGHT";
    case Occupant_Classification_Seat.SEAT_THIRD_ROW_MIDDLE:
      return "SEAT_THIRD_ROW_MIDDLE";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Occupant_Classification_Seat");
  }
}

/** Definition of hands related to the steering wheel (mostly driver). */
export enum Occupant_Classification_SteeringControl {
  /** STEERING_CONTROL_UNKNOWN - Hands state is unknown (must not be used in ground truth). */
  STEERING_CONTROL_UNKNOWN = 0,
  /**
   * STEERING_CONTROL_OTHER - Other (unspecified but known) hand positioning related to the
   * steering wheel.
   */
  STEERING_CONTROL_OTHER = 1,
  /** STEERING_CONTROL_NO_HAND - Hands are not on the steering wheel. */
  STEERING_CONTROL_NO_HAND = 2,
  /**
   * STEERING_CONTROL_ONE_HAND - One hand is on the steering wheel. Whether it is the left or
   * right hand is unspecified or unknown.
   *
   * \note If there is no differentiation between one or both hands on
   * the steering wheel, this value should be used.
   */
  STEERING_CONTROL_ONE_HAND = 3,
  /** STEERING_CONTROL_BOTH_HANDS - Both hands are on the steering wheel. */
  STEERING_CONTROL_BOTH_HANDS = 4,
  /** STEERING_CONTROL_LEFT_HAND - Only left hand is on the steering wheel. */
  STEERING_CONTROL_LEFT_HAND = 5,
  /** STEERING_CONTROL_RIGHT_HAND - Only right hand is on the steering wheel. */
  STEERING_CONTROL_RIGHT_HAND = 6,
}

export function occupant_Classification_SteeringControlFromJSON(object: any): Occupant_Classification_SteeringControl {
  switch (object) {
    case 0:
    case "STEERING_CONTROL_UNKNOWN":
      return Occupant_Classification_SteeringControl.STEERING_CONTROL_UNKNOWN;
    case 1:
    case "STEERING_CONTROL_OTHER":
      return Occupant_Classification_SteeringControl.STEERING_CONTROL_OTHER;
    case 2:
    case "STEERING_CONTROL_NO_HAND":
      return Occupant_Classification_SteeringControl.STEERING_CONTROL_NO_HAND;
    case 3:
    case "STEERING_CONTROL_ONE_HAND":
      return Occupant_Classification_SteeringControl.STEERING_CONTROL_ONE_HAND;
    case 4:
    case "STEERING_CONTROL_BOTH_HANDS":
      return Occupant_Classification_SteeringControl.STEERING_CONTROL_BOTH_HANDS;
    case 5:
    case "STEERING_CONTROL_LEFT_HAND":
      return Occupant_Classification_SteeringControl.STEERING_CONTROL_LEFT_HAND;
    case 6:
    case "STEERING_CONTROL_RIGHT_HAND":
      return Occupant_Classification_SteeringControl.STEERING_CONTROL_RIGHT_HAND;
    default:
      throw new globalThis.Error(
        "Unrecognized enum value " + object + " for enum Occupant_Classification_SteeringControl",
      );
  }
}

export function occupant_Classification_SteeringControlToJSON(object: Occupant_Classification_SteeringControl): string {
  switch (object) {
    case Occupant_Classification_SteeringControl.STEERING_CONTROL_UNKNOWN:
      return "STEERING_CONTROL_UNKNOWN";
    case Occupant_Classification_SteeringControl.STEERING_CONTROL_OTHER:
      return "STEERING_CONTROL_OTHER";
    case Occupant_Classification_SteeringControl.STEERING_CONTROL_NO_HAND:
      return "STEERING_CONTROL_NO_HAND";
    case Occupant_Classification_SteeringControl.STEERING_CONTROL_ONE_HAND:
      return "STEERING_CONTROL_ONE_HAND";
    case Occupant_Classification_SteeringControl.STEERING_CONTROL_BOTH_HANDS:
      return "STEERING_CONTROL_BOTH_HANDS";
    case Occupant_Classification_SteeringControl.STEERING_CONTROL_LEFT_HAND:
      return "STEERING_CONTROL_LEFT_HAND";
    case Occupant_Classification_SteeringControl.STEERING_CONTROL_RIGHT_HAND:
      return "STEERING_CONTROL_RIGHT_HAND";
    default:
      throw new globalThis.Error(
        "Unrecognized enum value " + object + " for enum Occupant_Classification_SteeringControl",
      );
  }
}

function createBaseOccupant(): Occupant {
  return { id: undefined, classification: undefined, source_reference: [] };
}

export const Occupant: MessageFns<Occupant> = {
  encode(message: Occupant, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Identifier.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.classification !== undefined) {
      Occupant_Classification.encode(message.classification, writer.uint32(18).fork()).join();
    }
    for (const v of message.source_reference) {
      ExternalReference.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Occupant {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOccupant();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.classification = Occupant_Classification.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.source_reference.push(ExternalReference.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Occupant {
    return {
      id: isSet(object.id) ? Identifier.fromJSON(object.id) : undefined,
      classification: isSet(object.classification)
        ? Occupant_Classification.fromJSON(object.classification)
        : undefined,
      source_reference: globalThis.Array.isArray(object?.source_reference)
        ? object.source_reference.map((e: any) => ExternalReference.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Occupant): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Identifier.toJSON(message.id);
    }
    if (message.classification !== undefined) {
      obj.classification = Occupant_Classification.toJSON(message.classification);
    }
    if (message.source_reference?.length) {
      obj.source_reference = message.source_reference.map((e) => ExternalReference.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Occupant>, I>>(base?: I): Occupant {
    return Occupant.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Occupant>, I>>(object: I): Occupant {
    const message = createBaseOccupant();
    message.id = (object.id !== undefined && object.id !== null) ? Identifier.fromPartial(object.id) : undefined;
    message.classification = (object.classification !== undefined && object.classification !== null)
      ? Occupant_Classification.fromPartial(object.classification)
      : undefined;
    message.source_reference = object.source_reference?.map((e) => ExternalReference.fromPartial(e)) || [];
    return message;
  },
};

function createBaseOccupant_Classification(): Occupant_Classification {
  return { is_driver: false, seat: 0, steering_control: 0 };
}

export const Occupant_Classification: MessageFns<Occupant_Classification> = {
  encode(message: Occupant_Classification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.is_driver !== undefined && message.is_driver !== false) {
      writer.uint32(16).bool(message.is_driver);
    }
    if (message.seat !== undefined && message.seat !== 0) {
      writer.uint32(24).int32(message.seat);
    }
    if (message.steering_control !== undefined && message.steering_control !== 0) {
      writer.uint32(32).int32(message.steering_control);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Occupant_Classification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOccupant_Classification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.is_driver = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.seat = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.steering_control = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Occupant_Classification {
    return {
      is_driver: isSet(object.is_driver) ? globalThis.Boolean(object.is_driver) : false,
      seat: isSet(object.seat) ? occupant_Classification_SeatFromJSON(object.seat) : 0,
      steering_control: isSet(object.steering_control)
        ? occupant_Classification_SteeringControlFromJSON(object.steering_control)
        : 0,
    };
  },

  toJSON(message: Occupant_Classification): unknown {
    const obj: any = {};
    if (message.is_driver !== undefined) {
      obj.is_driver = message.is_driver;
    }
    if (message.seat !== undefined) {
      obj.seat = occupant_Classification_SeatToJSON(message.seat);
    }
    if (message.steering_control !== undefined) {
      obj.steering_control = occupant_Classification_SteeringControlToJSON(message.steering_control);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Occupant_Classification>, I>>(base?: I): Occupant_Classification {
    return Occupant_Classification.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Occupant_Classification>, I>>(object: I): Occupant_Classification {
    const message = createBaseOccupant_Classification();
    message.is_driver = object.is_driver ?? false;
    message.seat = object.seat ?? 0;
    message.steering_control = object.steering_control ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
