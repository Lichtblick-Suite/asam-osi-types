// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_route.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Identifier } from "./osi_common";

/**
 * \brief A route in the road network
 *
 * A route is an e.g. planned or suggested path for an agent to travel from one
 * location to another within the road network. It is composed of a list of route
 * segments, which form a continuous path through the road network and should be
 * traversed in the order they are listed.
 * The route allows the simulation environment to provide agents with high level
 * path information, similar to that of a map or a navigation system, without the
 * need for the agent model to perform complex path planning on its own. This
 * allows for an efficient control of the agent's general direction, while
 * simultaneously giving it enough freedom on how to traverse the path.
 *
 * ## Example
 *
 * The example below shows the \link Route route\endlink of a vehicle.
 *
 * \image html OSI_Planned_Route.png "Route" width=850px
 *
 * The route is composed of three \link RouteSegment route segments\endlink RS1-3,
 * each indicated by a yellow outline. Two of the route segments
 * (RS2 and RS3) only contain a single \link LogicalLaneSegment logical lane segment\endlink
 * (highlighted in blue), while RS1 is composed of three
 * logical lane segments (green, blue and red).
 */
export interface Route {
  /**
   * The unique id of the route.
   *
   * \note This field is mandatory.
   *
   * \note This id must be unique within all route messages exchanged with
   * one traffic participant.
   *
   * \rules
   * is_set
   * \endrules
   */
  route_id?:
    | Identifier
    | undefined;
  /**
   * Route segments that form the route of an agent.
   *
   * Consecutive segments should be connected without gaps, meaning that the
   * two of them should form a continuous area.
   */
  route_segment: Route_RouteSegment[];
}

/**
 * \brief A segment of a logical lane.
 *
 * \note The LogicalLaneSegment allows that start_s > end_s.
 * If start_s < end_s, then the traffic agent should traverse the
 * segment in the logical lane's reference line definition direction.
 * If end_s > start_s, then the traffic agent should traverse the
 * segment in the opposite of the logical lane's reference line
 * definition direction.
 */
export interface Route_LogicalLaneSegment {
  /**
   * The ID of the logical lane this segment belongs to.
   *
   * \rules
   * refers_to: LogicalLane
   * \endrules
   */
  logical_lane_id?:
    | Identifier
    | undefined;
  /** S position on the logical lane where the segment starts. */
  start_s?:
    | number
    | undefined;
  /** S position on the logical lane where the segment ends. */
  end_s?: number | undefined;
}

/**
 * \brief A segment of a route.
 *
 * A route segment describes a segment of a traffic agent's route through the
 * logical lanes of the road network.
 *
 * Each time there is a successor-predecessor relation between the logical
 * lanes along the route (i.e. a logical lane ends, and is continued by another
 * logical lane, e.g. at a junction border), a new RouteSegment starts. The
 * RouteSegment then lists the logical lane segments that can be used to
 * travel through this space of the road.
 *
 * Together, the listed logical lane segments should form a continuous area,
 * where the traffic agent can move freely. These will mostly be parallel
 * lanes, though lanes may overlap (e.g. if one lane splits into two on a
 * junction). In general, the logical lane segments in a RouteSegment will
 * have the same length, though there are exceptions (e.g. if a lane
 * widening occurs, the newly appearing lane will have a shorter length).
 *
 * Typically a route segment will be either
 * - a set of parallel lanes between two junctions, or
 * - parallel lanes on an intersection with the same driving direction
 *
 * ## Example
 *
 * Consider the \link RouteSegment route segment\endlink between two intersections,
 * shown in the image below.
 *
 * \image html OSI_Route_Segment.png "RouteSegment" width=850px
 *
 * In the example, a single route segment RS with three
 * \link LogicalLaneSegment logical lane segments\endlink LL1, LL2 and LL3 is
 * shown. The segments are indicated by the green, blue and red highlighted areas,
 * one for each underlying logical lane The starting
 * s-position of each segment is indicated by the yellow dotted line and the s- prefix
 * (note that the start of LL2 lies further to the left, outside of the image),
 * while the ending s-position of all segments is shown by the yellow dotted line e-RS.
 *
 * As it can be seen in the example, all logical lane segments are parallel,
 * but two of them are opening at a later position, so their starting
 * s-positions will be different.
 */
export interface Route_RouteSegment {
  /**
   * Logical lane segments that form a route segment.
   *
   * The logical lane segments of a route segment should be connected without
   * gaps, meaning that, together, the lane segments should form a continuous
   * area.
   */
  lane_segment: Route_LogicalLaneSegment[];
}

function createBaseRoute(): Route {
  return { route_id: undefined, route_segment: [] };
}

export const Route: MessageFns<Route> = {
  encode(message: Route, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.route_id !== undefined) {
      Identifier.encode(message.route_id, writer.uint32(10).fork()).join();
    }
    for (const v of message.route_segment) {
      Route_RouteSegment.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Route {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoute();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.route_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.route_segment.push(Route_RouteSegment.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Route {
    return {
      route_id: isSet(object.route_id) ? Identifier.fromJSON(object.route_id) : undefined,
      route_segment: globalThis.Array.isArray(object?.route_segment)
        ? object.route_segment.map((e: any) => Route_RouteSegment.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Route): unknown {
    const obj: any = {};
    if (message.route_id !== undefined) {
      obj.route_id = Identifier.toJSON(message.route_id);
    }
    if (message.route_segment?.length) {
      obj.route_segment = message.route_segment.map((e) => Route_RouteSegment.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Route>, I>>(base?: I): Route {
    return Route.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Route>, I>>(object: I): Route {
    const message = createBaseRoute();
    message.route_id = (object.route_id !== undefined && object.route_id !== null)
      ? Identifier.fromPartial(object.route_id)
      : undefined;
    message.route_segment = object.route_segment?.map((e) => Route_RouteSegment.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRoute_LogicalLaneSegment(): Route_LogicalLaneSegment {
  return { logical_lane_id: undefined, start_s: 0, end_s: 0 };
}

export const Route_LogicalLaneSegment: MessageFns<Route_LogicalLaneSegment> = {
  encode(message: Route_LogicalLaneSegment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.logical_lane_id !== undefined) {
      Identifier.encode(message.logical_lane_id, writer.uint32(10).fork()).join();
    }
    if (message.start_s !== undefined && message.start_s !== 0) {
      writer.uint32(17).double(message.start_s);
    }
    if (message.end_s !== undefined && message.end_s !== 0) {
      writer.uint32(25).double(message.end_s);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Route_LogicalLaneSegment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoute_LogicalLaneSegment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logical_lane_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.start_s = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.end_s = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Route_LogicalLaneSegment {
    return {
      logical_lane_id: isSet(object.logical_lane_id) ? Identifier.fromJSON(object.logical_lane_id) : undefined,
      start_s: isSet(object.start_s) ? globalThis.Number(object.start_s) : 0,
      end_s: isSet(object.end_s) ? globalThis.Number(object.end_s) : 0,
    };
  },

  toJSON(message: Route_LogicalLaneSegment): unknown {
    const obj: any = {};
    if (message.logical_lane_id !== undefined) {
      obj.logical_lane_id = Identifier.toJSON(message.logical_lane_id);
    }
    if (message.start_s !== undefined) {
      obj.start_s = message.start_s;
    }
    if (message.end_s !== undefined) {
      obj.end_s = message.end_s;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Route_LogicalLaneSegment>, I>>(base?: I): Route_LogicalLaneSegment {
    return Route_LogicalLaneSegment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Route_LogicalLaneSegment>, I>>(object: I): Route_LogicalLaneSegment {
    const message = createBaseRoute_LogicalLaneSegment();
    message.logical_lane_id = (object.logical_lane_id !== undefined && object.logical_lane_id !== null)
      ? Identifier.fromPartial(object.logical_lane_id)
      : undefined;
    message.start_s = object.start_s ?? 0;
    message.end_s = object.end_s ?? 0;
    return message;
  },
};

function createBaseRoute_RouteSegment(): Route_RouteSegment {
  return { lane_segment: [] };
}

export const Route_RouteSegment: MessageFns<Route_RouteSegment> = {
  encode(message: Route_RouteSegment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.lane_segment) {
      Route_LogicalLaneSegment.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Route_RouteSegment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoute_RouteSegment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lane_segment.push(Route_LogicalLaneSegment.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Route_RouteSegment {
    return {
      lane_segment: globalThis.Array.isArray(object?.lane_segment)
        ? object.lane_segment.map((e: any) => Route_LogicalLaneSegment.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Route_RouteSegment): unknown {
    const obj: any = {};
    if (message.lane_segment?.length) {
      obj.lane_segment = message.lane_segment.map((e) => Route_LogicalLaneSegment.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Route_RouteSegment>, I>>(base?: I): Route_RouteSegment {
    return Route_RouteSegment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Route_RouteSegment>, I>>(object: I): Route_RouteSegment {
    const message = createBaseRoute_RouteSegment();
    message.lane_segment = object.lane_segment?.map((e) => Route_LogicalLaneSegment.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
