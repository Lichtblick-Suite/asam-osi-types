// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_lane.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ColorDescription, ExternalReference, Identifier, Vector3d } from "./osi_common";

/**
 * \brief A lane in the road network.
 *
 * A lane is part of a road and mainly characterized by its center line. It also
 * knows about any adjacent lanes, antecessor and successor lanes.
 * The following images will be referred to by later comments. The lane
 * l4 in image HighwayExit is used  as reference if not mentioned otherwise.
 *
 * <table border=0>
 * <tr>
 * <td>
 * \image html OSI_Highway_Exit.svg "" width=550px
 * <td>
 * \image html OSI_LaneIntersection.svg "" width=550px
 * <tr>
 * <td>
 * \image html OSI_Highway_Exit.jpg "HighwayExit" width=650px
 * <td>
 * \image html OSI_LaneIntersection.jpg "Intersection" width=650px
 * </table>
 *
 * \note In the examples, the symbols l1, l2, ... and lb1, lb2, ...
 * stand for the lane ids and lane boundary ids respectively, i.e. for
 * integers (uint64). The symbols cl1, cl2, ... represent the
 * osi3::Lane::Classification::centerline elements of the lanes with
 * the respective ids.
 */
export interface Lane {
  /**
   * The ID of the lane.
   * Example: l4 (see reference picture HighwayExit).
   *
   * \note Note ID is global unique.
   *
   * \rules
   * is_globally_unique
   * is_set
   * \endrules
   */
  id?:
    | Identifier
    | undefined;
  /** The classification of the lane. */
  classification?:
    | Lane_Classification
    | undefined;
  /**
   * Optional external reference to the lane source.
   *
   * The external reference points to the source of the lane, if it is derived
   * from one or more objects or external references.
   *
   * For example, to reference a lane defined in an OpenDRIVE map
   * the items should be set as follows:
   * * reference = URI to map, can remain empty if identical with definition
   *               in \c GroundTruth::map_reference
   * * type = "net.asam.opendrive"
   * * identifier[0] = id of t_road
   * * identifier[1] = s of t_road_lanes_laneSection
   * * identifier[2] = id of t_road_lanes_laneSection_left_lane,
   *                         t_road_lanes_laneSection_right_lane
   *
   * \note For non-ASAM Standards, it is implementation-specific how
   *       source_reference is resolved.
   *
   * \note The value has to be repeated, because one lane segment may be
   *       derived from more than one origin segment. Multiple sources
   *       may be added as reference as well, for example, a map and sensors.
   */
  source_reference: ExternalReference[];
}

/**
 * \brief \c Classification of a lane.
 *
 * <table border = 0>
 * <tr>
 * <td>
 * \anchor HighwayExit
 * \image html OSI_LaneClassification.jpg "HighwayExit" width=800px
 * </td>
 * <td>
 * <table border=0>
 * <tr>
 * <td>
 * <b>
 * \c Classification for lane l4 in image HighwayExit:
 * </b>
 * <tr>
 * <td>
 * \c #type = \c #TYPE_DRIVING
 * <tr>
 * <td>
 * \c #centerline = (cl4_1, cl4_2, cl4_3, cl4_4, cl4_5)
 * <tr>
 * <td>
 * \c #centerline_is_driving_direction = \c true
 * <tr>
 * <td>
 * \c #is_host_vehicle_lane = \c true
 * <tr>
 * <td>
 * \c #left_adjacent_lane_id = l3
 * <tr>
 * <td>
 * \c #right_adjacent_lane_id  = (l5,l6)
 * <tr>
 * <td>
 * \c #left_lane_boundary_id = lb5
 * <tr>
 * <td>
 * \c #right_lane_boundary_id = (lb9, lb6)
 * </table>
 * </td>
 * </tr>
 * <tr>
 * <td>
 * \anchor Intersection
 * \image html OSI_LaneIntersection.jpg "Intersection" width=800px
 * </td>
 * <td>
 * <table border=0>
 * <tr>
 * <td>
 * <b>
 * \c Classification for lane l7 in image Intersection:
 * </b>
 * </td>
 * </tr>
 * <tr>
 * <td>
 * \c #type = \c #TYPE_INTERSECTION
 * </td>
 * </tr>
 * <tr>
 * <td>
 * \c #is_host_vehicle_lane = \c false
 * </td>
 * </tr>
 * <tr>
 * <td>
 * \c #free_lane_boundary_id = lb11
 * </td>
 * </tr>
 * <tr>
 * <td>
 * <border = 0>
 * <tr>
 * <td>
 * \c #lane_pairing = (
 * <td>
 * (l2,l1), (l2,l3), (l2,l5),
 * <tr>
 * <td>
 * <td>
 * (l4,l1), (l4,l3), (l4,l5),
 * <tr>
 * <td>
 * <td>
 * (l6,l1), (l6,l3), (l6,l5) )
 * </table>
 * </td>
 * </tr>
 * </table>
 * </td>
 * </tr>
 * </table>
 *
 * \note In the examples, the symbols l1, l2, ... and lb1, lb2, ...
 * stand for the lane ids and lane boundary ids respectively, i.e. for
 * integers (uint64). The symbols cl1, cl2, ... represent the
 * osi3::Lane::Classification::centerline elements of the lanes with
 * the respective ids. The symbols cl1_1, cl1_2, ... stand for
 * \c #osi3::Vector3d elements.
 */
export interface Lane_Classification {
  /**
   * The type of the lane.
   *
   * Example: For l4 in image \ref HighwayExit the \c #type is \c
   * #TYPE_DRIVING.
   */
  type?:
    | Lane_Classification_Type
    | undefined;
  /**
   * Indicates that the host vehicle travels on this particular lane.
   * The host vehicle may travel on more than one lane at once. This does
   * also apply for the \c CandidateLane in the \c DetectedLane .
   */
  is_host_vehicle_lane?:
    | boolean
    | undefined;
  /**
   * The lane's center line (as a list of segments).
   *
   * The centerline describes the middle of the lane.
   *
   * \image html OSI_LaneCenterline.svg "Centerline" width=500px
   *
   * Example: In image \ref HighwayExit,
   * the centerline of lane l4 (black line) is given by
   * (cl4_1, cl4_2, cl4_3, cl4_4, cl4_5).
   *
   * \image html OSI_LaneBoundaries_And_CenterLine.svg "Centerline" width=500px
   *
   * \note
   * cl: center line
   * lb: lane boundary
   *
   * \attention The points describing the center line must be set in the
   * same ordering (ascending or descending) as the points describing the
   * lane boundaries. Example: If the points are deducted from a map format,
   * the order of points is recommended to be in line with the road coordinate
   * (e.g. s-coordinate in OpenDRIVE).
   *
   * \attention The points describing the center line might be set at
   * arbitrary distances. When the points are pairwise linearly connected,
   * the lateral distance to the real ideal line (as used by the
   * simulation environment internally) must not exceed 5cm. As shown in
   * the following image:
   *
   * \image html line_approximation_error.svg "Approximation error"
   * Approximation error green line.
   *
   * \note The center line is the line that a typical vehicle follows more
   * or less (depending on the situation, a little more to the left or
   * right of the center line). The intended direction of travel on the
   * lane is given by the direction defined by the sequence of points
   * forming the \c #centerline w.r.t. \c
   * #centerline_is_driving_direction.
   *
   * \note The \c #centerline is defined only for \c #type =
   * \c #TYPE_DRIVING and if exactly one or no \c #lane_pairing pair
   * exists.
   *
   * \note Intersections and non-driving lanes do not have a center line.
   * A vehicle must calculate this individually and depending on the
   * situation.
   */
  centerline: Vector3d[];
  /**
   * Definition of the intended driving direction.
   *
   * Defined and used for driving lanes.
   * \c true means driving direction is according to ascending storage
   * order of center line points. \c false means driving direction is
   * according to descending storage order of center line points.
   *
   * Example: \c #centerline_is_driving_direction = \c true for lane l4
   * and \c #centerline_is_driving_direction = \c false for lane l2 in
   * image \ref HighwayExit .
   *
   * \note The \c #centerline_is_driving_direction is defined for \c #type
   * = \c #TYPE_DRIVING .
   */
  centerline_is_driving_direction?:
    | boolean
    | undefined;
  /**
   * List of IDs of all lane segments that are directly adjacent to the
   * lane on the left side (w.r.t. ascending order of centerline points
   * and lane boundary points). Note that lengths of lane segments are
   * not synchronized and therefore there are multiple adjacent segments
   * if there is a split/merge point in the adjacent lane.
   *
   * Example: The lane l3 is the only left adjacent lane for lane l4
   * in image \ref HighwayExit.
   *
   * \note The \c #left_adjacent_lane_id is undefined for \c #type =
   * \c #TYPE_INTERSECTION .
   *
   * \note OSI uses singular instead of plural for repeated field names.
   *
   * \rules
   * check_if this.type is_different_to 4 else do_check is_set
   * \endrules
   */
  left_adjacent_lane_id: Identifier[];
  /**
   * List of IDs of all lane segments that are directly adjacent to the
   * lane on the right side (w.r.t. ascending order of centerline points
   * and lane boundary points). Note that lengths of lane segments are
   * not synchronized and therefore there are multiple adjacent segments
   * if there is a split/merge point in the adjacent lane.
   *
   * Example: \c #right_adjacent_lane_id = (l5, l6)
   * for lane l4 in image \ref HighwayExit.
   *
   * \note The \c #right_adjacent_lane_id is undefined for \c #type =
   * \c #TYPE_INTERSECTION .
   *
   * \note OSI uses singular instead of plural for repeated field names.
   *
   * \rules
   * check_if this.type is_different_to 4 else do_check is_set
   * \endrules
   */
  right_adjacent_lane_id: Identifier[];
  /**
   * The antecessor/successor lane pairings of this lane. There can be
   * multiple pairings with the same antecessor and different successor
   * lanes and vice versa. The antecessor lanes end in the same point that
   * this lane starts from. The successor lanes start in the same point
   * that this lane ends in.
   *
   * Example: See image \ref Intersection.
   *
   * \note OSI uses singular instead of plural for repeated field names.
   */
  lane_pairing: Lane_Classification_LanePairing[];
  /**
   * The right adjacent lane boundaries \c #right_lane_boundary_id may
   * only be shared with/as the left adjacent lane boundaries \c
   * #left_lane_boundary_id of the nearest right adjacent lane \c
   * #right_adjacent_lane_id.
   *
   * Example: \c #right_lane_boundary_id =
   * (lb9, lb6) for reference lane l4 in image \ref HighwayExit.
   *
   * \note Empty for intersections.
   *
   * \note The \c #right_lane_boundary_id is undefined for \c #type =
   * \c #TYPE_INTERSECTION .
   *
   * \note OSI uses singular instead of plural for repeated field names.
   *
   * \note The boundary between adjacent lanes at different heights
   *       (e.g. a curb between a driving lane and a sidewalk)
   *       should not be shared, but modeled as two separate lane
   *       boundaries with individual ids. One for the upper, the
   *       other one for the lower lane.
   *
   * \rules
   * check_if this.type is_different_to 4 else do_check is_set
   * \endrules
   */
  right_lane_boundary_id: Identifier[];
  /**
   * The left adjacent lane boundaries \c #left_lane_boundary_id may only
   * be shared with/as the right adjacent lane boundaries \c
   * #right_lane_boundary_id of the nearest left adjacent lane \c
   * #left_adjacent_lane_id.
   *
   * Example: \c #left_lane_boundary_id = lb5 for lane l4 in image \ref
   * HighwayExit.
   *
   * \note Empty for intersections.
   *
   * \note The \c #left_lane_boundary_id is undefined for \c #type =
   * \c #TYPE_INTERSECTION .
   *
   * \note OSI uses singular instead of plural for repeated field names.
   *
   * \note The boundary between adjacent lanes at different heights
   *       (e.g. a curb between a driving lane and a sidewalk)
   *       should not be shared, but modeled as two separate lane
   *       boundaries with individual ids. One for the upper, the
   *       other one for the lower lane.
   *
   * \rules
   * check_if this.type is_different_to 4 else do_check is_set
   * \endrules
   */
  left_lane_boundary_id: Identifier[];
  /**
   * The free boundaries which have no/unknown assignment to left/right.
   *
   * Example: \c #free_lane_boundary_id = lb11 for lane l7 in image \ref
   * Intersection.
   *
   * \note \c Lane with \c #type = \c #TYPE_INTERSECTION use only free
   * lane boundaries.
   *
   * \note OSI uses singular instead of plural for repeated field names.
   *
   * \rules
   * check_if this.type is_different_to 4 else do_check is_set
   * \endrules
   */
  free_lane_boundary_id: Identifier[];
  /** The condition of the lane, e.g. influenced by weather. */
  road_condition?:
    | Lane_Classification_RoadCondition
    | undefined;
  /**
   * The subtype of the lane.
   *
   * This subtype specifies a lane more concretely.
   */
  subtype?: Lane_Classification_Subtype | undefined;
}

/** Definition of available lane types. */
export enum Lane_Classification_Type {
  /** TYPE_UNKNOWN - Lane of unknown type (must not be used in ground truth). */
  TYPE_UNKNOWN = 0,
  /** TYPE_OTHER - Any other type of lane. */
  TYPE_OTHER = 1,
  /**
   * TYPE_DRIVING - A normal lane.
   * Example: Lanes with IDs l1, l2, l3, l4 and l6 in image \ref
   * HighwayExit.
   */
  TYPE_DRIVING = 2,
  /**
   * TYPE_NONDRIVING - A road where driving is normally not permitted.
   * Example: Lane with ID l5 in image \ref HighwayExit.
   */
  TYPE_NONDRIVING = 3,
  /**
   * TYPE_INTERSECTION - An intersection as a lane.
   * Example: Lane with ID l7 in image \ref Intersection.
   *
   * \image html OSI_X-Junction.svg "" width=600px
   */
  TYPE_INTERSECTION = 4,
}

export function lane_Classification_TypeFromJSON(object: any): Lane_Classification_Type {
  switch (object) {
    case 0:
    case "TYPE_UNKNOWN":
      return Lane_Classification_Type.TYPE_UNKNOWN;
    case 1:
    case "TYPE_OTHER":
      return Lane_Classification_Type.TYPE_OTHER;
    case 2:
    case "TYPE_DRIVING":
      return Lane_Classification_Type.TYPE_DRIVING;
    case 3:
    case "TYPE_NONDRIVING":
      return Lane_Classification_Type.TYPE_NONDRIVING;
    case 4:
    case "TYPE_INTERSECTION":
      return Lane_Classification_Type.TYPE_INTERSECTION;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Lane_Classification_Type");
  }
}

export function lane_Classification_TypeToJSON(object: Lane_Classification_Type): string {
  switch (object) {
    case Lane_Classification_Type.TYPE_UNKNOWN:
      return "TYPE_UNKNOWN";
    case Lane_Classification_Type.TYPE_OTHER:
      return "TYPE_OTHER";
    case Lane_Classification_Type.TYPE_DRIVING:
      return "TYPE_DRIVING";
    case Lane_Classification_Type.TYPE_NONDRIVING:
      return "TYPE_NONDRIVING";
    case Lane_Classification_Type.TYPE_INTERSECTION:
      return "TYPE_INTERSECTION";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Lane_Classification_Type");
  }
}

/** Definition of available lane subtypes, aligned with OpenDRIVE. */
export enum Lane_Classification_Subtype {
  /** SUBTYPE_UNKNOWN - Lane of unknown subtype. Do not use in ground truth. */
  SUBTYPE_UNKNOWN = 0,
  /** SUBTYPE_OTHER - Any other subtype of lane. */
  SUBTYPE_OTHER = 1,
  /**
   * SUBTYPE_NORMAL - A normal driving lane.
   * Example: Lanes with IDs l1, l2, l3 and l4 in image \ref
   * HighwayExit.
   *
   * Since it is intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_DRIVING.
   */
  SUBTYPE_NORMAL = 2,
  /**
   * SUBTYPE_BIKING - A lane that is designated for bicycles.
   *
   * Since it is not intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_NONDRIVING.
   */
  SUBTYPE_BIKING = 3,
  /**
   * SUBTYPE_SIDEWALK - A lane that is designated for pedestrians (sidewalk).
   *
   * Since it is not intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_NONDRIVING.
   */
  SUBTYPE_SIDEWALK = 4,
  /**
   * SUBTYPE_PARKING - A lane with parking spaces.
   *
   * Since it is not intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_NONDRIVING.
   */
  SUBTYPE_PARKING = 5,
  /**
   * SUBTYPE_STOP - A hard shoulder on motorways for emergency stops.
   * Example: Lane l5 in image \ref
   * HighwayExit.
   *
   * Since it is not intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_NONDRIVING.
   */
  SUBTYPE_STOP = 6,
  /**
   * SUBTYPE_RESTRICTED - A lane on which cars should not drive.
   *
   * Since it is not intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_NONDRIVING.
   */
  SUBTYPE_RESTRICTED = 7,
  /**
   * SUBTYPE_BORDER - A hard border on the edge of a road.
   *
   * Since it is not intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_NONDRIVING.
   */
  SUBTYPE_BORDER = 8,
  /**
   * SUBTYPE_SHOULDER - A soft border on the edge of a road.
   *
   * Since it is not intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_NONDRIVING.
   */
  SUBTYPE_SHOULDER = 9,
  /**
   * SUBTYPE_EXIT - A deceleration lane in parallel to the main road.
   * Example: Lane l6 in image \ref
   * HighwayExit.
   *
   * Since it is intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_DRIVING.
   */
  SUBTYPE_EXIT = 10,
  /**
   * SUBTYPE_ENTRY - An acceleration lane in parallel to the main road.
   *
   * Since it is intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_DRIVING.
   */
  SUBTYPE_ENTRY = 11,
  /**
   * SUBTYPE_ONRAMP - A ramp from rural or urban roads joining a motorway.
   *
   * Since it is intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_DRIVING.
   */
  SUBTYPE_ONRAMP = 12,
  /**
   * SUBTYPE_OFFRAMP - A ramp leading off a motorway onto rural or urban roads.
   *
   * Since it is intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_DRIVING.
   */
  SUBTYPE_OFFRAMP = 13,
  /**
   * SUBTYPE_CONNECTINGRAMP - A ramp that connect two motorways.
   *
   * Since it is intended to be used for normal automotive
   * driving, it should be used in combination with TYPE_DRIVING.
   */
  SUBTYPE_CONNECTINGRAMP = 14,
}

export function lane_Classification_SubtypeFromJSON(object: any): Lane_Classification_Subtype {
  switch (object) {
    case 0:
    case "SUBTYPE_UNKNOWN":
      return Lane_Classification_Subtype.SUBTYPE_UNKNOWN;
    case 1:
    case "SUBTYPE_OTHER":
      return Lane_Classification_Subtype.SUBTYPE_OTHER;
    case 2:
    case "SUBTYPE_NORMAL":
      return Lane_Classification_Subtype.SUBTYPE_NORMAL;
    case 3:
    case "SUBTYPE_BIKING":
      return Lane_Classification_Subtype.SUBTYPE_BIKING;
    case 4:
    case "SUBTYPE_SIDEWALK":
      return Lane_Classification_Subtype.SUBTYPE_SIDEWALK;
    case 5:
    case "SUBTYPE_PARKING":
      return Lane_Classification_Subtype.SUBTYPE_PARKING;
    case 6:
    case "SUBTYPE_STOP":
      return Lane_Classification_Subtype.SUBTYPE_STOP;
    case 7:
    case "SUBTYPE_RESTRICTED":
      return Lane_Classification_Subtype.SUBTYPE_RESTRICTED;
    case 8:
    case "SUBTYPE_BORDER":
      return Lane_Classification_Subtype.SUBTYPE_BORDER;
    case 9:
    case "SUBTYPE_SHOULDER":
      return Lane_Classification_Subtype.SUBTYPE_SHOULDER;
    case 10:
    case "SUBTYPE_EXIT":
      return Lane_Classification_Subtype.SUBTYPE_EXIT;
    case 11:
    case "SUBTYPE_ENTRY":
      return Lane_Classification_Subtype.SUBTYPE_ENTRY;
    case 12:
    case "SUBTYPE_ONRAMP":
      return Lane_Classification_Subtype.SUBTYPE_ONRAMP;
    case 13:
    case "SUBTYPE_OFFRAMP":
      return Lane_Classification_Subtype.SUBTYPE_OFFRAMP;
    case 14:
    case "SUBTYPE_CONNECTINGRAMP":
      return Lane_Classification_Subtype.SUBTYPE_CONNECTINGRAMP;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Lane_Classification_Subtype");
  }
}

export function lane_Classification_SubtypeToJSON(object: Lane_Classification_Subtype): string {
  switch (object) {
    case Lane_Classification_Subtype.SUBTYPE_UNKNOWN:
      return "SUBTYPE_UNKNOWN";
    case Lane_Classification_Subtype.SUBTYPE_OTHER:
      return "SUBTYPE_OTHER";
    case Lane_Classification_Subtype.SUBTYPE_NORMAL:
      return "SUBTYPE_NORMAL";
    case Lane_Classification_Subtype.SUBTYPE_BIKING:
      return "SUBTYPE_BIKING";
    case Lane_Classification_Subtype.SUBTYPE_SIDEWALK:
      return "SUBTYPE_SIDEWALK";
    case Lane_Classification_Subtype.SUBTYPE_PARKING:
      return "SUBTYPE_PARKING";
    case Lane_Classification_Subtype.SUBTYPE_STOP:
      return "SUBTYPE_STOP";
    case Lane_Classification_Subtype.SUBTYPE_RESTRICTED:
      return "SUBTYPE_RESTRICTED";
    case Lane_Classification_Subtype.SUBTYPE_BORDER:
      return "SUBTYPE_BORDER";
    case Lane_Classification_Subtype.SUBTYPE_SHOULDER:
      return "SUBTYPE_SHOULDER";
    case Lane_Classification_Subtype.SUBTYPE_EXIT:
      return "SUBTYPE_EXIT";
    case Lane_Classification_Subtype.SUBTYPE_ENTRY:
      return "SUBTYPE_ENTRY";
    case Lane_Classification_Subtype.SUBTYPE_ONRAMP:
      return "SUBTYPE_ONRAMP";
    case Lane_Classification_Subtype.SUBTYPE_OFFRAMP:
      return "SUBTYPE_OFFRAMP";
    case Lane_Classification_Subtype.SUBTYPE_CONNECTINGRAMP:
      return "SUBTYPE_CONNECTINGRAMP";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Lane_Classification_Subtype");
  }
}

/** \brief The condition of the road surface. */
export interface Lane_Classification_RoadCondition {
  /**
   * The temperature of the roads surface in Kelvin.
   *
   * Unit: K
   *
   * \rules
   * is_greater_than_or_equal_to: 0
   * \endrules
   */
  surface_temperature?:
    | number
    | undefined;
  /**
   * The height of the water film on top of the surface in mm.
   *
   * Unit: mm
   *
   * \rules
   * is_greater_than_or_equal_to: 0
   * \endrules
   */
  surface_water_film?:
    | number
    | undefined;
  /**
   * The temperature where the water on top of the surface would start
   * to freeze or dew in Kelvin.
   *
   * Unit: K
   *
   * \rules
   * is_greater_than_or_equal_to: 0
   * \endrules
   */
  surface_freezing_point?:
    | number
    | undefined;
  /**
   * The percentage of ice covering the road.
   *
   * Unit: %
   *
   * \rules
   * is_greater_than_or_equal_to: 0
   * \endrules
   */
  surface_ice?:
    | number
    | undefined;
  /**
   * The coefficient representing the roughness or unevenness of the
   * road. International Roughness Index (IRI) [1] values range from 0
   * = smooth ground (equivalent to driving on a plate of glass) up to
   * >
   * 20 mm/m (a very rough road).
   *
   * Estimated value ranges (IRI):
   * 0.0 mm/m absolutely perfect evenness
   * 0.3 -  1.8 mm/m airport runways and superhighways
   * 1.4 -  3.4 mm/m new pavements
   * 2.2 -  5.7 mm/m older pavements
   * 3.2 -  9.8 mm/m maintained unpaved roads
   * 4.0 - 11.0 mm/m damaged pavements
   * 8.0 - >
   * 20 mm/m rough unpaved roads
   *
   * Speed of normal use (IRI):
   * 30 km/h - 20   mm/m
   * 50 km/h - 14.5 mm/m
   * 60 km/h - 10.0 mm/m
   * 80 km/h -  8.5 mm/m
   * 100 km/h -  3.4 mm/m
   *
   * Road conditions (IRI);
   * 15 mm/m erosion gulleys and deep depressions
   * 11 mm/m frequent shallow depressions, some deep
   * 9 mm/m frequent minor depressions
   * 5 mm/m surface imperfections
   *
   * Unit: mm/m
   *
   * \par Reference:
   * [1] Sayers, M. W. & Karamihas, S. M. (1998). <em>Little Book of Profiling</em>. University of Michigan Transportation Research Institute. Retrieved January 25, 2020, from http://www.umtri.umich.edu/content/LittleBook98R.pdf pp. 45 ff.
   *
   * \rules
   * is_greater_than_or_equal_to: 0
   * \endrules
   */
  surface_roughness?:
    | number
    | undefined;
  /**
   * The surface texture or fine roughness
   *
   * Whereas the IRI-based roughness or unevenness measure only takes
   * into account road wavelengths around 0.5 m - 100 m, the surface
   * texture or fine roughness [1] measures only wavelengths below
   * 0.5 m. It is given as the standard height deviation of fine
   * roughness
   *
   * Unit: m
   *
   * \par Reference:
   * [1] Schneider, R. (1998). <em>Modellierung der Wellenausbreitung fuer ein bildgebendes Kfz-Radar</em>. PhD thesis. Karlsruhe, Germany. Universitaet Karlsruhe, Fak. f. Elektrotechnik.
   */
  surface_texture?: number | undefined;
}

/**
 * \brief The lane ID pairings of antecessor and successor lanes.
 *
 * <table border = 0>
 * <tr>
 * <td>
 * \image html OSI_LaneIntersection.svg "" width=600px
 * </td>
 * <td>
 * <table border=0>
 * <tr>
 * <td>
 * <b>
 * \c LanePairing for lane l7:
 * </b>
 * </td>
 * </tr>
 * <tr>
 * <td>
 * <table border = 0>
 * <tr>
 * <td>
 * (l2,l1), (l2,l3), (l2,l5),
 * <tr>
 * <td>
 * (l4,l1), (l4,l3), (l4,l5),
 * <tr>
 * <td>
 * (l6,l1), (l6,l3), (l6,l5)
 * </table>
 * </td>
 * </tr>
 * </table>
 * </td>
 * </tr>
 * </table>
 *
 * \note In the example, the symbols l1, l2, ... stand for the
 * respective lane ids, i.e. for integers (uint64). The symbols cl1,
 * cl2, ... represent the osi3::Lane::Classification::centerline
 * elements of the lanes with the respective ids.
 */
export interface Lane_Classification_LanePairing {
  /**
   * The antecessor lane ID.
   *
   * \rules
   * refers_to: Lane
   * \endrules
   */
  antecessor_lane_id?:
    | Identifier
    | undefined;
  /**
   * The successor lane ID.
   *
   * \rules
   * refers_to: Lane
   * \endrules
   */
  successor_lane_id?: Identifier | undefined;
}

/**
 * \brief A lane boundary defining the border of a lane.
 *
 * The left and right lane boundary define the width of the lane. Additionally,
 * free markings can be defined, e.g. at construction sites. Free markings
 * across multiple lanes may be defined multiple times for all affected lanes.
 *
 * \image html OSI_LaneBoundary.svg "LaneBoundary" width=800px
 *
 * \note In the example, the symbols l1, l2, ... and lb1, lb2, ...
 * stand for the lane ids and lane boundary ids respectively, i.e. for
 * integers (uint64). The symbols bp2_{i+1}, bp2_{i}, bp2_{i-1} stand
 * for \c #osi3::LaneBoundary::BoundaryPoint elements.
 */
export interface LaneBoundary {
  /**
   * The ID of the lane boundary.
   *
   * \rules
   * is_globally_unique
   * \endrules
   */
  id?:
    | Identifier
    | undefined;
  /**
   * \note For dashed lines, one \c BoundaryPoint has to be at the start and
   * another at the end of each dashed line segment. The first
   * \c BoundaryPoint defines the beginning of the first dashed lane marking.
   * The last \c BoundaryPoint defines the end of the last dashed lane
   * marking.
   * \note For Botts' dots lines, one \c BoundaryPoint position has to define
   * each Botts' dot.
   *
   * \attention For \c BoundaryPoint the same rule for the approximation
   * error applies as for \c Lane::Classification::centerline.
   *
   * \rules
   * first_element width is_equal_to 0.13
   * first_element height is_equal_to 0.14
   * last_element width is_equal_to 0.13
   * last_element height is_equal_to 0.13
   * \endrules
   */
  boundary_line: LaneBoundary_BoundaryPoint[];
  /** The classification of the lane boundary. */
  classification?:
    | LaneBoundary_Classification
    | undefined;
  /**
   * Optional external reference to the lane boundary source.
   *
   * \note For OpenDRIVE, there is no direct possibility to reference the
   *       RoadMark, as there is no unique identifier in this sub-object.
   *
   * \note For non-ASAM Standards, it is implementation-specific how
   *       source_reference is resolved.
   *
   * \note The value has to be repeated because one object may be derived
   *       from more than one origin source, for example, from a scenario file
   *       and from sensors.
   */
  source_reference: ExternalReference[];
  /**
   * The visual color of the material of the lane boundary.
   *
   * \note This does not represent the semantic classification but the visual
   * appearance. For semantic classification of the lane boundary use the color
   * field in \c Classification.
   */
  color_description?: ColorDescription | undefined;
}

/**
 * \brief A single point of a lane boundary.
 *
 * \image html OSI_LaneBoundaries_And_CenterLine.svg "" width=800px
 *
 * \note
 * cl: center line
 * lb: lane boundary
 *
 * \image html OSI_LaneBoundary.svg "" width=800px
 *
 * The lane boundary point bp2_{i} is one of the points of lane boundary
 * lb2.
 *
 * \note In the example, the symbols l1, l2, ... and lb1, lb2, ...
 * stand for the lane ids and lane boundary ids respectively, i.e. for
 * integers (uint64). The symbols bp2_{i+1}, bp2_{i}, bp2_{i-1} stand
 * for \c #osi3::LaneBoundary::BoundaryPoint elements.
 */
export interface LaneBoundary_BoundaryPoint {
  /** The position of the \c BoundaryPoint. */
  position?:
    | Vector3d
    | undefined;
  /**
   * The overall width of the lane boundary at the position of the
   * \c BoundaryPoint .
   * Used for lines forming lane markings.
   *
   * \image html OSI_LaneBoundaryWidth.svg "" width=600px
   *
   * \note Field need not be set if it is defined previous.
   * See \c LaneBoundary.
   */
  width?:
    | number
    | undefined;
  /**
   * The overall height of the lane boundary at the position of the
   * \c BoundaryPoint .
   * Used for guard rails, curbstone, or similar.
   *
   * \image html OSI_LaneBoundaryHeight.svg "" width=600px
   *
   * \note Field need not be set if it is previously defined.
   *
   * \note The boundary point height should not be used to model the boundary
   *       between two adjacent lanes at different heights as a single, shared
   *       boundary.
   *       Boundaries between adjacent lanes at different heights should be
   *       modeled as two separate lane boundaries.
   *
   * See \c LaneBoundary .
   */
  height?:
    | number
    | undefined;
  /**
   * Alternation of dashes in case of a dashed lane boundary. In
   * context, this field gives information about the location of
   * dashes on the boundary line.
   */
  dash?: LaneBoundary_BoundaryPoint_Dash | undefined;
}

/**
 * This enum describes the alternation of dashes in case of a
 * dashed lane boundary.
 *
 * \note The enum descriptions adhere to the definition direction
 * of the lane boundary points. This means that start or end of a
 * dash are understood with respect to the direction in which the
 * points of the boundary line are defined.
 */
export enum LaneBoundary_BoundaryPoint_Dash {
  /**
   * DASH_UNKNOWN - The current state of the dash alternation is not known (must
   * not be used in ground truth).
   */
  DASH_UNKNOWN = 0,
  /** DASH_OTHER - Other (unspecified but known) type of dash alternation state. */
  DASH_OTHER = 1,
  /** DASH_START - The current \c BoundaryPoint indicates the start of a dash. */
  DASH_START = 2,
  /**
   * DASH_CONTINUE - The current \c BoundaryPoint is located on a dash of a dashed
   * line. This enables a dash to continue across multiple points.
   */
  DASH_CONTINUE = 3,
  /** DASH_END - The current \c BoundaryPoint indicates the end of a dash. */
  DASH_END = 4,
  /**
   * DASH_GAP - The current \c BoundaryPoint is located in the gap between
   * two dashes. When used to describe a first/last point of a lane
   * boundary, it indicates that the lane boundary starts/ends in
   * a gap.
   */
  DASH_GAP = 5,
}

export function laneBoundary_BoundaryPoint_DashFromJSON(object: any): LaneBoundary_BoundaryPoint_Dash {
  switch (object) {
    case 0:
    case "DASH_UNKNOWN":
      return LaneBoundary_BoundaryPoint_Dash.DASH_UNKNOWN;
    case 1:
    case "DASH_OTHER":
      return LaneBoundary_BoundaryPoint_Dash.DASH_OTHER;
    case 2:
    case "DASH_START":
      return LaneBoundary_BoundaryPoint_Dash.DASH_START;
    case 3:
    case "DASH_CONTINUE":
      return LaneBoundary_BoundaryPoint_Dash.DASH_CONTINUE;
    case 4:
    case "DASH_END":
      return LaneBoundary_BoundaryPoint_Dash.DASH_END;
    case 5:
    case "DASH_GAP":
      return LaneBoundary_BoundaryPoint_Dash.DASH_GAP;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum LaneBoundary_BoundaryPoint_Dash");
  }
}

export function laneBoundary_BoundaryPoint_DashToJSON(object: LaneBoundary_BoundaryPoint_Dash): string {
  switch (object) {
    case LaneBoundary_BoundaryPoint_Dash.DASH_UNKNOWN:
      return "DASH_UNKNOWN";
    case LaneBoundary_BoundaryPoint_Dash.DASH_OTHER:
      return "DASH_OTHER";
    case LaneBoundary_BoundaryPoint_Dash.DASH_START:
      return "DASH_START";
    case LaneBoundary_BoundaryPoint_Dash.DASH_CONTINUE:
      return "DASH_CONTINUE";
    case LaneBoundary_BoundaryPoint_Dash.DASH_END:
      return "DASH_END";
    case LaneBoundary_BoundaryPoint_Dash.DASH_GAP:
      return "DASH_GAP";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum LaneBoundary_BoundaryPoint_Dash");
  }
}

/**
 * \brief \c Classification of a lane boundary.
 *
 * <table border = 0>
 * <tr>
 * <td>
 * <table border=0>
 * <tr>
 * <td>
 * <b>
 * Image Intersection
 * </b>
 * <tr>
 * <td>
 * <tr>
 * <td>
 * \c Classification for lane boundaries lb2 and lb9:
 * </td>
 * </tr>
 * <tr>
 * <td>
 * <table border = 0>
 * <tr>
 * <td>
 * \c #type = #TYPE_DASHED_LINE
 * <tr>
 * <td>
 * \c #color = #COLOR_WHITE
 * <tr>
 * </table>
 * </td>
 * </tr>
 * <tr>
 * <td>
 * \c Classification for lane boundaries lb1, lb3, lb4, lb7, lb8, lb10,
 * lb11, lb12 and lb14:
 * </td>
 * </tr>
 * <tr>
 * <td>
 * <table border = 0>
 * <tr>
 * <td>
 * \c #type = #TYPE_SOLID_LINE
 * <tr>
 * <td>
 * \c #color = #COLOR_WHITE
 * <tr>
 * </table>
 * </td>
 * </tr>
 * <tr>
 * <td>
 * \c Classification for lane boundaries lb5, lb6 and lb13:
 * </td>
 * </tr>
 * <tr>
 * <td>
 * <table border = 0>
 * <tr>
 * <td>
 * \c #type = #TYPE_CURB
 * </table>
 * </td>
 * </tr>
 * </table>
 * </td>
 * <td>
 * <table border=0>
 * <tr>
 * <td>
 * \image html OSI_LaneIntersection.svg "" width=500px
 * </td>
 * <tr>
 * <td>
 * \image html OSI_LaneBoundaryClassification.jpg "" width=600px
 * </td>
 * </table>
 * </td>
 * </tr>
 * </table>
 *
 * \note In the example, the symbols l1, l2, ... and lb1, lb2, ...
 * stand for the lane ids and lane boundary ids respectively, i.e. for
 * integers (uint64). The symbols cl1, cl2, ... represent the
 * osi3::Lane::Classification::centerline elements of the lanes with
 * the respective ids.
 */
export interface LaneBoundary_Classification {
  /** The type of the lane boundary. */
  type?:
    | LaneBoundary_Classification_Type
    | undefined;
  /**
   * The semantic color of the lane boundary in case of lane markings.
   *
   * \note The color types represent the semantic classification of
   * lane markings only. They do not represent an actual visual appearance.
   */
  color?:
    | LaneBoundary_Classification_Color
    | undefined;
  /**
   * The ids of \c StationaryObject which limit the corresponding lane.
   * This field must be set if the \c #type is set to
   * \c #TYPE_STRUCTURE
   *
   * \rules
   * refers_to: StationaryObject
   * \endrules
   */
  limiting_structure_id: Identifier[];
}

/**
 * The lane boundary type.
 * There is no special representation for double lines, e.g. solid /
 * solid or dashed / solid. In such cases, each lane will define its own
 * side of the lane boundary.
 */
export enum LaneBoundary_Classification_Type {
  /**
   * TYPE_UNKNOWN - The type of lane boundary is unknown. Value must not be used in
   * ground truth.
   */
  TYPE_UNKNOWN = 0,
  /**
   * TYPE_OTHER - Unspecified but known type of lane boundary.
   * Consider proposing an additional type if using TYPE_OTHER.
   */
  TYPE_OTHER = 1,
  /** TYPE_NO_LINE - An invisible lane boundary. */
  TYPE_NO_LINE = 2,
  /** TYPE_SOLID_LINE - A solid line at the lane boundary. */
  TYPE_SOLID_LINE = 3,
  /** TYPE_DASHED_LINE - A dashed line at the lane boundary. */
  TYPE_DASHED_LINE = 4,
  /** TYPE_BOTTS_DOTS - A lane boundary consisting of Botts' dots (multiple Botts dots). */
  TYPE_BOTTS_DOTS = 5,
  /**
   * TYPE_ROAD_EDGE - A lane boundary formed by the road's edge.
   * The road edge is the end of the (paved) road surface.
   */
  TYPE_ROAD_EDGE = 6,
  /**
   * TYPE_SNOW_EDGE - A lane boundary formed by a snow edge that may be on the road
   * surface.
   */
  TYPE_SNOW_EDGE = 7,
  /** TYPE_GRASS_EDGE - A lane boundary covered by grass. */
  TYPE_GRASS_EDGE = 8,
  /** TYPE_GRAVEL_EDGE - A lane boundary covered by gravel. */
  TYPE_GRAVEL_EDGE = 9,
  /** TYPE_SOIL_EDGE - A lane boundary covered by soil. */
  TYPE_SOIL_EDGE = 10,
  /** TYPE_GUARD_RAIL - A guard rail. */
  TYPE_GUARD_RAIL = 11,
  /** TYPE_CURB - A curb. */
  TYPE_CURB = 12,
  /** TYPE_STRUCTURE - A structure (e.g. building or tunnel wall). */
  TYPE_STRUCTURE = 13,
  /** TYPE_BARRIER - A barrier to guide vehicles and to prevent them from entering other lanes (e.g. a concrete barrier on a highway). */
  TYPE_BARRIER = 14,
  /** TYPE_SOUND_BARRIER - A sound barrier. */
  TYPE_SOUND_BARRIER = 15,
}

export function laneBoundary_Classification_TypeFromJSON(object: any): LaneBoundary_Classification_Type {
  switch (object) {
    case 0:
    case "TYPE_UNKNOWN":
      return LaneBoundary_Classification_Type.TYPE_UNKNOWN;
    case 1:
    case "TYPE_OTHER":
      return LaneBoundary_Classification_Type.TYPE_OTHER;
    case 2:
    case "TYPE_NO_LINE":
      return LaneBoundary_Classification_Type.TYPE_NO_LINE;
    case 3:
    case "TYPE_SOLID_LINE":
      return LaneBoundary_Classification_Type.TYPE_SOLID_LINE;
    case 4:
    case "TYPE_DASHED_LINE":
      return LaneBoundary_Classification_Type.TYPE_DASHED_LINE;
    case 5:
    case "TYPE_BOTTS_DOTS":
      return LaneBoundary_Classification_Type.TYPE_BOTTS_DOTS;
    case 6:
    case "TYPE_ROAD_EDGE":
      return LaneBoundary_Classification_Type.TYPE_ROAD_EDGE;
    case 7:
    case "TYPE_SNOW_EDGE":
      return LaneBoundary_Classification_Type.TYPE_SNOW_EDGE;
    case 8:
    case "TYPE_GRASS_EDGE":
      return LaneBoundary_Classification_Type.TYPE_GRASS_EDGE;
    case 9:
    case "TYPE_GRAVEL_EDGE":
      return LaneBoundary_Classification_Type.TYPE_GRAVEL_EDGE;
    case 10:
    case "TYPE_SOIL_EDGE":
      return LaneBoundary_Classification_Type.TYPE_SOIL_EDGE;
    case 11:
    case "TYPE_GUARD_RAIL":
      return LaneBoundary_Classification_Type.TYPE_GUARD_RAIL;
    case 12:
    case "TYPE_CURB":
      return LaneBoundary_Classification_Type.TYPE_CURB;
    case 13:
    case "TYPE_STRUCTURE":
      return LaneBoundary_Classification_Type.TYPE_STRUCTURE;
    case 14:
    case "TYPE_BARRIER":
      return LaneBoundary_Classification_Type.TYPE_BARRIER;
    case 15:
    case "TYPE_SOUND_BARRIER":
      return LaneBoundary_Classification_Type.TYPE_SOUND_BARRIER;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum LaneBoundary_Classification_Type");
  }
}

export function laneBoundary_Classification_TypeToJSON(object: LaneBoundary_Classification_Type): string {
  switch (object) {
    case LaneBoundary_Classification_Type.TYPE_UNKNOWN:
      return "TYPE_UNKNOWN";
    case LaneBoundary_Classification_Type.TYPE_OTHER:
      return "TYPE_OTHER";
    case LaneBoundary_Classification_Type.TYPE_NO_LINE:
      return "TYPE_NO_LINE";
    case LaneBoundary_Classification_Type.TYPE_SOLID_LINE:
      return "TYPE_SOLID_LINE";
    case LaneBoundary_Classification_Type.TYPE_DASHED_LINE:
      return "TYPE_DASHED_LINE";
    case LaneBoundary_Classification_Type.TYPE_BOTTS_DOTS:
      return "TYPE_BOTTS_DOTS";
    case LaneBoundary_Classification_Type.TYPE_ROAD_EDGE:
      return "TYPE_ROAD_EDGE";
    case LaneBoundary_Classification_Type.TYPE_SNOW_EDGE:
      return "TYPE_SNOW_EDGE";
    case LaneBoundary_Classification_Type.TYPE_GRASS_EDGE:
      return "TYPE_GRASS_EDGE";
    case LaneBoundary_Classification_Type.TYPE_GRAVEL_EDGE:
      return "TYPE_GRAVEL_EDGE";
    case LaneBoundary_Classification_Type.TYPE_SOIL_EDGE:
      return "TYPE_SOIL_EDGE";
    case LaneBoundary_Classification_Type.TYPE_GUARD_RAIL:
      return "TYPE_GUARD_RAIL";
    case LaneBoundary_Classification_Type.TYPE_CURB:
      return "TYPE_CURB";
    case LaneBoundary_Classification_Type.TYPE_STRUCTURE:
      return "TYPE_STRUCTURE";
    case LaneBoundary_Classification_Type.TYPE_BARRIER:
      return "TYPE_BARRIER";
    case LaneBoundary_Classification_Type.TYPE_SOUND_BARRIER:
      return "TYPE_SOUND_BARRIER";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum LaneBoundary_Classification_Type");
  }
}

/**
 * The semantic color of the lane boundary in case of a lane markings.
 * Lane markings that alternate in color must be represented by
 * individual \c LaneBoundary segments.
 *
 * \note The color types represent the semantic color classification of
 * lane markings only. They do not represent an actual visual appearance.
 */
export enum LaneBoundary_Classification_Color {
  /**
   * COLOR_UNKNOWN - Color of marking is unknown. Value must not be used in ground
   * truth.
   */
  COLOR_UNKNOWN = 0,
  /** COLOR_OTHER - Other (unspecified but known) color. */
  COLOR_OTHER = 1,
  /**
   * COLOR_NONE - Marking without color. Used to represent logical boundaries
   * without actual physical markings at the respective position.
   * Value may be used in ground truth only.
   */
  COLOR_NONE = 2,
  /** COLOR_WHITE - Marking with white color. */
  COLOR_WHITE = 3,
  /** COLOR_YELLOW - Marking with yellow / orange-yellow color. */
  COLOR_YELLOW = 4,
  /** COLOR_RED - Marking with red color */
  COLOR_RED = 5,
  /** COLOR_BLUE - Marking with blue color. */
  COLOR_BLUE = 6,
  /** COLOR_GREEN - Marking with green color; */
  COLOR_GREEN = 7,
  /** COLOR_VIOLET - Marking with violet color. */
  COLOR_VIOLET = 8,
  /** COLOR_ORANGE - Marking with orange color. */
  COLOR_ORANGE = 9,
}

export function laneBoundary_Classification_ColorFromJSON(object: any): LaneBoundary_Classification_Color {
  switch (object) {
    case 0:
    case "COLOR_UNKNOWN":
      return LaneBoundary_Classification_Color.COLOR_UNKNOWN;
    case 1:
    case "COLOR_OTHER":
      return LaneBoundary_Classification_Color.COLOR_OTHER;
    case 2:
    case "COLOR_NONE":
      return LaneBoundary_Classification_Color.COLOR_NONE;
    case 3:
    case "COLOR_WHITE":
      return LaneBoundary_Classification_Color.COLOR_WHITE;
    case 4:
    case "COLOR_YELLOW":
      return LaneBoundary_Classification_Color.COLOR_YELLOW;
    case 5:
    case "COLOR_RED":
      return LaneBoundary_Classification_Color.COLOR_RED;
    case 6:
    case "COLOR_BLUE":
      return LaneBoundary_Classification_Color.COLOR_BLUE;
    case 7:
    case "COLOR_GREEN":
      return LaneBoundary_Classification_Color.COLOR_GREEN;
    case 8:
    case "COLOR_VIOLET":
      return LaneBoundary_Classification_Color.COLOR_VIOLET;
    case 9:
    case "COLOR_ORANGE":
      return LaneBoundary_Classification_Color.COLOR_ORANGE;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum LaneBoundary_Classification_Color");
  }
}

export function laneBoundary_Classification_ColorToJSON(object: LaneBoundary_Classification_Color): string {
  switch (object) {
    case LaneBoundary_Classification_Color.COLOR_UNKNOWN:
      return "COLOR_UNKNOWN";
    case LaneBoundary_Classification_Color.COLOR_OTHER:
      return "COLOR_OTHER";
    case LaneBoundary_Classification_Color.COLOR_NONE:
      return "COLOR_NONE";
    case LaneBoundary_Classification_Color.COLOR_WHITE:
      return "COLOR_WHITE";
    case LaneBoundary_Classification_Color.COLOR_YELLOW:
      return "COLOR_YELLOW";
    case LaneBoundary_Classification_Color.COLOR_RED:
      return "COLOR_RED";
    case LaneBoundary_Classification_Color.COLOR_BLUE:
      return "COLOR_BLUE";
    case LaneBoundary_Classification_Color.COLOR_GREEN:
      return "COLOR_GREEN";
    case LaneBoundary_Classification_Color.COLOR_VIOLET:
      return "COLOR_VIOLET";
    case LaneBoundary_Classification_Color.COLOR_ORANGE:
      return "COLOR_ORANGE";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum LaneBoundary_Classification_Color");
  }
}

function createBaseLane(): Lane {
  return { id: undefined, classification: undefined, source_reference: [] };
}

export const Lane: MessageFns<Lane> = {
  encode(message: Lane, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Identifier.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.classification !== undefined) {
      Lane_Classification.encode(message.classification, writer.uint32(18).fork()).join();
    }
    for (const v of message.source_reference) {
      ExternalReference.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Lane {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLane();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.classification = Lane_Classification.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.source_reference.push(ExternalReference.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Lane {
    return {
      id: isSet(object.id) ? Identifier.fromJSON(object.id) : undefined,
      classification: isSet(object.classification) ? Lane_Classification.fromJSON(object.classification) : undefined,
      source_reference: globalThis.Array.isArray(object?.source_reference)
        ? object.source_reference.map((e: any) => ExternalReference.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Lane): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Identifier.toJSON(message.id);
    }
    if (message.classification !== undefined) {
      obj.classification = Lane_Classification.toJSON(message.classification);
    }
    if (message.source_reference?.length) {
      obj.source_reference = message.source_reference.map((e) => ExternalReference.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Lane>, I>>(base?: I): Lane {
    return Lane.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lane>, I>>(object: I): Lane {
    const message = createBaseLane();
    message.id = (object.id !== undefined && object.id !== null) ? Identifier.fromPartial(object.id) : undefined;
    message.classification = (object.classification !== undefined && object.classification !== null)
      ? Lane_Classification.fromPartial(object.classification)
      : undefined;
    message.source_reference = object.source_reference?.map((e) => ExternalReference.fromPartial(e)) || [];
    return message;
  },
};

function createBaseLane_Classification(): Lane_Classification {
  return {
    type: 0,
    is_host_vehicle_lane: false,
    centerline: [],
    centerline_is_driving_direction: false,
    left_adjacent_lane_id: [],
    right_adjacent_lane_id: [],
    lane_pairing: [],
    right_lane_boundary_id: [],
    left_lane_boundary_id: [],
    free_lane_boundary_id: [],
    road_condition: undefined,
    subtype: 0,
  };
}

export const Lane_Classification: MessageFns<Lane_Classification> = {
  encode(message: Lane_Classification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== undefined && message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.is_host_vehicle_lane !== undefined && message.is_host_vehicle_lane !== false) {
      writer.uint32(16).bool(message.is_host_vehicle_lane);
    }
    for (const v of message.centerline) {
      Vector3d.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.centerline_is_driving_direction !== undefined && message.centerline_is_driving_direction !== false) {
      writer.uint32(32).bool(message.centerline_is_driving_direction);
    }
    for (const v of message.left_adjacent_lane_id) {
      Identifier.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.right_adjacent_lane_id) {
      Identifier.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.lane_pairing) {
      Lane_Classification_LanePairing.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.right_lane_boundary_id) {
      Identifier.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.left_lane_boundary_id) {
      Identifier.encode(v!, writer.uint32(74).fork()).join();
    }
    for (const v of message.free_lane_boundary_id) {
      Identifier.encode(v!, writer.uint32(82).fork()).join();
    }
    if (message.road_condition !== undefined) {
      Lane_Classification_RoadCondition.encode(message.road_condition, writer.uint32(90).fork()).join();
    }
    if (message.subtype !== undefined && message.subtype !== 0) {
      writer.uint32(96).int32(message.subtype);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Lane_Classification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLane_Classification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.is_host_vehicle_lane = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.centerline.push(Vector3d.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.centerline_is_driving_direction = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.left_adjacent_lane_id.push(Identifier.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.right_adjacent_lane_id.push(Identifier.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.lane_pairing.push(Lane_Classification_LanePairing.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.right_lane_boundary_id.push(Identifier.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.left_lane_boundary_id.push(Identifier.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.free_lane_boundary_id.push(Identifier.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.road_condition = Lane_Classification_RoadCondition.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.subtype = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Lane_Classification {
    return {
      type: isSet(object.type) ? lane_Classification_TypeFromJSON(object.type) : 0,
      is_host_vehicle_lane: isSet(object.is_host_vehicle_lane)
        ? globalThis.Boolean(object.is_host_vehicle_lane)
        : false,
      centerline: globalThis.Array.isArray(object?.centerline)
        ? object.centerline.map((e: any) => Vector3d.fromJSON(e))
        : [],
      centerline_is_driving_direction: isSet(object.centerline_is_driving_direction)
        ? globalThis.Boolean(object.centerline_is_driving_direction)
        : false,
      left_adjacent_lane_id: globalThis.Array.isArray(object?.left_adjacent_lane_id)
        ? object.left_adjacent_lane_id.map((e: any) => Identifier.fromJSON(e))
        : [],
      right_adjacent_lane_id: globalThis.Array.isArray(object?.right_adjacent_lane_id)
        ? object.right_adjacent_lane_id.map((e: any) => Identifier.fromJSON(e))
        : [],
      lane_pairing: globalThis.Array.isArray(object?.lane_pairing)
        ? object.lane_pairing.map((e: any) => Lane_Classification_LanePairing.fromJSON(e))
        : [],
      right_lane_boundary_id: globalThis.Array.isArray(object?.right_lane_boundary_id)
        ? object.right_lane_boundary_id.map((e: any) => Identifier.fromJSON(e))
        : [],
      left_lane_boundary_id: globalThis.Array.isArray(object?.left_lane_boundary_id)
        ? object.left_lane_boundary_id.map((e: any) => Identifier.fromJSON(e))
        : [],
      free_lane_boundary_id: globalThis.Array.isArray(object?.free_lane_boundary_id)
        ? object.free_lane_boundary_id.map((e: any) => Identifier.fromJSON(e))
        : [],
      road_condition: isSet(object.road_condition)
        ? Lane_Classification_RoadCondition.fromJSON(object.road_condition)
        : undefined,
      subtype: isSet(object.subtype) ? lane_Classification_SubtypeFromJSON(object.subtype) : 0,
    };
  },

  toJSON(message: Lane_Classification): unknown {
    const obj: any = {};
    if (message.type !== undefined) {
      obj.type = lane_Classification_TypeToJSON(message.type);
    }
    if (message.is_host_vehicle_lane !== undefined) {
      obj.is_host_vehicle_lane = message.is_host_vehicle_lane;
    }
    if (message.centerline?.length) {
      obj.centerline = message.centerline.map((e) => Vector3d.toJSON(e));
    }
    if (message.centerline_is_driving_direction !== undefined) {
      obj.centerline_is_driving_direction = message.centerline_is_driving_direction;
    }
    if (message.left_adjacent_lane_id?.length) {
      obj.left_adjacent_lane_id = message.left_adjacent_lane_id.map((e) => Identifier.toJSON(e));
    }
    if (message.right_adjacent_lane_id?.length) {
      obj.right_adjacent_lane_id = message.right_adjacent_lane_id.map((e) => Identifier.toJSON(e));
    }
    if (message.lane_pairing?.length) {
      obj.lane_pairing = message.lane_pairing.map((e) => Lane_Classification_LanePairing.toJSON(e));
    }
    if (message.right_lane_boundary_id?.length) {
      obj.right_lane_boundary_id = message.right_lane_boundary_id.map((e) => Identifier.toJSON(e));
    }
    if (message.left_lane_boundary_id?.length) {
      obj.left_lane_boundary_id = message.left_lane_boundary_id.map((e) => Identifier.toJSON(e));
    }
    if (message.free_lane_boundary_id?.length) {
      obj.free_lane_boundary_id = message.free_lane_boundary_id.map((e) => Identifier.toJSON(e));
    }
    if (message.road_condition !== undefined) {
      obj.road_condition = Lane_Classification_RoadCondition.toJSON(message.road_condition);
    }
    if (message.subtype !== undefined) {
      obj.subtype = lane_Classification_SubtypeToJSON(message.subtype);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Lane_Classification>, I>>(base?: I): Lane_Classification {
    return Lane_Classification.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lane_Classification>, I>>(object: I): Lane_Classification {
    const message = createBaseLane_Classification();
    message.type = object.type ?? 0;
    message.is_host_vehicle_lane = object.is_host_vehicle_lane ?? false;
    message.centerline = object.centerline?.map((e) => Vector3d.fromPartial(e)) || [];
    message.centerline_is_driving_direction = object.centerline_is_driving_direction ?? false;
    message.left_adjacent_lane_id = object.left_adjacent_lane_id?.map((e) => Identifier.fromPartial(e)) || [];
    message.right_adjacent_lane_id = object.right_adjacent_lane_id?.map((e) => Identifier.fromPartial(e)) || [];
    message.lane_pairing = object.lane_pairing?.map((e) => Lane_Classification_LanePairing.fromPartial(e)) || [];
    message.right_lane_boundary_id = object.right_lane_boundary_id?.map((e) => Identifier.fromPartial(e)) || [];
    message.left_lane_boundary_id = object.left_lane_boundary_id?.map((e) => Identifier.fromPartial(e)) || [];
    message.free_lane_boundary_id = object.free_lane_boundary_id?.map((e) => Identifier.fromPartial(e)) || [];
    message.road_condition = (object.road_condition !== undefined && object.road_condition !== null)
      ? Lane_Classification_RoadCondition.fromPartial(object.road_condition)
      : undefined;
    message.subtype = object.subtype ?? 0;
    return message;
  },
};

function createBaseLane_Classification_RoadCondition(): Lane_Classification_RoadCondition {
  return {
    surface_temperature: 0,
    surface_water_film: 0,
    surface_freezing_point: 0,
    surface_ice: 0,
    surface_roughness: 0,
    surface_texture: 0,
  };
}

export const Lane_Classification_RoadCondition: MessageFns<Lane_Classification_RoadCondition> = {
  encode(message: Lane_Classification_RoadCondition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.surface_temperature !== undefined && message.surface_temperature !== 0) {
      writer.uint32(9).double(message.surface_temperature);
    }
    if (message.surface_water_film !== undefined && message.surface_water_film !== 0) {
      writer.uint32(17).double(message.surface_water_film);
    }
    if (message.surface_freezing_point !== undefined && message.surface_freezing_point !== 0) {
      writer.uint32(25).double(message.surface_freezing_point);
    }
    if (message.surface_ice !== undefined && message.surface_ice !== 0) {
      writer.uint32(33).double(message.surface_ice);
    }
    if (message.surface_roughness !== undefined && message.surface_roughness !== 0) {
      writer.uint32(41).double(message.surface_roughness);
    }
    if (message.surface_texture !== undefined && message.surface_texture !== 0) {
      writer.uint32(49).double(message.surface_texture);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Lane_Classification_RoadCondition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLane_Classification_RoadCondition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.surface_temperature = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.surface_water_film = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.surface_freezing_point = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.surface_ice = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.surface_roughness = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.surface_texture = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Lane_Classification_RoadCondition {
    return {
      surface_temperature: isSet(object.surface_temperature) ? globalThis.Number(object.surface_temperature) : 0,
      surface_water_film: isSet(object.surface_water_film) ? globalThis.Number(object.surface_water_film) : 0,
      surface_freezing_point: isSet(object.surface_freezing_point)
        ? globalThis.Number(object.surface_freezing_point)
        : 0,
      surface_ice: isSet(object.surface_ice) ? globalThis.Number(object.surface_ice) : 0,
      surface_roughness: isSet(object.surface_roughness) ? globalThis.Number(object.surface_roughness) : 0,
      surface_texture: isSet(object.surface_texture) ? globalThis.Number(object.surface_texture) : 0,
    };
  },

  toJSON(message: Lane_Classification_RoadCondition): unknown {
    const obj: any = {};
    if (message.surface_temperature !== undefined) {
      obj.surface_temperature = message.surface_temperature;
    }
    if (message.surface_water_film !== undefined) {
      obj.surface_water_film = message.surface_water_film;
    }
    if (message.surface_freezing_point !== undefined) {
      obj.surface_freezing_point = message.surface_freezing_point;
    }
    if (message.surface_ice !== undefined) {
      obj.surface_ice = message.surface_ice;
    }
    if (message.surface_roughness !== undefined) {
      obj.surface_roughness = message.surface_roughness;
    }
    if (message.surface_texture !== undefined) {
      obj.surface_texture = message.surface_texture;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Lane_Classification_RoadCondition>, I>>(
    base?: I,
  ): Lane_Classification_RoadCondition {
    return Lane_Classification_RoadCondition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lane_Classification_RoadCondition>, I>>(
    object: I,
  ): Lane_Classification_RoadCondition {
    const message = createBaseLane_Classification_RoadCondition();
    message.surface_temperature = object.surface_temperature ?? 0;
    message.surface_water_film = object.surface_water_film ?? 0;
    message.surface_freezing_point = object.surface_freezing_point ?? 0;
    message.surface_ice = object.surface_ice ?? 0;
    message.surface_roughness = object.surface_roughness ?? 0;
    message.surface_texture = object.surface_texture ?? 0;
    return message;
  },
};

function createBaseLane_Classification_LanePairing(): Lane_Classification_LanePairing {
  return { antecessor_lane_id: undefined, successor_lane_id: undefined };
}

export const Lane_Classification_LanePairing: MessageFns<Lane_Classification_LanePairing> = {
  encode(message: Lane_Classification_LanePairing, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.antecessor_lane_id !== undefined) {
      Identifier.encode(message.antecessor_lane_id, writer.uint32(10).fork()).join();
    }
    if (message.successor_lane_id !== undefined) {
      Identifier.encode(message.successor_lane_id, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Lane_Classification_LanePairing {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLane_Classification_LanePairing();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.antecessor_lane_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.successor_lane_id = Identifier.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Lane_Classification_LanePairing {
    return {
      antecessor_lane_id: isSet(object.antecessor_lane_id) ? Identifier.fromJSON(object.antecessor_lane_id) : undefined,
      successor_lane_id: isSet(object.successor_lane_id) ? Identifier.fromJSON(object.successor_lane_id) : undefined,
    };
  },

  toJSON(message: Lane_Classification_LanePairing): unknown {
    const obj: any = {};
    if (message.antecessor_lane_id !== undefined) {
      obj.antecessor_lane_id = Identifier.toJSON(message.antecessor_lane_id);
    }
    if (message.successor_lane_id !== undefined) {
      obj.successor_lane_id = Identifier.toJSON(message.successor_lane_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Lane_Classification_LanePairing>, I>>(base?: I): Lane_Classification_LanePairing {
    return Lane_Classification_LanePairing.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lane_Classification_LanePairing>, I>>(
    object: I,
  ): Lane_Classification_LanePairing {
    const message = createBaseLane_Classification_LanePairing();
    message.antecessor_lane_id = (object.antecessor_lane_id !== undefined && object.antecessor_lane_id !== null)
      ? Identifier.fromPartial(object.antecessor_lane_id)
      : undefined;
    message.successor_lane_id = (object.successor_lane_id !== undefined && object.successor_lane_id !== null)
      ? Identifier.fromPartial(object.successor_lane_id)
      : undefined;
    return message;
  },
};

function createBaseLaneBoundary(): LaneBoundary {
  return {
    id: undefined,
    boundary_line: [],
    classification: undefined,
    source_reference: [],
    color_description: undefined,
  };
}

export const LaneBoundary: MessageFns<LaneBoundary> = {
  encode(message: LaneBoundary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Identifier.encode(message.id, writer.uint32(10).fork()).join();
    }
    for (const v of message.boundary_line) {
      LaneBoundary_BoundaryPoint.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.classification !== undefined) {
      LaneBoundary_Classification.encode(message.classification, writer.uint32(26).fork()).join();
    }
    for (const v of message.source_reference) {
      ExternalReference.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.color_description !== undefined) {
      ColorDescription.encode(message.color_description, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LaneBoundary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLaneBoundary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = Identifier.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.boundary_line.push(LaneBoundary_BoundaryPoint.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.classification = LaneBoundary_Classification.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.source_reference.push(ExternalReference.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.color_description = ColorDescription.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LaneBoundary {
    return {
      id: isSet(object.id) ? Identifier.fromJSON(object.id) : undefined,
      boundary_line: globalThis.Array.isArray(object?.boundary_line)
        ? object.boundary_line.map((e: any) => LaneBoundary_BoundaryPoint.fromJSON(e))
        : [],
      classification: isSet(object.classification)
        ? LaneBoundary_Classification.fromJSON(object.classification)
        : undefined,
      source_reference: globalThis.Array.isArray(object?.source_reference)
        ? object.source_reference.map((e: any) => ExternalReference.fromJSON(e))
        : [],
      color_description: isSet(object.color_description)
        ? ColorDescription.fromJSON(object.color_description)
        : undefined,
    };
  },

  toJSON(message: LaneBoundary): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Identifier.toJSON(message.id);
    }
    if (message.boundary_line?.length) {
      obj.boundary_line = message.boundary_line.map((e) => LaneBoundary_BoundaryPoint.toJSON(e));
    }
    if (message.classification !== undefined) {
      obj.classification = LaneBoundary_Classification.toJSON(message.classification);
    }
    if (message.source_reference?.length) {
      obj.source_reference = message.source_reference.map((e) => ExternalReference.toJSON(e));
    }
    if (message.color_description !== undefined) {
      obj.color_description = ColorDescription.toJSON(message.color_description);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LaneBoundary>, I>>(base?: I): LaneBoundary {
    return LaneBoundary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LaneBoundary>, I>>(object: I): LaneBoundary {
    const message = createBaseLaneBoundary();
    message.id = (object.id !== undefined && object.id !== null) ? Identifier.fromPartial(object.id) : undefined;
    message.boundary_line = object.boundary_line?.map((e) => LaneBoundary_BoundaryPoint.fromPartial(e)) || [];
    message.classification = (object.classification !== undefined && object.classification !== null)
      ? LaneBoundary_Classification.fromPartial(object.classification)
      : undefined;
    message.source_reference = object.source_reference?.map((e) => ExternalReference.fromPartial(e)) || [];
    message.color_description = (object.color_description !== undefined && object.color_description !== null)
      ? ColorDescription.fromPartial(object.color_description)
      : undefined;
    return message;
  },
};

function createBaseLaneBoundary_BoundaryPoint(): LaneBoundary_BoundaryPoint {
  return { position: undefined, width: 0, height: 0, dash: 0 };
}

export const LaneBoundary_BoundaryPoint: MessageFns<LaneBoundary_BoundaryPoint> = {
  encode(message: LaneBoundary_BoundaryPoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.position !== undefined) {
      Vector3d.encode(message.position, writer.uint32(10).fork()).join();
    }
    if (message.width !== undefined && message.width !== 0) {
      writer.uint32(17).double(message.width);
    }
    if (message.height !== undefined && message.height !== 0) {
      writer.uint32(25).double(message.height);
    }
    if (message.dash !== undefined && message.dash !== 0) {
      writer.uint32(32).int32(message.dash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LaneBoundary_BoundaryPoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLaneBoundary_BoundaryPoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.position = Vector3d.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.width = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.height = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.dash = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LaneBoundary_BoundaryPoint {
    return {
      position: isSet(object.position) ? Vector3d.fromJSON(object.position) : undefined,
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      dash: isSet(object.dash) ? laneBoundary_BoundaryPoint_DashFromJSON(object.dash) : 0,
    };
  },

  toJSON(message: LaneBoundary_BoundaryPoint): unknown {
    const obj: any = {};
    if (message.position !== undefined) {
      obj.position = Vector3d.toJSON(message.position);
    }
    if (message.width !== undefined) {
      obj.width = message.width;
    }
    if (message.height !== undefined) {
      obj.height = message.height;
    }
    if (message.dash !== undefined) {
      obj.dash = laneBoundary_BoundaryPoint_DashToJSON(message.dash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LaneBoundary_BoundaryPoint>, I>>(base?: I): LaneBoundary_BoundaryPoint {
    return LaneBoundary_BoundaryPoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LaneBoundary_BoundaryPoint>, I>>(object: I): LaneBoundary_BoundaryPoint {
    const message = createBaseLaneBoundary_BoundaryPoint();
    message.position = (object.position !== undefined && object.position !== null)
      ? Vector3d.fromPartial(object.position)
      : undefined;
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    message.dash = object.dash ?? 0;
    return message;
  },
};

function createBaseLaneBoundary_Classification(): LaneBoundary_Classification {
  return { type: 0, color: 0, limiting_structure_id: [] };
}

export const LaneBoundary_Classification: MessageFns<LaneBoundary_Classification> = {
  encode(message: LaneBoundary_Classification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== undefined && message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.color !== undefined && message.color !== 0) {
      writer.uint32(16).int32(message.color);
    }
    for (const v of message.limiting_structure_id) {
      Identifier.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LaneBoundary_Classification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLaneBoundary_Classification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.color = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.limiting_structure_id.push(Identifier.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LaneBoundary_Classification {
    return {
      type: isSet(object.type) ? laneBoundary_Classification_TypeFromJSON(object.type) : 0,
      color: isSet(object.color) ? laneBoundary_Classification_ColorFromJSON(object.color) : 0,
      limiting_structure_id: globalThis.Array.isArray(object?.limiting_structure_id)
        ? object.limiting_structure_id.map((e: any) => Identifier.fromJSON(e))
        : [],
    };
  },

  toJSON(message: LaneBoundary_Classification): unknown {
    const obj: any = {};
    if (message.type !== undefined) {
      obj.type = laneBoundary_Classification_TypeToJSON(message.type);
    }
    if (message.color !== undefined) {
      obj.color = laneBoundary_Classification_ColorToJSON(message.color);
    }
    if (message.limiting_structure_id?.length) {
      obj.limiting_structure_id = message.limiting_structure_id.map((e) => Identifier.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LaneBoundary_Classification>, I>>(base?: I): LaneBoundary_Classification {
    return LaneBoundary_Classification.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LaneBoundary_Classification>, I>>(object: I): LaneBoundary_Classification {
    const message = createBaseLaneBoundary_Classification();
    message.type = object.type ?? 0;
    message.color = object.color ?? 0;
    message.limiting_structure_id = object.limiting_structure_id?.map((e) => Identifier.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
