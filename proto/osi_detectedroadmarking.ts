// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_detectedroadmarking.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { BaseStationary, ColorDescription } from "./osi_common";
import { DetectedItemHeader } from "./osi_detectedobject";
import { RoadMarking_Classification } from "./osi_roadmarking";

/**
 * \brief A road marking in the environment as detected by the sensor.
 *
 * \image html OSI_DetectedRoadMarking.svg
 *
 * The figure shows two STOP road markings (\c
 * DetectedRoadMarking::CandidateRoadMarking::classification). STOP \c
 * RoadMarking::Classification::type == \c
 * RoadMarking::Classification::TYPE_TEXTUAL_TRAFFIC_SIGN is marked, STOP \c
 * RoadMarking::Classification::type == \c
 * RoadMarking::Classification::TYPE_SYMBOLIC_TRAFFIC_SIGN is not marked.
 *
 * The parent frame of a detected road marking is the virtual sensor coordinate
 * system.
 *
 * /note The virtual sensor coordinate system is relative to the vehicle coordinate
 * system which has its origin in the center of the rear axle of the ego
 * vehicle. This means if virtual sensor mounting position and orientation are
 * set to (0,0,0) the virtual sensor coordinate system coincides with the
 * vehicle coordinate system.
 */
export interface DetectedRoadMarking {
  /** Common information of one detected item. */
  header?:
    | DetectedItemHeader
    | undefined;
  /**
   * The base parameters of the road marking.
   *
   * The orientation of the bounding box \c #base
   * \c BaseStationary::orientation is defined as follows:
   * The z-axis of the \c BaseStationary::orientation is the vector from the
   * 'bottom' to the 'top' of the road marking's (i.e. painted traffic sign)
   * 2D image area.
   * (Normally it is in the ground truth xy-plain.)
   * The x-axis of the \c BaseStationary::orientation is the view normal of
   * the road marking's 2D image area.
   * Normally this x-axis points to the sky.
   *
   * \note If a valid unidirectional road marking is assigned to the host
   * vehicle's current lane and the driving direction of the latter roughly
   * matches the z-axis of the \c #base \c BaseStationary::orientation then
   * the road marking is of relevance to (i.e. in effect for) the host
   * vehicle.
   */
  base?:
    | BaseStationary
    | undefined;
  /**
   * The root mean squared error of the base parameters of the detected
   * road marking. \c RoadMarking::base has to be identical for
   * all \c #candidate road markings.
   */
  base_rmse?:
    | BaseStationary
    | undefined;
  /**
   * A list of candidates for this road marking as estimated by the
   * sensor.
   */
  candidate: DetectedRoadMarking_CandidateRoadMarking[];
  /**
   * The visual color of the material of the road marking.
   *
   * \note This does not represent the semantic classification but the visual
   * appearance. For semantic classification of the road marking use the color
   * field in \c CandidateRoadMarking::classification.
   */
  color_description?: ColorDescription | undefined;
}

/**
 * \brief A candidate for a detected road marking as estimated by the
 * sensor.
 */
export interface DetectedRoadMarking_CandidateRoadMarking {
  /**
   * The estimated probability that this candidate is the true value.
   *
   * \note The sum of all \c #probability must be one. This probability is
   * given under the condition of
   * \c DetectedItemHeader::existence_probability.
   *
   * \rules
   * is_less_than_or_equal_to: 1
   * is_greater_than_or_equal_to: 0
   * \endrules
   */
  probability?:
    | number
    | undefined;
  /**
   * The classification of the road marking.
   *
   * \note IDs, which are referenced in this message, usually
   * reference to \c DetectedXXX::tracking_id IDs.
   */
  classification?: RoadMarking_Classification | undefined;
}

function createBaseDetectedRoadMarking(): DetectedRoadMarking {
  return { header: undefined, base: undefined, base_rmse: undefined, candidate: [], color_description: undefined };
}

export const DetectedRoadMarking: MessageFns<DetectedRoadMarking> = {
  encode(message: DetectedRoadMarking, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      DetectedItemHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.base !== undefined) {
      BaseStationary.encode(message.base, writer.uint32(18).fork()).join();
    }
    if (message.base_rmse !== undefined) {
      BaseStationary.encode(message.base_rmse, writer.uint32(26).fork()).join();
    }
    for (const v of message.candidate) {
      DetectedRoadMarking_CandidateRoadMarking.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.color_description !== undefined) {
      ColorDescription.encode(message.color_description, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectedRoadMarking {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectedRoadMarking();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = DetectedItemHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.base = BaseStationary.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.base_rmse = BaseStationary.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.candidate.push(DetectedRoadMarking_CandidateRoadMarking.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.color_description = ColorDescription.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectedRoadMarking {
    return {
      header: isSet(object.header) ? DetectedItemHeader.fromJSON(object.header) : undefined,
      base: isSet(object.base) ? BaseStationary.fromJSON(object.base) : undefined,
      base_rmse: isSet(object.base_rmse) ? BaseStationary.fromJSON(object.base_rmse) : undefined,
      candidate: globalThis.Array.isArray(object?.candidate)
        ? object.candidate.map((e: any) => DetectedRoadMarking_CandidateRoadMarking.fromJSON(e))
        : [],
      color_description: isSet(object.color_description)
        ? ColorDescription.fromJSON(object.color_description)
        : undefined,
    };
  },

  toJSON(message: DetectedRoadMarking): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = DetectedItemHeader.toJSON(message.header);
    }
    if (message.base !== undefined) {
      obj.base = BaseStationary.toJSON(message.base);
    }
    if (message.base_rmse !== undefined) {
      obj.base_rmse = BaseStationary.toJSON(message.base_rmse);
    }
    if (message.candidate?.length) {
      obj.candidate = message.candidate.map((e) => DetectedRoadMarking_CandidateRoadMarking.toJSON(e));
    }
    if (message.color_description !== undefined) {
      obj.color_description = ColorDescription.toJSON(message.color_description);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectedRoadMarking>, I>>(base?: I): DetectedRoadMarking {
    return DetectedRoadMarking.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectedRoadMarking>, I>>(object: I): DetectedRoadMarking {
    const message = createBaseDetectedRoadMarking();
    message.header = (object.header !== undefined && object.header !== null)
      ? DetectedItemHeader.fromPartial(object.header)
      : undefined;
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseStationary.fromPartial(object.base)
      : undefined;
    message.base_rmse = (object.base_rmse !== undefined && object.base_rmse !== null)
      ? BaseStationary.fromPartial(object.base_rmse)
      : undefined;
    message.candidate = object.candidate?.map((e) => DetectedRoadMarking_CandidateRoadMarking.fromPartial(e)) || [];
    message.color_description = (object.color_description !== undefined && object.color_description !== null)
      ? ColorDescription.fromPartial(object.color_description)
      : undefined;
    return message;
  },
};

function createBaseDetectedRoadMarking_CandidateRoadMarking(): DetectedRoadMarking_CandidateRoadMarking {
  return { probability: 0, classification: undefined };
}

export const DetectedRoadMarking_CandidateRoadMarking: MessageFns<DetectedRoadMarking_CandidateRoadMarking> = {
  encode(message: DetectedRoadMarking_CandidateRoadMarking, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.probability !== undefined && message.probability !== 0) {
      writer.uint32(9).double(message.probability);
    }
    if (message.classification !== undefined) {
      RoadMarking_Classification.encode(message.classification, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectedRoadMarking_CandidateRoadMarking {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectedRoadMarking_CandidateRoadMarking();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.probability = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.classification = RoadMarking_Classification.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectedRoadMarking_CandidateRoadMarking {
    return {
      probability: isSet(object.probability) ? globalThis.Number(object.probability) : 0,
      classification: isSet(object.classification)
        ? RoadMarking_Classification.fromJSON(object.classification)
        : undefined,
    };
  },

  toJSON(message: DetectedRoadMarking_CandidateRoadMarking): unknown {
    const obj: any = {};
    if (message.probability !== undefined) {
      obj.probability = message.probability;
    }
    if (message.classification !== undefined) {
      obj.classification = RoadMarking_Classification.toJSON(message.classification);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectedRoadMarking_CandidateRoadMarking>, I>>(
    base?: I,
  ): DetectedRoadMarking_CandidateRoadMarking {
    return DetectedRoadMarking_CandidateRoadMarking.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectedRoadMarking_CandidateRoadMarking>, I>>(
    object: I,
  ): DetectedRoadMarking_CandidateRoadMarking {
    const message = createBaseDetectedRoadMarking_CandidateRoadMarking();
    message.probability = object.probability ?? 0;
    message.classification = (object.classification !== undefined && object.classification !== null)
      ? RoadMarking_Classification.fromPartial(object.classification)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
