// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_trafficupdate.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./osi_common";
import { HostVehicleData } from "./osi_hostvehicledata";
import { MovingObject } from "./osi_object";
import { InterfaceVersion } from "./osi_version";

/**
 * \brief The traffic update message is provided by traffic participant
 * models to provide updates to their position, state and future
 * trajectory back to the simulation environment. The message is
 * designed to update data of exactly one traffic participant,
 * optionally with an attached trailer.
 *
 * \note For reasons of convenience and consistency, the updated
 * information is provided as a MovingObject.  Certain fields of this
 * sub-message are not required to be set and will be ignored by the
 * simulation environment, because they are static information.
 * Instead of creating a separate message type for only the non-static
 * information, re-use existing message.
 */
export interface TrafficUpdate {
  /**
   * The interface version used by the sender (traffic participant model).
   *
   * \rules
   * is_set
   * \endrules
   */
  version?:
    | InterfaceVersion
    | undefined;
  /**
   * The data timestamp of the simulation environment. Zero time is arbitrary
   * but must be identical for all messages. Zero time does not need to
   * coincide with the UNIX epoch. Recommended is the starting time point of
   * the simulation.
   *
   * \note For moving object update data the timestamp coincides both with
   * the notional simulation time the data applies to and the time it was sent.
   * There is no inherent latency for moving object update data, as opposed
   * to sensor data.
   *
   * \rules
   * is_set
   * \endrules
   */
  timestamp?:
    | Timestamp
    | undefined;
  /**
   * Updated traffic participant data
   *
   * \note It is not expected that static fields are populated. If they
   * are, they may be ignored by the receiver of this message, for example, dimensions,
   * or vehicle category.  All dynamic fields should be populated where known,
   * for example, velocity, light states, or future trajectory.
   *
   * \note The field is repeated because it is possible to have a trailer attached to
   * a vehicle, see MovingObject::VehicleClassification::has_trailer and
   * MovingObject::VehicleClassification::trailer_id.
   */
  update: MovingObject[];
  /**
   * Internal state for each vehicle.
   *
   * This is an optional field as internal state may not be known or relevant,
   * for example, a trailer might not have any internal state.
   * It is also allowed to only specify internal_state for a subset of the
   * objects referenced in the update.
   *
   * \note This covers any information which cannot be externally perceived
   * and therefore cannot be included in messages available in ground truth.
   *
   * \note The id field from this should match the id in the update field
   * above where the same vehicle is being referenced.
   */
  internal_state: HostVehicleData[];
}

function createBaseTrafficUpdate(): TrafficUpdate {
  return { version: undefined, timestamp: undefined, update: [], internal_state: [] };
}

export const TrafficUpdate: MessageFns<TrafficUpdate> = {
  encode(message: TrafficUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== undefined) {
      InterfaceVersion.encode(message.version, writer.uint32(10).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(18).fork()).join();
    }
    for (const v of message.update) {
      MovingObject.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.internal_state) {
      HostVehicleData.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrafficUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrafficUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = InterfaceVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.update.push(MovingObject.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.internal_state.push(HostVehicleData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrafficUpdate {
    return {
      version: isSet(object.version) ? InterfaceVersion.fromJSON(object.version) : undefined,
      timestamp: isSet(object.timestamp) ? Timestamp.fromJSON(object.timestamp) : undefined,
      update: globalThis.Array.isArray(object?.update) ? object.update.map((e: any) => MovingObject.fromJSON(e)) : [],
      internal_state: globalThis.Array.isArray(object?.internal_state)
        ? object.internal_state.map((e: any) => HostVehicleData.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TrafficUpdate): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = InterfaceVersion.toJSON(message.version);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = Timestamp.toJSON(message.timestamp);
    }
    if (message.update?.length) {
      obj.update = message.update.map((e) => MovingObject.toJSON(e));
    }
    if (message.internal_state?.length) {
      obj.internal_state = message.internal_state.map((e) => HostVehicleData.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrafficUpdate>, I>>(base?: I): TrafficUpdate {
    return TrafficUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrafficUpdate>, I>>(object: I): TrafficUpdate {
    const message = createBaseTrafficUpdate();
    message.version = (object.version !== undefined && object.version !== null)
      ? InterfaceVersion.fromPartial(object.version)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.update = object.update?.map((e) => MovingObject.fromPartial(e)) || [];
    message.internal_state = object.internal_state?.map((e) => HostVehicleData.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
