// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_detectedlane.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ColorDescription } from "./osi_common";
import { DetectedItemHeader } from "./osi_detectedobject";
import { Lane_Classification, LaneBoundary_BoundaryPoint, LaneBoundary_Classification } from "./osi_lane";

/** \brief A lane segment as detected by the sensor. */
export interface DetectedLane {
  /** Common information of one detected item. */
  header?:
    | DetectedItemHeader
    | undefined;
  /** A list of candidates for this lane as estimated by the sensor. */
  candidate: DetectedLane_CandidateLane[];
}

/**
 * \brief A candidate for a detected lane as estimated by the
 * sensor.
 */
export interface DetectedLane_CandidateLane {
  /**
   * The estimated probability that this candidate is the true value.
   *
   * \note The sum of all \c #probability must be one. This probability is
   * given under the condition of
   * \c DetectedItemHeader::existence_probability.
   *
   * \rules
   * is_less_than_or_equal_to: 1
   * is_greater_than_or_equal_to: 0
   * \endrules
   */
  probability?:
    | number
    | undefined;
  /**
   * The classification of one lane that defines this candidate.
   *
   * \note IDs, which are referenced in this message, usually
   * reference to \c DetectedXXX::tracking_id IDs.
   */
  classification?: Lane_Classification | undefined;
}

/**
 * \brief A lane boundary segment as detected by the sensor.
 *
 * \image html OSI_DetectedLaneBoundary.svg
 *
 * The parent frame of a detected lane boundary is the virtual sensor coordinate
 * system.
 *
 * /note The virtual sensor coordinate system is relative to the vehicle coordinate
 * system which has its origin in the center of the rear axle of the ego
 * vehicle. This means if virtual sensor mounting position and orientation are
 * set to (0,0,0) the virtual sensor coordinate system coincides with the
 * vehicle coordinate system.
 */
export interface DetectedLaneBoundary {
  /** Common information of one detected item. */
  header?:
    | DetectedItemHeader
    | undefined;
  /**
   * A list of candidates for this lane boundary as estimated by the
   * sensor.
   */
  candidate: DetectedLaneBoundary_CandidateLaneBoundary[];
  /**
   * The list of individual points defining the location of the lane boundary
   * (as a list of segments).
   *
   * Since a \c BoundaryPoint is part of a sequence, only the position
   * attribute has to be set for each instance. All other values will be
   * reused from the previous \c BoundaryPoint in the sequence or set to
   * default values if there is none or it was never set. For dashed lines,
   * one \c LaneBoundary::BoundaryPoint has to be at the start and another at
   * the end of each dashed line segment. For Botts' dots lines, one \c
   * LaneBoundary::BoundaryPoint position has to define each Botts' dot.
   *
   * \attention For \c LaneBoundary::BoundaryPoint the same rules regarding
   * maximum distance and approximation error apply as for \c
   * Lane::Classification::centerline.
   */
  boundary_line: LaneBoundary_BoundaryPoint[];
  /**
   * The root mean squared error of the \c LaneBoundary::BoundaryPoint.
   * Each \c #candidate has the same \c #boundary_line points and exact
   * one \c #boundary_line_rmse rmse confidence value is
   * specified which is suitable for all candidates.
   */
  boundary_line_rmse: LaneBoundary_BoundaryPoint[];
  /**
   * Confidence of the segments of the \c LaneBoundary::BoundaryPoint.
   * Each \c #candidate has the same \c #boundary_line points and exact
   * one \c #boundary_line_confidences confidence value is
   * specified which is suitable for all candidates.
   *
   * \rules
   * is_greater_than_or_equal_to: 0
   * is_less_than_or_equal_to: 1
   * \endrules
   */
  boundary_line_confidences: number[];
  /**
   * The visual color of the material of the lane boundary.
   *
   * \note This does not represent the semantic classification but the visual
   * appearance. For semantic classification of the lane boundary use the color
   * field in \c CandidateLaneBoundary::classification.
   */
  color_description?: ColorDescription | undefined;
}

/**
 * \brief A candidate for a detected lane boundary as estimated by the
 * sensor.
 */
export interface DetectedLaneBoundary_CandidateLaneBoundary {
  /**
   * The estimated probability that this candidate is the true value.
   *
   * \note The sum of all \c #probability must be one. This probability is
   * given under the condition of
   * \c DetectedItemHeader::existence_probability.
   *
   * \rules
   * is_greater_than_or_equal_to: 0
   * is_less_than_or_equal_to: 1
   * \endrules
   */
  probability?:
    | number
    | undefined;
  /**
   * The classification of one lane boundary that defines this candidate.
   *
   * \note IDs, which are referenced in this message, usually
   * reference to \c DetectedXXX::tracking_id IDs.
   */
  classification?: LaneBoundary_Classification | undefined;
}

function createBaseDetectedLane(): DetectedLane {
  return { header: undefined, candidate: [] };
}

export const DetectedLane: MessageFns<DetectedLane> = {
  encode(message: DetectedLane, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      DetectedItemHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    for (const v of message.candidate) {
      DetectedLane_CandidateLane.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectedLane {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectedLane();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = DetectedItemHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.candidate.push(DetectedLane_CandidateLane.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectedLane {
    return {
      header: isSet(object.header) ? DetectedItemHeader.fromJSON(object.header) : undefined,
      candidate: globalThis.Array.isArray(object?.candidate)
        ? object.candidate.map((e: any) => DetectedLane_CandidateLane.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DetectedLane): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = DetectedItemHeader.toJSON(message.header);
    }
    if (message.candidate?.length) {
      obj.candidate = message.candidate.map((e) => DetectedLane_CandidateLane.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectedLane>, I>>(base?: I): DetectedLane {
    return DetectedLane.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectedLane>, I>>(object: I): DetectedLane {
    const message = createBaseDetectedLane();
    message.header = (object.header !== undefined && object.header !== null)
      ? DetectedItemHeader.fromPartial(object.header)
      : undefined;
    message.candidate = object.candidate?.map((e) => DetectedLane_CandidateLane.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDetectedLane_CandidateLane(): DetectedLane_CandidateLane {
  return { probability: 0, classification: undefined };
}

export const DetectedLane_CandidateLane: MessageFns<DetectedLane_CandidateLane> = {
  encode(message: DetectedLane_CandidateLane, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.probability !== undefined && message.probability !== 0) {
      writer.uint32(9).double(message.probability);
    }
    if (message.classification !== undefined) {
      Lane_Classification.encode(message.classification, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectedLane_CandidateLane {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectedLane_CandidateLane();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.probability = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.classification = Lane_Classification.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectedLane_CandidateLane {
    return {
      probability: isSet(object.probability) ? globalThis.Number(object.probability) : 0,
      classification: isSet(object.classification) ? Lane_Classification.fromJSON(object.classification) : undefined,
    };
  },

  toJSON(message: DetectedLane_CandidateLane): unknown {
    const obj: any = {};
    if (message.probability !== undefined) {
      obj.probability = message.probability;
    }
    if (message.classification !== undefined) {
      obj.classification = Lane_Classification.toJSON(message.classification);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectedLane_CandidateLane>, I>>(base?: I): DetectedLane_CandidateLane {
    return DetectedLane_CandidateLane.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectedLane_CandidateLane>, I>>(object: I): DetectedLane_CandidateLane {
    const message = createBaseDetectedLane_CandidateLane();
    message.probability = object.probability ?? 0;
    message.classification = (object.classification !== undefined && object.classification !== null)
      ? Lane_Classification.fromPartial(object.classification)
      : undefined;
    return message;
  },
};

function createBaseDetectedLaneBoundary(): DetectedLaneBoundary {
  return {
    header: undefined,
    candidate: [],
    boundary_line: [],
    boundary_line_rmse: [],
    boundary_line_confidences: [],
    color_description: undefined,
  };
}

export const DetectedLaneBoundary: MessageFns<DetectedLaneBoundary> = {
  encode(message: DetectedLaneBoundary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      DetectedItemHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    for (const v of message.candidate) {
      DetectedLaneBoundary_CandidateLaneBoundary.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.boundary_line) {
      LaneBoundary_BoundaryPoint.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.boundary_line_rmse) {
      LaneBoundary_BoundaryPoint.encode(v!, writer.uint32(34).fork()).join();
    }
    writer.uint32(42).fork();
    for (const v of message.boundary_line_confidences) {
      writer.double(v);
    }
    writer.join();
    if (message.color_description !== undefined) {
      ColorDescription.encode(message.color_description, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectedLaneBoundary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectedLaneBoundary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = DetectedItemHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.candidate.push(DetectedLaneBoundary_CandidateLaneBoundary.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.boundary_line.push(LaneBoundary_BoundaryPoint.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.boundary_line_rmse.push(LaneBoundary_BoundaryPoint.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag === 41) {
            message.boundary_line_confidences.push(reader.double());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.boundary_line_confidences.push(reader.double());
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.color_description = ColorDescription.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectedLaneBoundary {
    return {
      header: isSet(object.header) ? DetectedItemHeader.fromJSON(object.header) : undefined,
      candidate: globalThis.Array.isArray(object?.candidate)
        ? object.candidate.map((e: any) => DetectedLaneBoundary_CandidateLaneBoundary.fromJSON(e))
        : [],
      boundary_line: globalThis.Array.isArray(object?.boundary_line)
        ? object.boundary_line.map((e: any) => LaneBoundary_BoundaryPoint.fromJSON(e))
        : [],
      boundary_line_rmse: globalThis.Array.isArray(object?.boundary_line_rmse)
        ? object.boundary_line_rmse.map((e: any) => LaneBoundary_BoundaryPoint.fromJSON(e))
        : [],
      boundary_line_confidences: globalThis.Array.isArray(object?.boundary_line_confidences)
        ? object.boundary_line_confidences.map((e: any) => globalThis.Number(e))
        : [],
      color_description: isSet(object.color_description)
        ? ColorDescription.fromJSON(object.color_description)
        : undefined,
    };
  },

  toJSON(message: DetectedLaneBoundary): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = DetectedItemHeader.toJSON(message.header);
    }
    if (message.candidate?.length) {
      obj.candidate = message.candidate.map((e) => DetectedLaneBoundary_CandidateLaneBoundary.toJSON(e));
    }
    if (message.boundary_line?.length) {
      obj.boundary_line = message.boundary_line.map((e) => LaneBoundary_BoundaryPoint.toJSON(e));
    }
    if (message.boundary_line_rmse?.length) {
      obj.boundary_line_rmse = message.boundary_line_rmse.map((e) => LaneBoundary_BoundaryPoint.toJSON(e));
    }
    if (message.boundary_line_confidences?.length) {
      obj.boundary_line_confidences = message.boundary_line_confidences;
    }
    if (message.color_description !== undefined) {
      obj.color_description = ColorDescription.toJSON(message.color_description);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectedLaneBoundary>, I>>(base?: I): DetectedLaneBoundary {
    return DetectedLaneBoundary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectedLaneBoundary>, I>>(object: I): DetectedLaneBoundary {
    const message = createBaseDetectedLaneBoundary();
    message.header = (object.header !== undefined && object.header !== null)
      ? DetectedItemHeader.fromPartial(object.header)
      : undefined;
    message.candidate = object.candidate?.map((e) => DetectedLaneBoundary_CandidateLaneBoundary.fromPartial(e)) || [];
    message.boundary_line = object.boundary_line?.map((e) => LaneBoundary_BoundaryPoint.fromPartial(e)) || [];
    message.boundary_line_rmse = object.boundary_line_rmse?.map((e) => LaneBoundary_BoundaryPoint.fromPartial(e)) || [];
    message.boundary_line_confidences = object.boundary_line_confidences?.map((e) => e) || [];
    message.color_description = (object.color_description !== undefined && object.color_description !== null)
      ? ColorDescription.fromPartial(object.color_description)
      : undefined;
    return message;
  },
};

function createBaseDetectedLaneBoundary_CandidateLaneBoundary(): DetectedLaneBoundary_CandidateLaneBoundary {
  return { probability: 0, classification: undefined };
}

export const DetectedLaneBoundary_CandidateLaneBoundary: MessageFns<DetectedLaneBoundary_CandidateLaneBoundary> = {
  encode(message: DetectedLaneBoundary_CandidateLaneBoundary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.probability !== undefined && message.probability !== 0) {
      writer.uint32(9).double(message.probability);
    }
    if (message.classification !== undefined) {
      LaneBoundary_Classification.encode(message.classification, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectedLaneBoundary_CandidateLaneBoundary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectedLaneBoundary_CandidateLaneBoundary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.probability = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.classification = LaneBoundary_Classification.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectedLaneBoundary_CandidateLaneBoundary {
    return {
      probability: isSet(object.probability) ? globalThis.Number(object.probability) : 0,
      classification: isSet(object.classification)
        ? LaneBoundary_Classification.fromJSON(object.classification)
        : undefined,
    };
  },

  toJSON(message: DetectedLaneBoundary_CandidateLaneBoundary): unknown {
    const obj: any = {};
    if (message.probability !== undefined) {
      obj.probability = message.probability;
    }
    if (message.classification !== undefined) {
      obj.classification = LaneBoundary_Classification.toJSON(message.classification);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectedLaneBoundary_CandidateLaneBoundary>, I>>(
    base?: I,
  ): DetectedLaneBoundary_CandidateLaneBoundary {
    return DetectedLaneBoundary_CandidateLaneBoundary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectedLaneBoundary_CandidateLaneBoundary>, I>>(
    object: I,
  ): DetectedLaneBoundary_CandidateLaneBoundary {
    const message = createBaseDetectedLaneBoundary_CandidateLaneBoundary();
    message.probability = object.probability ?? 0;
    message.classification = (object.classification !== undefined && object.classification !== null)
      ? LaneBoundary_Classification.fromPartial(object.classification)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
