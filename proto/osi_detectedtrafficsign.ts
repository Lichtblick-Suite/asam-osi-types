// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osi_detectedtrafficsign.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { BaseStationary } from "./osi_common";
import { DetectedItemHeader } from "./osi_detectedobject";
import { TrafficSign_MainSign_Classification, TrafficSign_SupplementarySign_Classification } from "./osi_trafficsign";

/**
 * \brief A traffic sign in the environment as detected by the sensor.
 *
 * \image html OSI_DetectedSign.svg
 *
 * The parent frame of a detected traffic sign is the virtual sensor coordinate
 * system.
 *
 * \note The virtual sensor coordinate system is relative to the vehicle coordinate
 * system which has its origin in the center of the rear axle of the ego
 * vehicle. This means if virtual sensor mounting position and orientation are
 * set to (0,0,0) the virtual sensor coordinate system coincides with the
 * vehicle coordinate system.
 */
export interface DetectedTrafficSign {
  /** Common information of one detected item. */
  header?:
    | DetectedItemHeader
    | undefined;
  /** The main sign as detected by the sensor. */
  main_sign?:
    | DetectedTrafficSign_DetectedMainSign
    | undefined;
  /** A list of additional supplementary sign(s) as detected by the sensor. */
  supplementary_sign: DetectedTrafficSign_DetectedSupplementarySign[];
}

/**
 * \brief Candidates for a detected main sign as estimated by the sensor.
 *
 * \image html OSI_DetectedMainSign.svg
 */
export interface DetectedTrafficSign_DetectedMainSign {
  /**
   * A list of candidates for this traffic sign as estimated by the
   * sensor.
   *
   * \note OSI uses singular instead of plural for repeated field names.
   */
  candidate: DetectedTrafficSign_DetectedMainSign_CandidateMainSign[];
  /**
   * The base parameters of the traffic sign.
   *
   * The orientation of the bounding box \c #base
   * ( \c BaseStationary::orientation ) is defined as follows:
   *
   * The z-axis of the given \c BaseStationary::orientation is the vector
   * from the bottom to the top of the traffic sign's 2D image plate.
   * (Normally it is equal to the ground truth z-axis.)
   *
   * The x-axis of the given \c BaseStationary::orientation is view
   * normal of the traffic sign's image.
   *
   * This x-axis points from the traffic sign's image in the direction
   * from where a 'viewer' could see the traffic sign image.
   */
  base?:
    | BaseStationary
    | undefined;
  /**
   * The root mean squared error of the base parameters of the detected
   * traffic sign. \c TrafficSign::MainSign::base has to be identical for
   * all \c #candidate traffic signs.
   */
  base_rmse?:
    | BaseStationary
    | undefined;
  /** The estimated geometry of the traffic sign. */
  geometry?: DetectedTrafficSign_DetectedMainSign_Geometry | undefined;
}

/** Definition of traffic sign geometries. */
export enum DetectedTrafficSign_DetectedMainSign_Geometry {
  /**
   * GEOMETRY_UNKNOWN - Geometry of the traffic sign is unknown (must not be used in
   * ground truth).
   */
  GEOMETRY_UNKNOWN = 0,
  /** GEOMETRY_OTHER - Geometry of the traffic sign is unspecified (but known). */
  GEOMETRY_OTHER = 1,
  /** GEOMETRY_CIRCLE - Traffic sign has a circular geometry. (0 corners) */
  GEOMETRY_CIRCLE = 2,
  /**
   * GEOMETRY_TRIANGLE_TOP - Traffic sign has a triangular geometry pointing to the top. (3
   * corners)
   */
  GEOMETRY_TRIANGLE_TOP = 3,
  /** GEOMETRY_TRIANGLE_DOWN - Traffic sign has a triangular geometry pointing down. (3 corners) */
  GEOMETRY_TRIANGLE_DOWN = 4,
  /** GEOMETRY_SQUARE - Traffic sign has a square geometry. (4 corners) */
  GEOMETRY_SQUARE = 5,
  /**
   * GEOMETRY_POLE - Traffic sign that has a pole geometry. (height is bigger than
   * width e.g. pole indicating highways exit in xx m). (4 corners)
   */
  GEOMETRY_POLE = 6,
  /**
   * GEOMETRY_RECTANGLE - Traffic sign has a rectangle geometry. (width is bigger than
   * height e.g. one-way) (4 corners)
   */
  GEOMETRY_RECTANGLE = 7,
  /**
   * GEOMETRY_PLATE - Traffic sign that has an oversize rectangle geometry. (4 corners)
   * E.g. direction plates on highway or city signs.
   */
  GEOMETRY_PLATE = 8,
  /** GEOMETRY_DIAMOND - Traffic sign has a diamond geometry. (4 corners) */
  GEOMETRY_DIAMOND = 9,
  /**
   * GEOMETRY_ARROW_LEFT - Traffic sign has a geometry of an arrow pointing to the left. (5
   * corners)
   */
  GEOMETRY_ARROW_LEFT = 10,
  /**
   * GEOMETRY_ARROW_RIGHT - Traffic sign has a geometry of an arrow pointing to the right. (5
   * corners)
   */
  GEOMETRY_ARROW_RIGHT = 11,
  /** GEOMETRY_OCTAGON - Traffic sign has an octagon geometry. (8 corners) */
  GEOMETRY_OCTAGON = 12,
}

export function detectedTrafficSign_DetectedMainSign_GeometryFromJSON(
  object: any,
): DetectedTrafficSign_DetectedMainSign_Geometry {
  switch (object) {
    case 0:
    case "GEOMETRY_UNKNOWN":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_UNKNOWN;
    case 1:
    case "GEOMETRY_OTHER":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_OTHER;
    case 2:
    case "GEOMETRY_CIRCLE":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_CIRCLE;
    case 3:
    case "GEOMETRY_TRIANGLE_TOP":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_TRIANGLE_TOP;
    case 4:
    case "GEOMETRY_TRIANGLE_DOWN":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_TRIANGLE_DOWN;
    case 5:
    case "GEOMETRY_SQUARE":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_SQUARE;
    case 6:
    case "GEOMETRY_POLE":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_POLE;
    case 7:
    case "GEOMETRY_RECTANGLE":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_RECTANGLE;
    case 8:
    case "GEOMETRY_PLATE":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_PLATE;
    case 9:
    case "GEOMETRY_DIAMOND":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_DIAMOND;
    case 10:
    case "GEOMETRY_ARROW_LEFT":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_ARROW_LEFT;
    case 11:
    case "GEOMETRY_ARROW_RIGHT":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_ARROW_RIGHT;
    case 12:
    case "GEOMETRY_OCTAGON":
      return DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_OCTAGON;
    default:
      throw new globalThis.Error(
        "Unrecognized enum value " + object + " for enum DetectedTrafficSign_DetectedMainSign_Geometry",
      );
  }
}

export function detectedTrafficSign_DetectedMainSign_GeometryToJSON(
  object: DetectedTrafficSign_DetectedMainSign_Geometry,
): string {
  switch (object) {
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_UNKNOWN:
      return "GEOMETRY_UNKNOWN";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_OTHER:
      return "GEOMETRY_OTHER";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_CIRCLE:
      return "GEOMETRY_CIRCLE";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_TRIANGLE_TOP:
      return "GEOMETRY_TRIANGLE_TOP";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_TRIANGLE_DOWN:
      return "GEOMETRY_TRIANGLE_DOWN";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_SQUARE:
      return "GEOMETRY_SQUARE";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_POLE:
      return "GEOMETRY_POLE";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_RECTANGLE:
      return "GEOMETRY_RECTANGLE";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_PLATE:
      return "GEOMETRY_PLATE";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_DIAMOND:
      return "GEOMETRY_DIAMOND";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_ARROW_LEFT:
      return "GEOMETRY_ARROW_LEFT";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_ARROW_RIGHT:
      return "GEOMETRY_ARROW_RIGHT";
    case DetectedTrafficSign_DetectedMainSign_Geometry.GEOMETRY_OCTAGON:
      return "GEOMETRY_OCTAGON";
    default:
      throw new globalThis.Error(
        "Unrecognized enum value " + object + " for enum DetectedTrafficSign_DetectedMainSign_Geometry",
      );
  }
}

/**
 * \brief A candidate for a detected main sign as estimated by the
 * sensor.
 */
export interface DetectedTrafficSign_DetectedMainSign_CandidateMainSign {
  /**
   * The estimated probability that this candidate is the true value.
   *
   * \note The sum of all \c #probability must be one. This
   * probability is given under the condition of \c
   * DetectedItemHeader::existence_probability.
   *
   * \rules
   * is_less_than_or_equal_to: 1
   * is_greater_than_or_equal_to: 0
   * \endrules
   */
  probability?:
    | number
    | undefined;
  /**
   * The classification of one main sign that defines this candidate.
   *
   * \note IDs, which are referenced in this message, usually
   * reference to \c DetectedXXX::tracking_id IDs.
   */
  classification?: TrafficSign_MainSign_Classification | undefined;
}

/**
 * \brief Candidates for all detected supplementary signs of one traffic
 * sign as estimated by the sensor.
 *
 * \image html OSI_DetectedSupplementarySign.svg
 */
export interface DetectedTrafficSign_DetectedSupplementarySign {
  /**
   * The definition of one of more supplementary signs that together
   * define this candidate.
   *
   * \note OSI uses singular instead of plural for repeated field names.
   */
  candidate: DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign[];
  /**
   * The base parameters of the supplementary traffic sign.
   *
   * The orientation of the bounding box \c SupplementarySign::base
   * ( \c BaseStationary::orientation ) is defined as follows:
   * The z-axis of the given \c BaseStationary::orientation is the vector
   * from the bottom to the top of the supplementary traffic sign's 2D
   * image plate. (Normally it is equal to the ground truth z-axis.) The
   * x-axis of the given \c BaseStationary::orientation is view normal of
   * the supplementary traffic sign's image. This x-axis points from the
   * supplementary traffic sign's image in the direction from where a
   * 'viewer' could see the supplementary traffic sign image.
   */
  base?:
    | BaseStationary
    | undefined;
  /**
   * The root mean squared error of the base parameters of the detected
   * supplementary traffic sign. \c #candidate
   * \c TrafficSign::SupplementarySign::base has to be identical for all
   * \c #candidate supplementary traffic signs.
   */
  base_rmse?: BaseStationary | undefined;
}

/**
 * \brief A candidate for a detected supplementary sign as estimated by
 * the sensor.
 */
export interface DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign {
  /**
   * The estimated probability that this candidate is the true value.
   *
   * \note The sum of all \c #probability must be one. This
   * probability is given under the condition of \c
   * DetectedItemHeader::existence_probability.
   *
   * \rules
   * is_less_than_or_equal_to: 1
   * is_greater_than_or_equal_to: 0
   * \endrules
   */
  probability?:
    | number
    | undefined;
  /**
   * The classification of one of more supplementary signs that
   * together define this candidate.
   *
   * \note IDs, which are referenced in this message, usually
   * reference to \c DetectedXXX::tracking_id IDs.
   */
  classification?: TrafficSign_SupplementarySign_Classification | undefined;
}

function createBaseDetectedTrafficSign(): DetectedTrafficSign {
  return { header: undefined, main_sign: undefined, supplementary_sign: [] };
}

export const DetectedTrafficSign: MessageFns<DetectedTrafficSign> = {
  encode(message: DetectedTrafficSign, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      DetectedItemHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.main_sign !== undefined) {
      DetectedTrafficSign_DetectedMainSign.encode(message.main_sign, writer.uint32(18).fork()).join();
    }
    for (const v of message.supplementary_sign) {
      DetectedTrafficSign_DetectedSupplementarySign.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectedTrafficSign {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectedTrafficSign();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = DetectedItemHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.main_sign = DetectedTrafficSign_DetectedMainSign.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.supplementary_sign.push(
            DetectedTrafficSign_DetectedSupplementarySign.decode(reader, reader.uint32()),
          );
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectedTrafficSign {
    return {
      header: isSet(object.header) ? DetectedItemHeader.fromJSON(object.header) : undefined,
      main_sign: isSet(object.main_sign) ? DetectedTrafficSign_DetectedMainSign.fromJSON(object.main_sign) : undefined,
      supplementary_sign: globalThis.Array.isArray(object?.supplementary_sign)
        ? object.supplementary_sign.map((e: any) => DetectedTrafficSign_DetectedSupplementarySign.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DetectedTrafficSign): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = DetectedItemHeader.toJSON(message.header);
    }
    if (message.main_sign !== undefined) {
      obj.main_sign = DetectedTrafficSign_DetectedMainSign.toJSON(message.main_sign);
    }
    if (message.supplementary_sign?.length) {
      obj.supplementary_sign = message.supplementary_sign.map((e) =>
        DetectedTrafficSign_DetectedSupplementarySign.toJSON(e)
      );
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectedTrafficSign>, I>>(base?: I): DetectedTrafficSign {
    return DetectedTrafficSign.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectedTrafficSign>, I>>(object: I): DetectedTrafficSign {
    const message = createBaseDetectedTrafficSign();
    message.header = (object.header !== undefined && object.header !== null)
      ? DetectedItemHeader.fromPartial(object.header)
      : undefined;
    message.main_sign = (object.main_sign !== undefined && object.main_sign !== null)
      ? DetectedTrafficSign_DetectedMainSign.fromPartial(object.main_sign)
      : undefined;
    message.supplementary_sign =
      object.supplementary_sign?.map((e) => DetectedTrafficSign_DetectedSupplementarySign.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDetectedTrafficSign_DetectedMainSign(): DetectedTrafficSign_DetectedMainSign {
  return { candidate: [], base: undefined, base_rmse: undefined, geometry: 0 };
}

export const DetectedTrafficSign_DetectedMainSign: MessageFns<DetectedTrafficSign_DetectedMainSign> = {
  encode(message: DetectedTrafficSign_DetectedMainSign, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.candidate) {
      DetectedTrafficSign_DetectedMainSign_CandidateMainSign.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.base !== undefined) {
      BaseStationary.encode(message.base, writer.uint32(18).fork()).join();
    }
    if (message.base_rmse !== undefined) {
      BaseStationary.encode(message.base_rmse, writer.uint32(26).fork()).join();
    }
    if (message.geometry !== undefined && message.geometry !== 0) {
      writer.uint32(32).int32(message.geometry);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectedTrafficSign_DetectedMainSign {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectedTrafficSign_DetectedMainSign();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.candidate.push(
            DetectedTrafficSign_DetectedMainSign_CandidateMainSign.decode(reader, reader.uint32()),
          );
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.base = BaseStationary.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.base_rmse = BaseStationary.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.geometry = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectedTrafficSign_DetectedMainSign {
    return {
      candidate: globalThis.Array.isArray(object?.candidate)
        ? object.candidate.map((e: any) => DetectedTrafficSign_DetectedMainSign_CandidateMainSign.fromJSON(e))
        : [],
      base: isSet(object.base) ? BaseStationary.fromJSON(object.base) : undefined,
      base_rmse: isSet(object.base_rmse) ? BaseStationary.fromJSON(object.base_rmse) : undefined,
      geometry: isSet(object.geometry) ? detectedTrafficSign_DetectedMainSign_GeometryFromJSON(object.geometry) : 0,
    };
  },

  toJSON(message: DetectedTrafficSign_DetectedMainSign): unknown {
    const obj: any = {};
    if (message.candidate?.length) {
      obj.candidate = message.candidate.map((e) => DetectedTrafficSign_DetectedMainSign_CandidateMainSign.toJSON(e));
    }
    if (message.base !== undefined) {
      obj.base = BaseStationary.toJSON(message.base);
    }
    if (message.base_rmse !== undefined) {
      obj.base_rmse = BaseStationary.toJSON(message.base_rmse);
    }
    if (message.geometry !== undefined) {
      obj.geometry = detectedTrafficSign_DetectedMainSign_GeometryToJSON(message.geometry);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectedTrafficSign_DetectedMainSign>, I>>(
    base?: I,
  ): DetectedTrafficSign_DetectedMainSign {
    return DetectedTrafficSign_DetectedMainSign.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectedTrafficSign_DetectedMainSign>, I>>(
    object: I,
  ): DetectedTrafficSign_DetectedMainSign {
    const message = createBaseDetectedTrafficSign_DetectedMainSign();
    message.candidate =
      object.candidate?.map((e) => DetectedTrafficSign_DetectedMainSign_CandidateMainSign.fromPartial(e)) || [];
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseStationary.fromPartial(object.base)
      : undefined;
    message.base_rmse = (object.base_rmse !== undefined && object.base_rmse !== null)
      ? BaseStationary.fromPartial(object.base_rmse)
      : undefined;
    message.geometry = object.geometry ?? 0;
    return message;
  },
};

function createBaseDetectedTrafficSign_DetectedMainSign_CandidateMainSign(): DetectedTrafficSign_DetectedMainSign_CandidateMainSign {
  return { probability: 0, classification: undefined };
}

export const DetectedTrafficSign_DetectedMainSign_CandidateMainSign: MessageFns<
  DetectedTrafficSign_DetectedMainSign_CandidateMainSign
> = {
  encode(
    message: DetectedTrafficSign_DetectedMainSign_CandidateMainSign,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.probability !== undefined && message.probability !== 0) {
      writer.uint32(9).double(message.probability);
    }
    if (message.classification !== undefined) {
      TrafficSign_MainSign_Classification.encode(message.classification, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectedTrafficSign_DetectedMainSign_CandidateMainSign {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectedTrafficSign_DetectedMainSign_CandidateMainSign();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.probability = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.classification = TrafficSign_MainSign_Classification.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectedTrafficSign_DetectedMainSign_CandidateMainSign {
    return {
      probability: isSet(object.probability) ? globalThis.Number(object.probability) : 0,
      classification: isSet(object.classification)
        ? TrafficSign_MainSign_Classification.fromJSON(object.classification)
        : undefined,
    };
  },

  toJSON(message: DetectedTrafficSign_DetectedMainSign_CandidateMainSign): unknown {
    const obj: any = {};
    if (message.probability !== undefined) {
      obj.probability = message.probability;
    }
    if (message.classification !== undefined) {
      obj.classification = TrafficSign_MainSign_Classification.toJSON(message.classification);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectedTrafficSign_DetectedMainSign_CandidateMainSign>, I>>(
    base?: I,
  ): DetectedTrafficSign_DetectedMainSign_CandidateMainSign {
    return DetectedTrafficSign_DetectedMainSign_CandidateMainSign.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectedTrafficSign_DetectedMainSign_CandidateMainSign>, I>>(
    object: I,
  ): DetectedTrafficSign_DetectedMainSign_CandidateMainSign {
    const message = createBaseDetectedTrafficSign_DetectedMainSign_CandidateMainSign();
    message.probability = object.probability ?? 0;
    message.classification = (object.classification !== undefined && object.classification !== null)
      ? TrafficSign_MainSign_Classification.fromPartial(object.classification)
      : undefined;
    return message;
  },
};

function createBaseDetectedTrafficSign_DetectedSupplementarySign(): DetectedTrafficSign_DetectedSupplementarySign {
  return { candidate: [], base: undefined, base_rmse: undefined };
}

export const DetectedTrafficSign_DetectedSupplementarySign: MessageFns<DetectedTrafficSign_DetectedSupplementarySign> =
  {
    encode(
      message: DetectedTrafficSign_DetectedSupplementarySign,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      for (const v of message.candidate) {
        DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign.encode(v!, writer.uint32(10).fork())
          .join();
      }
      if (message.base !== undefined) {
        BaseStationary.encode(message.base, writer.uint32(18).fork()).join();
      }
      if (message.base_rmse !== undefined) {
        BaseStationary.encode(message.base_rmse, writer.uint32(26).fork()).join();
      }
      return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DetectedTrafficSign_DetectedSupplementarySign {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      let end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDetectedTrafficSign_DetectedSupplementarySign();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.candidate.push(
              DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign.decode(reader, reader.uint32()),
            );
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.base = BaseStationary.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.base_rmse = BaseStationary.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): DetectedTrafficSign_DetectedSupplementarySign {
      return {
        candidate: globalThis.Array.isArray(object?.candidate)
          ? object.candidate.map((e: any) =>
            DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign.fromJSON(e)
          )
          : [],
        base: isSet(object.base) ? BaseStationary.fromJSON(object.base) : undefined,
        base_rmse: isSet(object.base_rmse) ? BaseStationary.fromJSON(object.base_rmse) : undefined,
      };
    },

    toJSON(message: DetectedTrafficSign_DetectedSupplementarySign): unknown {
      const obj: any = {};
      if (message.candidate?.length) {
        obj.candidate = message.candidate.map((e) =>
          DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign.toJSON(e)
        );
      }
      if (message.base !== undefined) {
        obj.base = BaseStationary.toJSON(message.base);
      }
      if (message.base_rmse !== undefined) {
        obj.base_rmse = BaseStationary.toJSON(message.base_rmse);
      }
      return obj;
    },

    create<I extends Exact<DeepPartial<DetectedTrafficSign_DetectedSupplementarySign>, I>>(
      base?: I,
    ): DetectedTrafficSign_DetectedSupplementarySign {
      return DetectedTrafficSign_DetectedSupplementarySign.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DetectedTrafficSign_DetectedSupplementarySign>, I>>(
      object: I,
    ): DetectedTrafficSign_DetectedSupplementarySign {
      const message = createBaseDetectedTrafficSign_DetectedSupplementarySign();
      message.candidate =
        object.candidate?.map((e) =>
          DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign.fromPartial(e)
        ) || [];
      message.base = (object.base !== undefined && object.base !== null)
        ? BaseStationary.fromPartial(object.base)
        : undefined;
      message.base_rmse = (object.base_rmse !== undefined && object.base_rmse !== null)
        ? BaseStationary.fromPartial(object.base_rmse)
        : undefined;
      return message;
    },
  };

function createBaseDetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign(): DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign {
  return { probability: 0, classification: undefined };
}

export const DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign: MessageFns<
  DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign
> = {
  encode(
    message: DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.probability !== undefined && message.probability !== 0) {
      writer.uint32(9).double(message.probability);
    }
    if (message.classification !== undefined) {
      TrafficSign_SupplementarySign_Classification.encode(message.classification, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.probability = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.classification = TrafficSign_SupplementarySign_Classification.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign {
    return {
      probability: isSet(object.probability) ? globalThis.Number(object.probability) : 0,
      classification: isSet(object.classification)
        ? TrafficSign_SupplementarySign_Classification.fromJSON(object.classification)
        : undefined,
    };
  },

  toJSON(message: DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign): unknown {
    const obj: any = {};
    if (message.probability !== undefined) {
      obj.probability = message.probability;
    }
    if (message.classification !== undefined) {
      obj.classification = TrafficSign_SupplementarySign_Classification.toJSON(message.classification);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign>, I>>(
    base?: I,
  ): DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign {
    return DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign.fromPartial(base ?? ({} as any));
  },
  fromPartial<
    I extends Exact<DeepPartial<DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign>, I>,
  >(object: I): DetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign {
    const message = createBaseDetectedTrafficSign_DetectedSupplementarySign_CandidateSupplementarySign();
    message.probability = object.probability ?? 0;
    message.classification = (object.classification !== undefined && object.classification !== null)
      ? TrafficSign_SupplementarySign_Classification.fromPartial(object.classification)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
